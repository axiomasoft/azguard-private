# REST Conventions

- Use plural nouns: `/users`, `/orders`
- HTTP verbs: GET (read), POST (create), PUT/PATCH (update), DELETE
- Version via header or `/v1/` prefix
- Pagination: `?page=1&per_page=20`
