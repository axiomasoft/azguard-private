# Шаблон команды /prime — скопировать в .claude/commands/prime.md проекта

```markdown
---
description: Загрузка стартового контекста сессии — минимум, достаточный для работы
model: haiku
---

Подготовь рабочий контекст сессии:

1. Прочитай CLAUDE.md и выведи однострочную сводку правил проекта.
2. Выполни `git status` и `git log --oneline -5` — текущее состояние ветки.
3. Если в `.claude/plans/` есть незавершённый план — назови его, но НЕ читай целиком.
4. Спроси, над чем работаем. Ничего больше не читай проактивно.
```
