{
  "skill": "docker-postgres",
  "snippets": [
    {
      "file": "postgres.conf",
      "description": "Тюнинг параметров PostgreSQL",
      "tags": ["config"]
    },
    {
      "file": "init.sql",
      "description": "SQL-инициализация: расширения",
      "tags": ["sql", "init"]
    },
    {
      "file": "pgsql-service.yml",
      "description": "Сервис pgsql в compose: healthcheck pg_isready, named volume, mount init-скриптов",
      "tags": ["compose", "postgres", "healthcheck", "volumes"]
    },
    {
      "file": "init-test-db.sh",
      "description": "Создание тестовой БД <app>_test: init-скрипт для /docker-entrypoint-initdb.d + идемпотентный backfill для существующего volume",
      "tags": ["postgres", "init", "testing", "shell"]
    }
  ]
}
