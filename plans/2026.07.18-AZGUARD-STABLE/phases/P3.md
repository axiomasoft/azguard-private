# P3 — Release-готовность: cut-line, заморозка поверхности, SemVer-политика

## Phase Context

Точка «stable» (образец VLT-REL) — три шага над финальной поверхностью P2: (1) **исполнить
cut-line** фасада по замороженной target-спеке P2.5 (`root/contracts/facade-cutline.md`):
удалить 2 мёртвых `@method`+их методы менеджера, доставить `@internal` где спека требует,
ревизовать `@api`/`@internal` после переездов неймспейсов P2.1 — и зафиксировать итоговую
поверхность в `root/api-surface.md`; (2) **заморозить** её машинным гейтом — расширить
reflection-тест `tests/Unit/ApiBoundaryTest.php` до snapshot-гейта (состав `@api`-типов +
сигнатуры публичных методов с именами параметров) против закоммиченного фикстура; дрейф
поверхности краснит тест, регенерация фикстура — осознанный акт под D#; (3) **зафиксировать
политику** — SemVer 0.x (что считается breaking до 1.0, deprecate-first дисциплина, snapshot-
гейт как энфорсер), каталог известных ограничений (documented, не блокеры) и завершить путь
`0.2 → 0.3` в `docs/introduction/upgrading.md` (+RU). После закрытия фазы любое изменение
публичной поверхности — только через явный D# с bump-семантикой и регенерацией снапшота.

> **Контракт фазы (contract-first).** Потребляет: финальные структуру/имена P2 ·
> `root/contracts/facade-cutline.md` (target-спека cut-line, производит P2.5) ·
> `root/glossary.md` (P2.7) · `tests/Unit/ApiBoundaryTest.php` (существующий @api-гейт) ·
> ARCHITECT_REVIEW.md §6. Производит: `root/api-surface.md` (итоговая cut-line-поверхность) ·
> snapshot-гейт поверхности в `tests/Unit/` (+ фикстур) прогоняется существующим CI ·
> `root/semver-policy.md` · дополненный `docs/introduction/upgrading.md` (0.2→0.3, EN+RU).
> Границы: **поведенческий редизайн API завершён P2** — P3 исполняет ТОЛЬКО cut-line
> (удаление мёртвых методов + переклассификация `@api`/`@internal` по спеке P2.5), новой
> функциональности НЕ добавляет (D19); тег НЕ здесь (→ P5); тестовая инфраструктура
> углубления (docker/race/paratest/mutation-ratchet) НЕ здесь (→ P4).

## Phase Status

| ID | Title | Status | Updated |
|:--|:--|:--|:--|
| P3.1 | Исполнение cut-line фасада + ревизия @api/@internal → root/api-surface.md | 🟠 Done with deviations | 2026-07-18 |
| P3.2 | Заморозка: snapshot-гейт поверхности (reflection-фикстур) в CI | ⬜ Not started | 2026-07-18 |
| P3.3 | SemVer-политика 0.x + каталог ограничений + UPGRADING 0.2→0.3 | ⬜ Not started | 2026-07-18 |

### P3.1 — Исполнение cut-line фасада + ревизия @api/@internal → root/api-surface.md

**Status:** 🟠 Done with deviations
**Intent:** Исполнить cut-line публичного фасада по замороженной target-спеке P2.5 (`root/contracts/facade-cutline.md`): удалить мёртвые `@method` (`tryPermission`, `panelIdForPermission`) и их методы на `AzGuardManager` (0 потребителей), доставить `@internal` там, где спека предписывает и P2.3 ещё не проставил, оставить `hasContextGuard` публичным с задокументированным локусом; провести сквозную ревизию `@api`/`@internal` после переездов неймспейсов P2.1; зафиксировать итоговую поверхность в `root/api-surface.md`.
**Why:** Фасад шире локуса (B-05): 2 мёртвых резолвера + позиционные дубли + предикаты трейта против канона spatie (трейт/Gate — центр). P2.5 произвела target-спеку, но НЕ трогала код (SemVer-операция под заморозку — P3, D14). Пере­езды P2.1 (роспуск Support/) могли сдвинуть `@api`/`@internal`-докблоки — нужна сверка до заморозки. RAG:— (repo-grounded: findings/P0-axis-b-fluent.md §C-B4/§B-05, research/03-p2-canon.md §5; локус-канон RAG:✅ findings/P0-rag-fluent-dx.md Запрос 2).
**Scope Included:** (1) Применить каждую строку-вердикт `root/contracts/facade-cutline.md`: удалить `tryPermission`/`panelIdForPermission` из `@method`-блока фасада И одноимённые методы `AzGuardManager` (сверив 0 потребителей grep'ом перед удалением); проставить `@internal` на `grant`/`revoke`/`grants` (если P2.3 не проставил как часть грамматики — сверить, не дублировать), на `isSuperAdmin`; оставить `hasContextGuard` `@api` + докблок-локус «полезен без user». (2) Сквозная ревизия: пройти по всем перевезённым P2.1 типам (Panels/Permissions/Configuration/Runtime/Abilities/Auth/Database\Schema) — `@api`/`@internal`-докблоки сохранены и корректны после переезда; `@internal`-инфра (RequestState, ScopedRoleCache) не всплыла в `@api`-сигнатуры. (3) Написать `root/api-surface.md` — человекочитаемый реестр финальной публичной поверхности (фасад @method после cut, `@api`-контракты обоих доменов, Testing, публичные VO/enum, middleware-алиасы, config-ключи) как SSOT для снапшота P3.2 и каталога P3.3.
**Scope Excluded:** Проставление `@internal` на shorthands, если это уже сделал P2.3 (только сверка); поведенческий редизайн сигнатур (завершён P2); построение snapshot-гейта (P3.2); SemVer-политика/UPGRADING (P3.3); правка docs-глав кроме FQCN-примеров, задетых удалением методов.
**Inputs:** `root/contracts/facade-cutline.md` (P2.5, 17 строк-вердиктов) · research/03-p2-canon.md §5 · findings/P0-axis-b-fluent.md §C-B4 · Completion Notes P2.1 (карта переездов неймспейсов) · Completion Notes P2.3 (какие shorthands уже @internal).
**Files:** packages/core/src/Facades/AzGuard.php (@method-блок: удаление 2, @internal) · packages/core/src/AzGuardManager.php (удаление tryPermission/panelIdForPermission) · packages/core/src/Contracts/AzGuardManagerInterface.php (снять удаляемые методы из контракта, если объявлены) · перевезённые P2.1 типы в packages/core/src/{Panels,Permissions,Configuration,Runtime,Abilities,Auth,Database/Schema}/** (сверка докблоков) · tests/Unit/ApiBoundaryTest.php (должен остаться зелёным) · tests/** (вызовы удаляемых методов, если есть) · plans/2026.07.18-AZGUARD-STABLE/root/api-surface.md (создать) · docs/** (FQCN-примеры задетых методов, если встречаются).
**Required Reads:** `root/contracts/facade-cutline.md` (P2.5) · research/03-p2-canon.md §5 · findings/P0-axis-b-fluent.md §C-B4 · packages/core/src/Facades/AzGuard.php · packages/core/src/AzGuardManager.php · tests/Unit/ApiBoundaryTest.php · ARCHITECT_REVIEW.md §6 (курированный фасад, union-only).
**Implementation Rules:** Удаление метода — ТОЛЬКО после `grep`-подтверждения 0 потребителей во всём монорепо (src+tests+docs); если grep находит потребителя, не попавшего в спеку C-B4 → эскалация §10 (public contract, спека неполна), НЕ удалять молча. `@internal` — докблок-тег, не смена видимости (метод остаётся `public` для внутренней оркестрации, скрыт из публичного контракта; путь потребителя = fluent/трейт). Не менять сигнатуры оставляемых методов (поведение заморожено P2). `hasContextGuard` остаётся `@api` — не переклассифицировать. `root/api-surface.md` пишется от РЕАЛЬНОГО кода (reflection/чтение), не от спеки — это реестр факта после cut, вход снапшота. Двух-домовый канон контрактов (P2.1) отражён в реестре. Запрещён `git add -A` (Execution Rules) — коммит по Files.
**Code Guidance:** cut-line — необратимая SemVer-операция; действовать строго по спеке, «заодно почистить» соседнее НЕ добавлять (scope creep ломает заморозку). Не превращать удаление в переименование. Согласованность с P2.3: если shorthands уже `@internal` — НЕ ставить второй тег/не трогать (иначе дифф-шум); Completion Notes P2.3 — источник истины. `root/api-surface.md` обязан быть самодостаточным входом для холодного P3.2-исполнителя (перечисляет ровно то, что снапшот заморозит). if-then: если ревизия P2.1-переездов обнаружит `@api`-тип, потерявший докблок при переезде → восстановить тег (не изобретать новую границу), отметить в Completion Notes; если `@internal`-тип протёк в `@api`-сигнатуру (ApiBoundaryTest бы покраснел) → это дефект P2, эскалация §10. Тесты не добавляются (cut + доки); ApiBoundaryTest обязан остаться зелёным.
**Validation:** `grep -rE 'tryPermission|panelIdForPermission' packages tests docs --include='*.php' --include='*.md'` == пусто · `php vendor/bin/pest tests/Unit/ApiBoundaryTest.php` зелёный (граница цела после cut) · `composer analyse` зелёный · `composer test:unit` зелёный · `composer lint:check` зелёный · `test -f plans/2026.07.18-AZGUARD-STABLE/root/api-surface.md` и реестр покрывает фасад+контракты+Testing+config-ключи.
**Deliverables:** Cut-line исполнен (2 метода удалены, @internal проставлены по спеке, hasContextGuard задокументирован); @api/@internal сверены после P2.1; `root/api-surface.md` (SSOT поверхности); ApiBoundaryTest зелёный; зелёные analyse/unit/lint.
**Completion Notes:** Session: sonnet/high (plan-run, Routing §3/D28 — точное совпадение, недобора/перебора нет). Исполнено СТРОГО по `root/contracts/facade-cutline.md` (SSOT, D29), а не по буквальному тексту Scope Included/Files этого item'а (тот писался ДО P2.5/D29 и всё ещё несёт устаревшую посылку «0 потребителей» — расхождение уже разрешено D29, эскалация не требуется): (1) фасад `AzGuard.php` — из докблока убраны 2 `@method`-строки (`tryPermission`, `panelIdForPermission`); `isSuperAdmin` перенесён из блока «Actor API» в новую секцию `--- @internal ---` вместе с уже проставленными P2.3 `grant`/`revoke`/`grants`; `hasContextGuard` оставлен `@api` с локус-нотой (контейнерный чек vs `$user->hasContextGuard()`). (2) Методы `tryPermission`/`panelIdForPermission`/`isSuperAdmin` НЕ удалены из `AzGuardManager`/`AzGuardManagerInterface` (удаление сломало бы реальные швы `Permissions/PermissionName.php:31` и `Concerns/HasScopedRoles.php:324` — оба подтверждены §3 спеки) — получили метод-докблок `@internal` с адресом на `facade-cutline.md`. (3) Сквозная ревизия P2.1-переездов (Panels/Permissions/Configuration/Runtime/Abilities/Auth/Database\Schema): сравнение `@api`/`@internal`-тегов до/после `git show 9cc1897^` не выявило ни одной потери тега (`PermissionKey`/`Panel` сохранили `@api`, `RequestState`/`ScopedRoleCache` — `@internal`); большинство перевезённых файлов исходно без тега (baseline: 88/127 файлов core без `@api`/`@internal` вообще) — это норма пакета, не регрессия P2.1. (4) `root/api-surface.md` написан от факта кода (reflection/grep на item-коммите): 32 `@api`-типа core (18 Contracts/ + 6 Registry/Contracts/ + Facade + Panel + PermissionKey + PermissionSet + 4 Testing), финальный `@method`-состав фасада (11 @api + 3 Testing + fake() + 4 @internal), middleware `::using()`-сигнатуры, Blade-директивы, config-ключи core+filament; явно отмечает вне-скоупа (context/filament без `@api`/`@internal`-конвенции — 2 файла в context несут метод-уровня `@internal`, 0 в filament; `Attributes/*` документированы, но не тегированы — пред-P2.1 гэп, не фикс этого item'а). Follow-up Audit P2 (`## Audit P2 — 2026-07-18`, phases/P2.md) свёрнут: **F1** — `composer refactor` прогнан по всем 6 файлам (`ContextGrantBuilder.php`/`AzGuardManager.php`/`GrantBuilder.php`/`PermissionKey.php`/`PermissionName.php`/`AzGuardFake.php`), обнаружен и устранён скрытый конфликт rector↔pint (rector считает импорт, используемый только в PHPDoc `{@see Class}`, мёртвым; pint `fully_qualified_strict_types` наоборот хочет короткую форму+импорт — бесконечный пинг-понг) — 4 докблок-`@see`-ссылки на `Panel`/`PanelProvider`/`AzGuard`-фасад переписаны прозой без `{@see}`-тега, после чего `composer refactor`+`composer lint` сходятся одновременно к нулю (`refactor:check` 0 файлов, `lint:check` passed); формулировка «предсуществующий/не задет» была ошибочной ДО F1 (все 6 файлов внесены P2-items — см. Audit P2 F1), теперь неактуальна (долг устранён). **F3/F4/F6** — `roadmap.md` P3.1/P3.3 ресинхронизированы под D28 (fable→sonnet/high); `plan.md:168` (P2.10 Update Log) сокращён с 458 до ~150 символов «Что»; D28/D29 переставлены в хронологический порядок в Decision Log. **F2** оставлен P3.3 по прямому указанию команды (UPGRADING-консолидация — работа отдельного item'а), зафиксирован в Pending Work ниже. Validation: `grep -rE 'tryPermission|panelIdForPermission' packages tests docs --include='*.php' --include='*.md'` НЕ пуст (18 строк на `448028c`) — ОЖИДАЕМО по D29 (методы сохранены как `@internal`, а не удалены); фактическая проверка — facade docblock ЧИСТ (`grep -c 'tryPermission\|panelIdForPermission' packages/core/src/Facades/AzGuard.php` == 0 на `448028c`) и оба метода несут `@internal`. `php vendor/bin/pest tests/Unit/ApiBoundaryTest.php` 2 passed/215 assertions · `composer analyse` 0 errors · `composer test:unit` 286 passed/787 · `composer lint:check` passed · `composer refactor:check` 0 файлов (F1) · `test -f root/api-surface.md` ✓. Сверх ТЗ: полный `composer test` 666 passed/1772 assertions (без регрессий, совпадает с Audit P2 baseline на `46e057c`). Item-коммит `448028c`: 10 files, +173/−24 (`git show --stat 448028c`).
**Pending Work:** F2 (Audit P2) — внести в консолидированную UPGRADING-главу (P3.3) пропущенные breaking: P2.4 `panel_check` арг-флип `{panel},{perm}`→`{perm},{panel}` и P2.1 переезды публичных типов (`AzGuard\PermissionKey`/`AzGuard\PanelProvider` → новые неймспейсы) — материал восстановим из Completion Notes P2.1/P2.4. Регенерация bundled boost-скилла целиком (не только исправленная строка isSuperAdmin) после cut-line — по-прежнему кандидат отдельного прогона `laravel-package-generate-skill` (facade-cutline.md §5), эта правка ограничилась одной строкой примера. `Attributes/*` без `@api`-тегов (root/api-surface.md §8) — кандидат тегирования вне этого item'а.
**Known Deviations:** (1) material: item-коммит несёт дифф вне литерального перечня Files — `packages/context/src/ContextGrantBuilder.php`, `packages/core/src/Grants/GrantBuilder.php`, `packages/core/src/Testing/AzGuardFake.php` (все три — F1 `composer refactor` авто-фикс, поведение-сохраняющий) и `packages/core/resources/boost/skills/azguard-development/SKILL.md` (канонизация примера `isSuperAdmin`, прямое следствие де-публикации #11 facade-cutline.md) — ни один не в Files item'а; предписаны follow-up-скоупом launch-команды аудита (F1) и самой спекой cut-line (#11), не «улучшение заодно». (2) SemVer-breaking: НЕТ нового — `tryPermission`/`panelIdForPermission`/`isSuperAdmin` фасадные формы становятся недокументированными (`@internal`), но runtime не меняется (методы вызываемы как раньше) — это де-публикация, не removal; уже объявлено легальным facade-cutline.md/D29. (3) process: буквальный текст Scope Included/Files этого item'а («удалить методы AzGuardManager») расходится с фактическим исполнением (методы сохранены как `@internal`) — расхождение разрешено ЗАРАНЕЕ через D29 (создан при закрытии P2.5, ДО детализации этого текста устаревшим) и явно санкционировано Intent-строкой item'а («по замороженной target-спеке P2.5») — не новый дефект исполнителя.
**Escalation Needed:** no

### P3.2 — Заморозка: snapshot-гейт поверхности (reflection-фикстур) в CI

**Status:** ⬜ Not started
**Intent:** Расширить существующий reflection-тест `tests/Unit/ApiBoundaryTest.php` (либо новый sibling `ApiSurfaceSnapshotTest.php`) до snapshot-гейта: перечислить публичную поверхность (все `@api`-типы core + их публичные методы с сигнатурами и именами параметров), сериализовать в закоммиченный фикстур, на каждом прогоне сверять факт с фикстуром; любой дрейф (добавление/удаление/смена сигнатуры/имени параметра) краснит тест до осознанной регенерации фикстура.
**Why:** «stable»-точка фиксируется машинным гейтом, а не тегом (D5): без снапшота изменение публичной поверхности проходит молча. Существующий ApiBoundaryTest проверяет ГРАНИЦУ (@api несёт тег, @internal не течёт), но не СОСТАВ/сигнатуры — дрейф состава невидим. Для пре-1.0 строгой заморозки over-sensitivity снапшота (краснеет и на BC-safe добавления) — желаемое свойство: любое изменение поверхности осознанно (D20). RAG:✅ 2026-07-18 (perplexity: reflection-снапшот механически надёжен для детекта дрейфа; roave/backward-compatibility-check — tag-boundary BC-комплемент, для 0.x-строгой-заморозки снапшот+D#-дисциплина достаточны — findings/, RAG-выжимка в D20).
**Scope Included:** (1) Reflection-энумератор публичной поверхности: `@api`-типы core (Contracts/ + Registry/Contracts/ + прочие `@api`-помеченные VO/enum/фасад/Testing по `root/api-surface.md`), для каждого — публичные методы, объявленные в самом типе, с нормализованной сигнатурой (имя метода, порядок параметров, ИМЕНА параметров, типы, дефолты-наличие, тип возврата); детерминированная сортировка. (2) Сериализация в закоммиченный фикстур `tests/Fixtures/api-surface.snapshot.php` (или `.json`) — версионируемый SSOT поверхности. (3) Тест сверяет факт с фикстуром, при расхождении — понятная diff-ошибка (что добавилось/ушло/сменило сигнатуру). (4) Команда/режим регенерации фикстура (env-флаг `AZ_UPDATE_API_SNAPSHOT=1` или отдельный composer-скрипт) с инструкцией в докблоке теста «регенерация = осознанный акт под D# + bump». (5) Самопроверка гейта: временная мутация сигнатуры краснит тест (записать в Completion Notes, мутацию откатить).
**Scope Excluded:** Адоптация roave/backward-compatibility-check как dev-зависимости (отложено, D20 — не тащить новый dev-dep, снапшот+D# достаточны для 0.x); снапшот filament/context-пакетов (пока только core-поверхность — тот же охват, что ApiBoundaryTest; расширение на пакеты — follow-up, зафиксировать в каталоге P3.3); политика SemVer (P3.3); отдельный CI-джоб (снапшот-тест бежит в существующем pest-прогоне tests.yml/static-analysis — новый workflow не нужен).
**Inputs:** `root/api-surface.md` (P3.1, реестр поверхности) · tests/Unit/ApiBoundaryTest.php (reflection-паттерн-образец: `$classesIn`, `$hasTag`) · research/03-p2-canon.md §5 · .github/workflows/tests.yml + static-analysis.yml (где бежит pest — снапшот попадёт автоматически).
**Files:** tests/Unit/ApiBoundaryTest.php (расширить) ИЛИ tests/Unit/ApiSurfaceSnapshotTest.php (создать, sibling) · tests/Fixtures/api-surface.snapshot.php (создать — фикстур) · composer.json (опц. скрипт `test:api-snapshot` / регенерация) · tests/Pest.php (если фикстур-путь требует биндинга).
**Required Reads:** tests/Unit/ApiBoundaryTest.php (полностью — переиспользовать `$classesIn`/`$hasTag`/reflection-обход) · `root/api-surface.md` (P3.1) · D20 (механика заморозки, границы over-sensitivity) · ARCHITECT_REVIEW.md §6.
**Implementation Rules:** Переиспользовать существующий reflection-обход ApiBoundaryTest (`$classesIn`, `RecursiveDirectoryIterator`, фильтр `@api` через `$hasTag`) — НЕ писать второй параллельный энумератор с иной логикой «что публично». Снапшот включает ТОЛЬКО `@api`-поверхность (не `@internal`) — иначе гейт замораживает внутренности (урок RAG: фильтр internal обязателен). Имена параметров ВКЛЮЧАЮТСЯ в снапшот: проект использует named-arguments (скилл `php:named-arguments`) — имя параметра часть контракта, его смена breaking. Сериализация детерминирована (сортировка типов и методов) — иначе ложный дрейф. Регенерация — явный флаг/скрипт, НЕ авто-обновление при расхождении (авто-обновление убивает смысл гейта). Фикстур закоммичен (версионируется). Тест-ошибка — читаемый diff, не голый `toBe` на многострочном дампе. Не ослаблять существующие 2 теста ApiBoundaryTest (граница + утечка) — снапшот их ДОПОЛНЯЕТ.
**Code Guidance:** Снапшот пре-1.0 нарочно строгий (over-sensitive) — это фича заморозки, не баг: регенерация под D# — единственный легальный путь смены поверхности после P3. Не моделировать «BC-safe vs breaking»-политику в тесте (это работа человека+D#, не гейта — RAG-урок: снапшот детектит дрейф, политику несёт semver-policy.md). Фикстур-формат — стабильный к нерелевантным перестановкам (сортировать ключи). if-then: если reflection на публичном методе спотыкается о тип из внешнего вендора (Laravel Authenticatable и т.п.) → фиксировать FQCN как строку, не резолвить в снапшот-структуру глубже одного уровня (иначе снапшот ловит дрейф Laravel, не azguard); если named-параметры создают шум на промо­утированных конструкторах → включать, но задокументировать в докблоке, что это осознанная строгость. Самопроверка (мутация → red) ОБЯЗАТЕЛЬНА до закрытия — гейт, который не краснеет, бесполезен.
**Validation:** `php vendor/bin/pest tests/Unit/ApiBoundaryTest.php` (или `ApiSurfaceSnapshotTest.php`) зелёный на закоммиченном фикстуре · самопроверка: временная правка сигнатуры любого `@api`-метода → тест КРАСНЫЙ (показать в Completion Notes, откатить) · регенерация флагом/скриптом воспроизводит идентичный фикстур на неизменённом коде (идемпотентность) · `composer test:unit` зелёный · `composer analyse` зелёный · `composer lint:check` зелёный.
**Deliverables:** snapshot-гейт поверхности (расширенный/новый reflection-тест) + закоммиченный фикстур `tests/Fixtures/api-surface.snapshot.*` + режим регенерации под D#; самопроверка «мутация→red» подтверждена; бежит в существующем CI; зелёные unit/analyse/lint.
**Completion Notes:** —
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P3.3 — SemVer-политика 0.x + каталог ограничений + UPGRADING 0.2→0.3

**Status:** ⬜ Not started
**Intent:** Зафиксировать SemVer-политику 0.x (что считается breaking до 1.0, deprecate-first дисциплина, snapshot-гейт P3.2 как энфорсер, путь смены поверхности после заморозки), собрать каталог известных ограничений (documented, не блокеры) и завершить путь `0.2 → 0.3` в `docs/introduction/upgrading.md` (+RU) — сквозная сводка ВСЕХ breaking из P1+P2.
**Why:** «stable»-точка требует явного контракта стабильности (что потребитель вправе ожидать до 1.0) и честного каталога ограничений, иначе заморозка — молчаливая. UPGRADING точечно дополнялся P1 (query-scope) и P2.9 (wildcard-флип) — нужна консолидированная 0.2→0.3-глава до тега P5. RAG:— (repo-grounded: brief/00-brief.md п.7/п.8, ARCHITECT_REVIEW.md deprecate-first, findings/recon-api-surface-2026-07-18.md §3, research/02-backlog.md «Хвосты в P4»).
**Scope Included:** (1) `root/semver-policy.md`: определение публичной поверхности (= `@api`-типы, замороженные снапшотом P3.2; `@internal` вне гарантий); что breaking до 1.0 (удаление/переименование/смена сигнатуры или имени параметра `@api`-элемента); deprecate-first дисциплина (пометка `@deprecated` на цикл перед удалением, где возможно); snapshot-гейт как машинный энфорсер; процедура легальной смены поверхности после P3 — D# + регенерация снапшота + bump-решение; ссылка на roave/backward-compatibility-check как tag-boundary-комплемент (отложен, D20). (2) Каталог ограничений (documented, не блокеры) — раздел в `root/semver-policy.md` или `root/known-limitations.md`: кросс-процессный Redis race-тест + epoch unbounded/без reset (→ P4, из C-05/T6); Octane RequestState + «панель в jobs ставится явно» (→ P4/документированный контракт, из C-14); legacy `WildcardPermissionMatcher` — opt-out на один deprecate-цикл (D18); headless — doc-only, рантайм panel-less НЕ строится, fail-closed (D14/A-06); снапшот замораживает только core-поверхность (filament/context — follow-up, из P3.2); удаление публичного `InvalidCatalogException` — отложенный breaking (REMAINDER). (3) `docs/introduction/upgrading.md` (+ `docs/ru/introduction/upgrading.md`): консолидированная глава «0.2 → 0.3» — все breaking P1+P2 (query-scope fail-closed контракт D10; единый grant-корень + shorthands @internal + facade cut D16/P3.1; config→fluent Filament + middleware `::using()` + флип порядка аргументов D17; wildcard-флип на Hierarchical D18; роспуск Support/ — новые FQCN D15), каждый с old→new и grep-командой миграции.
**Scope Excluded:** Присвоение конкретной версии тега (0.3.0 vs иная — open-question Q1, решает владелец до P5.2); сам тег (P5); правка снапшот-гейта (P3.2); новый repo-root `UPGRADING.md` (канон — `docs/introduction/upgrading.md`, D21 — не плодить дубль VitePress-доки); исполнение follow-up ограничений (только каталогизировать).
**Inputs:** `root/api-surface.md` (P3.1) · Completion Notes P1.* и P2.* (перечень breaking) · plan.md §5 D10/D16/D17/D18/D19/D20 · findings/recon-api-surface-2026-07-18.md §3 · research/02-backlog.md §«Хвосты в P4» + §«Отклонено» · docs/introduction/upgrading.md (существующий, дополнить) · `root/glossary.md` (P2.7, термины).
**Files:** plans/2026.07.18-AZGUARD-STABLE/root/semver-policy.md (создать) · plans/2026.07.18-AZGUARD-STABLE/root/known-limitations.md (создать ИЛИ раздел в semver-policy.md) · docs/introduction/upgrading.md (консолидированная 0.2→0.3-глава) · docs/ru/introduction/upgrading.md (RU-зеркало) · docs/.vitepress/config при добавлении навигации на semver-политику, если решено вынести в docs.
**Required Reads:** docs/introduction/upgrading.md (существующая структура — дополнять в её стиле) · plan.md §5 (D10/D16/D17/D18 — источник breaking) · findings/recon-api-surface-2026-07-18.md §3 (долги/follow-ups) · research/02-backlog.md §«Хвосты в P4» · Completion Notes закрытых P1/P2 (что реально сломалось) · **P3.1 Pending Work выше (F2 из `## Audit P2 — 2026-07-18`, phases/P2.md): агрегат P2 Known Deviations пропускает P2.4 `panel_check` арг-флип и P2.1 переезды публичных типов — внести оба в UPGRADING здесь** · ARCHITECT_REVIEW.md §6 (deprecate-first).
**Implementation Rules:** SemVer 0.x-семантика — стандарт semver.org (minor может ломать до 1.0), НО проект принимает deprecate-first-дисциплину поверх (записать как проектное правило, не выдавать за требование spec). Каталог ограничений — ЧЕСТНЫЙ: каждое ограничение с причиной и адресом (→ P4 / doc-only / opt-out-цикл), не приукрашивать. UPGRADING собирается из ФАКТА (Completion Notes закрытых P1/P2), не из плана-намерения — если Completion Notes расходятся с D-записью, истина = что реально в коде (grep-проверить old→new). RU-паритет держать сразу (docs-parity-gate покраснеет иначе). Каждый old→new в UPGRADING сопровождается работающей grep-командой миграции (как существующие в upgrading.md:60-63). Не создавать repo-root UPGRADING.md (D21). `root/*.md` — судьба docs проекта (SKILL §3), при архивации плана переезжают в docs/.
**Code Guidance:** Политика и каталог — доки, кода не меняют (тестов нет, кроме docs-parity). semver-policy.md обязана назвать snapshot-гейт P3.2 машинным энфорсером и описать процедуру регенерации под D# — иначе политика и гейт рассинхронизированы. UPGRADING — сверять old→new grep'ом по РЕАЛЬНОМУ коду перед записью (P1/P2 могли отклониться от D-намерения — Known Deviations). if-then: если Completion Notes P1/P2 показывают breaking, не предвиденный D-логом (эмерджентный при исполнении) → внести в UPGRADING + отметить в Completion Notes P3.3 (каталог обязан быть полным); если ограничение из recon §3 оказалось закрытым в P1 (напр. OOM `composer test` — D-06 W1) → НЕ вносить в каталог (не ограничение больше), сверить статус. RU-зеркало — точный перевод, не сокращать.
**Validation:** `test -f plans/2026.07.18-AZGUARD-STABLE/root/semver-policy.md` · каталог ограничений перечисляет ≥6 пунктов с адресом (→P4/doc-only/opt-out) · `bash bin/docs-parity-gate.sh` зелёный (EN↔RU upgrading в паритете) · каждая old→new-строка UPGRADING имеет grep-команду · grep old→new паттернов по коду подтверждает актуальность (нет мёртвых миграций) · semver-policy.md ссылается на snapshot-гейт P3.2 и процедуру D#-регенерации.
**Deliverables:** `root/semver-policy.md` (политика 0.x + deprecate-first + энфорсер) + каталог ограничений; консолидированная 0.2→0.3-глава upgrading.md EN+RU; docs-parity зелёный; поверхность готова к тегу P5.
**Completion Notes:** —
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

## Phase Handoff

—
