# P4 — Тест-углубление (ось корректности): docker БД-матрица, race, параллельные прогоны, mutation-ratchet

## Phase Context

Углубление надёжности по образцу лестницы vaulter (ступени 1/3/5, без load-оси — D4):
docker-стенд с реальными сервисами (D24: SQLite + Postgres 16 + MySQL 8 + Redis 7; MariaDB
опц.; Redis-путь тестируется И на database cache-драйвере — shared-hosting без Redis),
зеркальный БД-лейн тестов (урок vaulter: на sqlite самоскипается БД-специфичный код),
параллельные прогоны (paratest — sqlite:memory самоизолируется по процессу, RAG P4-paratest),
кросс-процессные race-тесты (хвосты C-05 Redis + C-14 Octane RequestState), mutation-ratchet
(честный замер, пороги только вверх, ревизия excludes D-08), чистка тестовых дыр (мёртвый
suite UnitFilament, phpstan-baseline 35→0). Плюс collation-hardening RBAC-ключей под MySQL
(D24: главный портируемый риск — case-insensitive collation схлопывает ключи; owner D23
«MySQL first-class» + D10 fail-closed). Фаза — только тесты/инфраструктура/санкционированная
collation-миграция; поведение публичного API (заморожено P3) не меняется — найден баг →
эскалация §10 + D# с bump-семантикой P3.

**Ремедиация portability (D30, эскалация P4.2 §10):** реальный прогон лейна P4.2 против PG16/
MySQL8 вскрыл portability-баги в миграциях/query-builder (findings/P4.2-db-portability-failures.md);
recon якорей (findings/P4.2-remediation-anchors-2026-07-18.md) свёл 8 симптомов к 4 корням —
2 «класса» оказались каскадами (PG-boolean-суперадмин и MySQL-«table exists» — следствия сбоя
миграции 000005). Ремедиация — санкционированное контракт-блоком фазы исключение «прикладной код не
меняется, КРОМЕ багфиксов через эскалацию §10»; синтез структуры — research/04-p4.2-remediation.md.
Фаза выросла с 7 до 15 items (P4.2 re-scope + P4.8/P4.9/P4.10, P4.11 fixture portability,
P4.12 portable morph-index names, P4.13 forbidden-digest repair и P4.14 PG rollback-test
recovery и P4.15 ULID Testbench refresh-state isolation; P4.7 расширен).

Порядок исполнения последователен (scope связан по данным, оркестрация НЕ объявляется —
§7-критерий не сработал; §7-разбор — research/04 §3: общий docker-стенд + MySQL-каскад маскирует
нижележащие сбои, R1 обязан быть первым): P4.1 (стенд ✓) → **P4.2 (лейн-харнесс + тест-фикстура) →
P4.8 (миграция 000005: COALESCE+down-order) → P4.11 (PG wildcard fixture) → P4.7 (миграции 000002/000010: key-length+collation) →
P4.9 (filament LIKE-escape) → P4.12 (portable explicit morph-index names) → P4.13 (permitted
index-name digest) → P4.14 (PG rollback-test recovery) → P4.15 (ULID test isolation) → **P4.10 (green-proof обоих лейнов + CI-джоб)** → P4.3 (paratest sqlite) →
P4.4 (race Redis/Octane) → P4.5 (mutation) → P4.6 (чистка). Coupling D6: `.github/workflows/tests.yml`,
`phpunit.xml`, `composer.json` scripts, `tests/TestCase.php`, миграции 000002/000005/000010 —
трогаются несколькими items фазы (владение файлом — research/04 §2: каждая миграция правится ровно
одним item'ом).

> **Контракт фазы (contract-first).** Потребляет: замороженную поверхность P3 (поведение API
> менять нельзя — только тесты/инфраструктура; найден баг → эскалация, правка через D# c
> bump-семантикой P3) · findings/recon-test-ci-2026-07-18.md · findings/recon-db-portability-2026-07-18.md ·
> решение Q2 (состав docker-матрицы — зафиксировано D24). Производит: docker-стенд (compose +
> Makefile/composer-таргеты) · БД-лейн CI (PG+MySQL) · paratest-прогон · кросс-процессные
> race-тесты · поднятые mutation-пороги · закрытые тестовые дыры · collation-hardening
> миграции RBAC-ключей · **portability-фиксы миграций/query-builder (D30/D37): COALESCE morph-type-aware
> (000005), key-length composite-unique (000002/000010), down()-порядок MySQL FK, LIKE-escape MySQL,
> зелёные PG/MySQL-лейны + CI-джоб · P4.12/P4.13 internal `MorphColumns` explicit short table-aware index names без forbidden digest · P4.14 transaction-safe rollback regression proof · P4.15 class-local ULID Testbench refresh-state isolation**. Границы: прикладной код пакетов не меняется, КРОМЕ (а)
> санкционированной collation+key-length миграции P4.7 и (б) багфиксов portability через эскалацию §10
> (P4.8/P4.9 — миграция 000005 core + filament LIKE-запрос) — оба: portability-conformance под
> уже-корректный контракт, НЕ редизайн, @api-снапшот P3.2 не задет (миграции/query-builder не @api;
> тронулся снапшот → §10 + D# с bump P3); релиз/тег — P5.

## Phase Status

| ID | Title | Status | Updated |
|:--|:--|:--|:--|
| P4.1 | Docker-стенд: compose с реальными сервисами (D24) | 🟢 Done | 2026-07-18 |
| P4.2 | БД-лейн-харнесс (commit) + генерализация хрупкого теста + фикстура expires_at (re-scope D31) | 🟢 Done | 2026-07-18 |
| P4.8 | Portability миграции 000005: COALESCE morph-type-aware + down()-порядок MySQL (D30) | 🟢 Done | 2026-07-21 |
| P4.11 | PG wildcard: portability именованных role fixtures (D36) | 🟢 Done | 2026-07-21 |
| P4.7 | Key-length + collation-hardening RBAC-ключей 000002/000010 (D24+D32) | 🟠 Done with deviations | 2026-07-21 |
| P4.9 | Portability filament: LIKE-escape под MySQL-грамматику (D30) | 🟢 Done | 2026-07-21 |
| P4.12 | Portable explicit names для UUID morph indexes (D37) | 🟢 Done | 2026-07-22 |
| P4.13 | P4.12 repair: permitted deterministic digest для morph-index names (D38) | 🟢 Done | 2026-07-22 |
| P4.14 | PG rollback-test: transaction-safe expected failure recovery (D39) | 🟢 Done | 2026-07-22 |
| P4.15 | ULID morph Testbench refresh-state isolation (D40) | 🟠 Done with deviations | 2026-07-22 |
| P4.10 | Green-proof обоих лейнов + CI-джоб test-db-matrix + baseline→resolved (D30/D41) | 🟢 Done | 2026-07-22 |
| P4.3 | Параллельные прогоны (paratest) + hardening random order | 🟢 Done | 2026-07-22 |
| P4.4 | Кросс-процессные race-тесты (C-05 Redis + C-14 Octane) | 🟠 Done with deviations | 2026-07-22 |
| P4.5 | Mutation-ratchet: честный замер, пороги вверх, ревизия excludes | 🟡 In progress | 2026-07-22 |
| P4.6 | Чистка тестовых дыр (UnitFilament, baseline→0, verify D-01/D-06) | ⬜ Not started | 2026-07-18 |

### P4.1 — Docker-стенд: compose с реальными сервисами (D24)

**Status:** 🟢 Done
**Intent:** Собрать `docker-compose.yml` с реальными сервисами (Postgres 16, MySQL 8, Redis 7)
и Makefile-таргеты подъёма/останова стенда — фундамент для БД-лейна (P4.2) и race-тестов (P4.4).
**Why:** recon §4 — docker/compose в репо НЕТ; тесты только на sqlite:memory → БД-специфичный
код (collation, cross-process lock) не воспроизводится (урок vaulter: «зелено на sqlite, иначе
на PG/MySQL»). RAG:— (repo-grounded: findings/recon-test-ci-2026-07-18.md §4).
**Scope Included:** `docker-compose.yml` (сервисы `postgres:16`, `mysql:8`, `redis:7-alpine`,
у каждого healthcheck — `pg_isready` / `mysqladmin ping` / `redis-cli ping`; named volumes для
данных; проброс портов; креды через `.env`-переменные, НЕ хардкод); Makefile-таргеты
`up`/`down`/`logs`/`ps`; раздел «Локальная БД-матрица» в `DEVELOPMENT.md` (как поднять стенд).
**Scope Excluded:** CI-джобы и переключение тест-коннекшена (P4.2); composer `test:pgsql/test:mysql`
scripts (P4.2 — им нужен env-switch в TestCase); collation-init (P4.7); e2e/Playwright-стек
(vaulter ступень 2 — вне D4); Dockerfile образа пакета (azguard — библиотека без своего
рантайма, тесты бегут на ХОСТЕ против сервисов, не в контейнере).
**Inputs:** findings/recon-test-ci-2026-07-18.md §4; findings/recon-db-portability-2026-07-18.md
Takeaway (состав матрицы); plan.md D24 (утверждённый состав).
**Files:** `docker-compose.yml` (new), `Makefile` (new), `.env.example` (new, если нужен), `DEVELOPMENT.md`.
**Required Reads:** findings/recon-test-ci-2026-07-18.md §4; findings/recon-db-portability-2026-07-18.md Takeaway; скилл `devops:docker-postgres`; скилл `devops:docker-services`; phpunit.xml (текущие env); tests/TestCase.php (текущий коннекшен).
**Implementation Rules:** healthcheck ОБЯЗАТЕЛЕН у каждого сервиса (потребители P4.2/P4.4
ждут `service_healthy`, не голый `depends_on`); данные — named volumes (не bind); имя тест-БД
содержит подстроку `test` (инвариант test-isolation-guard); MySQL — образ `mysql:8` с дефолтной
collation (collation-hardening делает P4.7, стенд её НЕ преднастраивает); секреты только через
`.env`, `.env` в `.gitignore`, в git — `.env.example`.
**Code Guidance:** compose-канон — по скиллам docker-postgres/docker-services (pg_isready/named
volume/init-скрипт для тест-БД); НЕ добавлять app/queue/scheduler-контейнеры (пакет-библиотека,
не приложение — только БД+кэш-сервисы); НЕ преднастраивать binary collation в стенде (это код
P4.7, стенд должен ловить дефолтное поведение MySQL); честный статус: docker недоступен в среде
исполнителя → зафиксировать в Completion Notes, не выдавать непроверенное за зелёное.
**Validation:** `docker compose up -d && docker compose ps` — все сервисы `healthy`;
`docker compose exec -T postgres pg_isready` и `... mysqladmin ping` и `... redis-cli ping` — OK;
`make down` чисто останавливает и не оставляет volume-мусора. (Нет docker → честный skip-note.)
**Deliverables:** docker-compose.yml + Makefile + раздел DEVELOPMENT.md «Локальная БД-матрица».
**Completion Notes:** `docker-compose.yml` — сервисы `postgres` (postgres:16-alpine),
`mysql` (mysql:8), `redis` (redis:7-alpine), у каждого healthcheck (`pg_isready` /
`mysqladmin ping` / `redis-cli ping`), named volumes (`pgsql_data`/`mysql_data`/`redis_data`),
порты только на `127.0.0.1` через `.env`-переменные (`PGSQL_PORT`/`MYSQL_PORT`/`REDIS_PORT`,
дефолты 5432/3306/6379), креды через `.env` (`.env.example` создан, `.env` в `.gitignore`);
`Makefile` — таргеты `up`/`down`/`logs`/`ps`; `DEVELOPMENT.md` — раздел «Local database matrix».
Валидация (на item-коммите 1a9e46b): `docker compose up -d` → все три сервиса `healthy`
(порты переопределены через env — хост уже держит 5432/3306/6379 под другими локальными
проектами, конфигурируемость портов и есть механизм для этого); `docker compose exec -T
postgres pg_isready` / `... mysql mysqladmin ping ...` / `... redis redis-cli ping` — все три
`exit=0`; `make down` — контейнеры и сеть чисто удалены (`docker compose ps -a` пусто), named
volumes намеренно переживают down (данные, не мусор — конвенция скилла `docker-postgres`).
Сервис назван `postgres` (не `pgsql` из шаблона скилла `docker-postgres`) — выровнено под
буквальный текст Validation item'а (`docker compose exec -T postgres ...`).
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P4.2 — БД-лейн-харнесс (commit) + генерализация хрупкого теста + фикстура expires_at

**Status:** 🟢 Done
**Intent:** Закоммитить УЖЕ НАПИСАННЫЙ (лежит в рабочем дереве незакоммиченным) локальный БД-лейн-
харнесс: env-driven `TestCase` (`DB_CONNECTION=pgsql|mysql`, дефолт sqlite:memory), composer
`test:pgsql`/`test:mysql`, union-правило в `DEVELOPMENT.md`, генерализованный
`ScopeClassMigrationRollbackTest`; ПЛЮС починить тест-фикстуру R4 (`ContextTableNameConfigTest`
строит кастом-таблицу без `expires_at`). Харнесс = диагностический инструмент для P4.8/P4.7/P4.9;
полный green обоих лейнов + CI-джоб `test-db-matrix` — НЕ здесь (deferred в P4.10, D31).
**Why:** инфраструктура лейна валидирована в части, не зависящей от найденных багов (sqlite 667/667
зелёный; лейны РЕАЛЬНО прогоняются против стенда P4.1 и воспроизводят baseline), но полный green
недостижим до ремедиации P4.8/P4.7/P4.9 — блокировать коммит рабочего харнесса нельзя (§6: 🔴 не
применяет коммит-топологию, но это re-scope, а не блок). R4 — тест-баг (не app): фикстура не создаёт
`expires_at`, который штатная миграция 000011 добавляет (findings/P4.2-remediation-anchors §5) →
чинится в тест-scope здесь, разблокирует чистую диагностику context-лейна. RAG:— (repo-grounded:
findings/P4.2-remediation-anchors-2026-07-18.md §5; tests/Feature/Context/ContextTableNameConfigTest.php:36-47).
**Scope Included:** закоммитить `tests/TestCase.php` (`databaseConnectionConfig()` env-driven),
`composer.json` (`test:pgsql`/`test:mysql` через `@putenv`), `DEVELOPMENT.md` (union-правило),
`tests/Feature/ScopeClassMigrationRollbackTest.php` (ассерт `toThrow(QueryException::class)` без
текста драйвера) — ВСЁ уже написано, item их фиксирует item-коммитом; фикс R4 — добить
`createContextRolesTable()` в `ContextTableNameConfigTest` до КАНОНИЧЕСКОЙ схемы (добавить
`$table->timestamp('expires_at')->nullable()` — как миграция 000011), чтобы тест перестал бить в
несуществующую колонку.
**Scope Excluded:** CI-джоб `test-db-matrix` в `tests.yml` (написан, но красный из-за багов — коммит
джоба ОТЛОЖЕН в P4.10, добавляется зелёным); полный green PG/MySQL-лейнов (достигается P4.8→P4.7→P4.9,
доказывается P4.10); фиксы миграций/query (P4.8/P4.7/P4.9); collation (P4.7); paratest (P4.3); per-token
DB resolver (не нужен — лейн последователен).
**Inputs:** P4.1 (стенд, поднят на `PGSQL_PORT=25432`/`MYSQL_PORT=23306` — handoff); незакоммиченный
дифф рабочего дерева (TestCase/composer.json/DEVELOPMENT.md/ScopeClassMigrationRollbackTest —
`git diff`); findings/P4.2-remediation-anchors-2026-07-18.md §5; research/04-p4.2-remediation.md §2.
**Files:** `tests/TestCase.php`, `composer.json`, `DEVELOPMENT.md`,
`tests/Feature/ScopeClassMigrationRollbackTest.php`, `tests/Feature/Context/ContextTableNameConfigTest.php`.
**Required Reads:** findings/P4.2-remediation-anchors-2026-07-18.md §5; research/04-p4.2-remediation.md §2/§3; `git diff` (незакоммиченный харнесс); tests/Feature/Context/ContextTableNameConfigTest.php:36-47 (неполная фикстура); packages/context/database/migrations/*000011* (канон expires_at); скилл `devops:db-test-preflight`.
**Implementation Rules:** дефолт sqlite:memory НЕ трогать (быстрый локальный дефолт); НЕ добавлять
CI-джоб `tests.yml` в этом item'е (красный борд — P4.10 добавит зелёным); НЕ вводить driver-ветвления
в ПРИКЛАДНОЙ код (recon §2 — их нет by design); генерализация хрупкого теста — ассерт ПОВЕДЕНИЯ
(`toThrow(QueryException::class)` без текста драйвера), НЕ ослаблять до «просто не падает»; фикстуру
R4 привести к КАНОНУ (полная схема как миграции), НЕ удалять `expires_at`-фильтр из
`ContextPermissionLayer` (это не app-баг — findings-anchors §5); честность rules/tests.md.
**Code Guidance:** item-коммит — ТОЛЬКО пути из Files (`git add` поимённо, `-A`/`-a` запрещён — в
дереве может быть чужой дифф); коммит-сообщение `test(db): commit real-DB lane harness + fix context
table fixture (P4.2)`; после коммита `composer test` (sqlite) обязан быть 667+ зелёным (фикстурный фикс
не должен ломать sqlite); лейны `test:pgsql`/`test:mysql` ЕЩЁ красные (баги не чинены) — это ОЖИДАЕМО,
не выдавать за зелёное, зафиксировать в Completion Notes как baseline до ремедиации.
**Validation:** `composer test` (sqlite) зелёный (667+); `composer test -- --filter=ScopeClassMigrationRollback`
зелёный; `composer test -- --filter=ContextTableNameConfig` зелёный на sqlite (R4 починен); харнесс
закоммичен (`git log` показывает item-коммит с путями Files); `test:pgsql`/`test:mysql` ЗАПУСКАЮТСЯ и
воспроизводят baseline (не зелёные — фиксируется, не форсируется).
**Deliverables:** закоммиченный локальный БД-лейн-харнесс (env-TestCase + composer scripts + union-doc +
генерализованный rollback-тест) + починенная фикстура ContextTableNameConfigTest.
**Completion Notes:** Item-коммит 208943e (`git add tests/TestCase.php composer.json DEVELOPMENT.md
tests/Feature/ScopeClassMigrationRollbackTest.php tests/Feature/Context/ContextTableNameConfigTest.php`,
5 файлов, 54 insertions/10 deletions). Харнесс (env-driven `databaseConnectionConfig()` в TestCase,
`test:pgsql`/`test:mysql` composer-scripts, union-правило DEVELOPMENT.md, driver-agnostic assert в
ScopeClassMigrationRollbackTest) закоммичен как уже написанный дифф. Фикс R4: `createContextRolesTable()`
в ContextTableNameConfigTest получил `$table->timestamp('expires_at')->nullable()` — фикстура
отставала от канона миграции 000011 (`ContextPermissionLayer::apply` фильтрует `expires_at`, custom-таблица
без колонки ломала запрос на строгих драйверах). `.github/workflows/tests.yml` (CI-джоб `test-db-matrix`)
намеренно НЕ закоммичен — остаётся в дереве незакоммиченным до P4.10 (D31).
Validation (на item-коммите 208943e): `composer test` (sqlite) — 667 passed/1774 assertions (на HEAD 208943e);
`composer test -- --filter=ScopeClassMigrationRollback` — 2 passed; `composer test -- --filter=ContextTableNameConfig`
— 3 passed (R4 починен). `composer test:pgsql` (стенд P4.1, `PGSQL_PORT=25432`) — 660/667 passed, 2 failed
+ 5 errors — воспроизводит документированный baseline (COALESCE-каскад миграции 000005: PG boolean-суперадмин
+ PG transaction-abort, findings/P4.2-remediation-anchors §1/§6), НЕ форсировался. `composer test:mysql`
(`MYSQL_PORT=23306`) — запущен напрямую (`php -d memory_limit=1G vendor/bin/pest`, composer-обёртка упирается
в свой 300s process-timeout на этом объёме provider-каскада) — воспроизводит документированный MySQL
«table already exists»-каскад (findings-anchors §1/§6), лейн ЗАПУСКАЕТСЯ и красный по ожиданию, не зелёный —
acceptance P4.2 (D31) выполнен: харнесс инструмент готов, полный green — P4.10.
**Pending Work:** — (ремедиация багов — P4.8→P4.7→P4.9→P4.10, отдельные items).
**Known Deviations:** — (без отклонений от Scope Included; `composer test:mysql` пришлось звать без
composer-обёртки из-за 300s process-timeout самого composer — та же команда, что скрипт исполняет, только
без composer-CLI поверх; результат идентичен).
**Escalation Needed:** no

### P4.3 — Параллельные прогоны (paratest) + hardening random order

**Status:** 🟢 Done
**Intent:** Подключить Pest `--parallel` (dev-dep `brianium/paratest`), дать `composer test:parallel`
на sqlite-лейне (изоляция бесплатна — `:memory:` самоизолируется по процессу), провести
hardening тестового shared-state под parallel+random.
**Why:** recon §1 — paratest не подключён (только транзитивный suggest), параллельного прогона
нет; RAG P4-paratest — `:memory:` изолирован по процессу, parallel-изоляция sqlite-лейна
бесплатна. RAG:✅ 2026-07-18 (findings/P4-rag-paratest-2026-07-18.md §«Ключевой вывод»).
**Scope Included:** `composer require --dev brianium/paratest`; composer script `test:parallel`
(`vendor/bin/pest --parallel` на sqlite); аудит статического состояния тестов на parallel-safety
(напр. `PermissionCacheLockSpyStore::$log` — статический `list`, течёт между воркерами) → сброс
в `beforeEach`/изоляция; опц. parallel-джоб в CI для скорости sqlite-лейна; раздел в DEVELOPMENT.md.
**Scope Excluded:** parallel на РЕАЛЬНЫХ БД (per-token DB resolver — YAGNI follow-up, реальный
лейн последователен, P4.2); host-шардинг CI (follow-up); смена executionOrder (остаётся `random`).
**Inputs:** P4.1/P4.2 (лейны); findings/P4-rag-paratest-2026-07-18.md; recon §1; phpunit.xml.
**Files:** `composer.json` (require-dev + script), `.github/workflows/tests.yml` (opt parallel-джоб),
`tests/**` (фиксы shared-state, если найдены), `DEVELOPMENT.md`.
**Required Reads:** research/05-codex-execution-contract.md; findings/P4-rag-paratest-2026-07-18.md (весь); recon-test-ci §1; phpunit.xml (executionOrder=random); tests/Feature/PermissionCacheEpochInvalidationTest.php (статический `$log` — кандидат в утечку); скилл `laravel-testing`.
**Implementation Rules:** НЕ добавлять per-token DB resolver (sqlite:memory самоизолируется —
YAGNI, RAG-вывод); статические свойства в тестах сбрасывать в `beforeEach` (иначе parallel-флейк);
executionOrder=`random` НЕ ослаблять ради зелёного parallel; флейк под parallel+random = РЕАЛЬНЫЙ
порядок/state-баг → чинить причину или §10 эскалация, НЕ отключать random/parallel.
**Code Guidance:** `--parallel` только на sqlite-лейне (быстрый дефолт); реальный БД-лейн (P4.2)
остаётся последовательным — не смешивать; если найден shared-file/singleton-леак — изолировать по
`TEST_TOKEN`, не глобальным mutex; честность rules/tests.md: подгонка под зелёный parallel
(force config, отключение random) = `[POSSIBLE-DEFECT]`.
**Validation:** `composer test:parallel` зелёный (несколько прогонов подряд — 3× без флейка);
`composer test` (random, sequential) по-прежнему зелёный; type-coverage/lint не задеты.
**Deliverables:** paratest dev-dep + test:parallel script + parallel-safe тесты + doc.
**Completion Notes:** Item-коммит `23a2a7a` делает `brianium/paratest` явным require-dev и добавляет `composer test:parallel`: Pest запускается с `--parallel`, а `--passthru-php='-d memory_limit=1G'` передаёт лимит каждому worker. `phpunit.xml` сохраняет `executionOrder="random"`. Найдены и устранены реальные shared-state defects: static `PermissionCacheLockSpyStore::$log` сбрасывается в `beforeEach`; `MakeGuardPanelCommandTest` генерирует filesystem fixture под `TEST_TOKEN` и временно переопределяет Laravel config path на per-token directory. Fresh-vendor validation: три последовательных `composer test:parallel` — 669 passed / 1778 assertions на 28 processes; обычный `composer test` — 669 passed / 1778 assertions; `composer lint:check`, `composer analyse` и `git diff --check` зелёные. Независимый full review повторил fresh parallel validation и дал APPROVE.
**Pending Work:** P4.4 → P4.5 → P4.6; затем `task:plan-close` P4 и независимый phase audit.
**Known Deviations:** root ignored `vendor/` повреждён (отсутствует `laravel/agent-detector`), поэтому validation выполнялась только в fresh isolated worktree.
**Escalation Needed:** no

### P4.4 — Кросс-процессные race-тесты (C-05 Redis + C-14 Octane)

**Status:** 🟠 Done with deviations
**Intent:** Два теста реальной конкуренции: (а) C-05 — кросс-ПРОЦЕССНЫЙ epoch-race на РЕАЛЬНОМ
Redis (несколько OS-процессов конкурентно bump'ают эпоху одного user+panel; лок сериализует);
(б) C-14 — изоляция Octane RequestState (scope_class/panel не течёт между симулированными запросами).
**Why:** PermissionCache.php:112-118 — bump обёрнут в `lock()->block()` ТОЛЬКО под LockProvider,
но существующий `PermissionCacheEpochInvalidationTest` — single-process на array-spy (call-order),
РЕАЛЬНЫЙ кросс-процесс на Redis не покрыт; 02-backlog §«Хвосты в P4» перечисляет ровно эти два
хвоста. RAG:— (repo-grounded: packages/core/src/Registry/Resolver/PermissionCache.php:104-118;
research/02-backlog.md §«Хвосты в P4»).
**Scope Included:** тест C-05 — воркер-фикстура (`tests/Fixtures/epoch-bump-worker.php`), main-тест
`proc_open`'ит M воркеров конкурентно против ОБЩЕГО реального Redis, ассерт: финальная эпоха == M
(нет потерянных инкрементов) И монотонность (нет отката); честный skip при отсутствии `ext-redis`
(громкий warning, как coverage-gate); тест C-14 — Octane-сценарий: `RequestState` (scope_class/panel)
не переживает `forgetScopedInstances()` между симулированными запросами (расширить/зеркалить
`OctaneScopingTest`); привязка к Redis/лейну P4.1.
**Scope Excluded:** правка `PermissionCache`/`RequestState`/лок-логики (P4 = ТОЛЬКО тесты; найден
реальный race-баг → §10 эскалация + D# с bump-семантикой P3); load/k6 (D4); single-process
call-order (уже покрыт существующим тестом — не дублировать).
**Inputs:** P4.1 (Redis-сервис); packages/core/src/Registry/Resolver/PermissionCache.php:104-118;
tests/Feature/PermissionCacheEpochInvalidationTest.php (образец); tests/Unit/OctaneScopingTest.php;
research/02-backlog.md §«Хвосты в P4».
**Files:** `tests/Feature/PermissionCacheCrossProcessRaceTest.php` (new), `tests/Fixtures/epoch-bump-worker.php`
(new), `tests/Unit/OctaneRequestStateIsolationTest.php` (new; либо расширить OctaneScopingTest),
`tests/TestCase.php` (helper Redis-конфига, opt).
**Required Reads:** research/05-codex-execution-contract.md; packages/core/src/Registry/Resolver/PermissionCache.php:100-120 (lock/bump); tests/Feature/PermissionCacheEpochInvalidationTest.php (single-process образец + spy); tests/Unit/OctaneScopingTest.php; findings/recon-db-portability-2026-07-18.md §5 (Redis не обязателен, LockProvider); скиллы `diagnosing-bugs`, `laravel-testing`.
**Implementation Rules:** РЕАЛЬНАЯ кросс-процессная конкуренция через `proc_open` отдельных
php-процессов (НЕ pcntl_fork — непортируемо/проблемы с БД-коннектами; НЕ фейк-лок — существующий
spy уже покрывает call-order); общее состояние — реальный Redis-стор (не array); честный skip при
отсутствии `ext-redis`/недоступном Redis (громкий warning, не тихий зелёный); детерминизм —
задокументировать число воркеров M и итераций; НЕ подгонять тест под зелёное.
**Code Guidance:** воркер-фикстура — минимальный bootstrap Laravel+пакет, bump эпохи N раз
против Redis из env; main-тест ждёт всех воркеров (`proc_close`), затем читает эпоху; ассерт
монотонности+отсутствия потерь доказывает лок; если lost-increment ВОСПРОИЗВОДИТСЯ (лок не
держит) — это РЕАЛЬНЫЙ баг isolation → §10 эскалация (trigger: unclear architecture / rule
conflict), НЕ ослаблять ассерт; Octane-тест — не звать `handle()` в обход рантайма без пометки
(rules/tests.md); флейк недопустим — race-окно должно быть детерминированно воспроизводимо.
**Validation:** `composer test -- --filter=CrossProcessRace` зелёный при поднятом Redis (стенд
P4.1); честный skip без ext-redis (виден warning, не молчаливый pass); Octane RequestState-тест
зелёный; оба стабильны 3× подряд.
**Deliverables:** кросс-процессный Redis race-тест + воркер-фикстура + Octane RequestState-тест.
**Completion Notes:** Item-коммит `07cac2b` добавил main-тест с уникальным Redis prefix и
worker fixture: `proc_open()` стартует 8 отдельных Pest/OS-процессов, каждый выполняет 3 реальных
`PermissionCache::forgetForUser()` against Redis database 15. На `07cac2b` три combined запуска
`REDIS_PORT=26379 composer test -- tests/Feature/PermissionCacheCrossProcessRaceTest.php tests/Unit/OctaneScopingTest.php`
прошли по 4 tests / 30 assertions; standalone C-05 дал 1 test / 19 assertions. `composer lint:check`,
`composer analyse`, `composer test` (671 tests / 1800 assertions) и `git diff --check` зелёные.
Fixture inert без `P4_RACE_WORKER=1`; отсутствие `ext-redis` или Redis создаёт явный skip с причиной.
Независимый Sol/high review: APPROVE, blockers none.
**Pending Work:** P4.5 → P4.6 → `task:plan-close` P4 → независимый `task:plan-audit` P4.
**Known Deviations:** Scope Included формулирует raw final epoch как `M`, но production
`currentEpoch()` по умолчанию равен 1, а каждый реальный `add(1)+increment()` начинается с версии 2.
Поэтому committed assertion проверяет фактическую семантику: 8 × 3 bump = epoch 25, затем следующий
bump = 26; public/production-код не менялся.
**Escalation Needed:** no

### P4.5 — Mutation-ratchet: честный замер, пороги вверх, ревизия excludes

**Status:** 🟡 In progress
**Intent:** Честно замерить фактический MSI/coveredMSI по пакетам (холодный coverage), поднять
`minMsi`/`minCoveredMsi` до `floor(measured)−2` (ТОЛЬКО вверх vs текущих), пересмотреть excludes
(Commands/Facades/Resources/Pages — контекст D-08), зафиксировать рационал inline.
**Why:** recon §3 — текущие пороги (core 70/80, filament 60/75, context 65/80) могли отстать от
факта; vaulter-дисциплина ratchet «порог только вверх, floor(Score)−буфер» (recon-vaulter §2
ступень 5); D-08 отклонён из P1 как предмет mutation-ratchet P4 (research/02-backlog.md §Отклонено).
RAG:— (repo-grounded: findings/recon-test-ci-2026-07-18.md §3; findings/recon-vaulter-template-2026-07-18.md §2; research/02-backlog.md D-08).
**Scope Included:** прогон `composer mutate:core/filament/context` на СВЕЖЕМ coverage → зафиксировать
замер MSI (build/infection/*-summary.log → выжимка в `plans/2026.07.18-AZGUARD-STABLE/artifacts/P4-mutation-baseline.md`);
поднять пороги в `infection.{core,filament,context}.json5` до `floor(measured)−2` (только вверх,
никогда вниз); ревизия каждого exclude (обоснован ли — декларативный UI/не-unit-тестируем;
необоснованный убрать, дать ratchet замерить); inline-комментарий рационала каждого exclude;
раздел «Mutation-ratchet политика» в RELEASING.md/DEVELOPMENT.md.
**Scope Excluded:** смена движка Infection→Pest-native mutate (vaulter D38 — ИХ несовместимость,
не наша; azguard-Infection работает — YAGNI, out of scope); аспирационные пороги ≥95 без замера
(честность — floor от факта); правка прикладного кода ради MSI (если факт низкий — это сигнал
покрытия, не повод менять код в P4).
**Inputs:** findings/recon-test-ci-2026-07-18.md §3; findings/recon-vaulter-template-2026-07-18.md §2;
research/02-backlog.md §Отклонено (D-08); infection.{core,filament,context}.json5; bin/mutation-gate.sh.
**Files:** `infection.core.json5`, `infection.filament.json5`, `infection.context.json5`,
`plans/2026.07.18-AZGUARD-STABLE/artifacts/P4-mutation-baseline.md` (new замер), `RELEASING.md` (или DEVELOPMENT.md).
**Required Reads:** research/05-codex-execution-contract.md; findings/recon-test-ci-2026-07-18.md §3/§5 (пороги, honest-skip); findings/recon-vaulter-template-2026-07-18.md §2 (ratchet-дисциплина); скилл `mutation-testing`; infection.core.json5 (текущий формат); bin/mutation-gate.sh (honest-skip контракт).
**Implementation Rules:** пороги ТОЛЬКО вверх (ratchet-инвариант); НИКОГДА не понижать порог ради
зелёного — если фактический MSI НИЖЕ текущего порога, это РЕГРЕССИЯ покрытия → §10 эскалация, НЕ
понижение; замер на ХОЛОДНОМ coverage (не переиспользовать старый xml); буфер `floor−2` (не
впритык — чтобы недетерминизм мутатора не краснил гейт); exclude убирается ТОЛЬКО если код
реально unit-тестируем (Commands/Facades/Resources/Pages — обычно декларативны, но проверить, не
принимать на веру).
**Code Guidance:** замер требует coverage-драйвера (pcov/xdebug) — при отсутствии локально гейт
honest-скипается (bin/mutation-gate.sh), пороги ставить ВСЛЕПУЮ ЗАПРЕЩЕНО: замер делать в среде с
драйвером (CI mutation.yml push-джоб / локально установить pcov), иначе §10 эскалация «нет драйвера
для замера»; каждый поднятый порог — с якорем на замер (P4-mutation-baseline.md); exclude-рационал
— явный inline-комментарий в json5 (не молчаливый список).
**Validation:** `composer mutate:all` зелёный на ПОДНЯТЫХ порогах (в среде с драйвером);
`plans/.../artifacts/P4-mutation-baseline.md` содержит замер per-package; каждый exclude несёт
inline-рационал; ни один порог не понижен относительно текущих (диф json5 — только вверх).
**Deliverables:** поднятые пороги infection.*.json5 + замер-артефакт + ревизия/рационал excludes + doc-политика.
**Completion Notes:** —
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P4.6 — Чистка тестовых дыр (UnitFilament, baseline→0, verify D-01/D-06)

**Status:** ⬜ Not started
**Intent:** Убрать мёртвый testsuite `UnitFilament` (каталога нет); драйвить phpstan-baseline
35→0 (или обоснованный пол); ВЕРИФИЦИРОВАТЬ, что D-01 (мёртвая ссылка DiscoveryTest) и D-06
(OOM `composer test`) закрыты в P1 (не переделывать).
**Why:** recon §1 «Дыры» — testsuite `UnitFilament` объявлен, но `tests/Unit/Filament` НЕТ
(мёртвый suite); recon §5 — phpstan-baseline 35 записей, «должен уменьшаться». RAG:— (repo-grounded:
findings/recon-test-ci-2026-07-18.md §1/§5; phpunit.xml:27-29).
**Scope Included:** удалить `<testsuite name="UnitFilament">` из phpunit.xml (каталог отсутствует,
suite мёртв — `failOnEmptyTestSuite=true` его бы ронял, если бы селектился); драйв phpstan-baseline
— типизировать/чинить код так, чтобы записи уходили, до 0 ИЛИ обоснованного пола (каждая остаточная
запись — inline-обоснование, реальный larastan/framework false-positive); чистый `composer analyse`
без роста baseline; verify D-01 (grep: нет ссылки на `DiscoveryTest` в tests/Pest.php) и D-06
(`composer test` не OOM'ит — memory_limit применён).
**Scope Excluded:** D-01 и D-06 — закрыты в P1 (W2 P1.3 / W1 P1.2 соответственно, фаза-first: P1
раньше P4); НЕ переделывать, только verify отсутствия регрессии; фрагильный
ScopeClassMigrationRollbackTest (P4.2); coverage-exclude `packages/filament/src/Resources`
(декларативный UI — обоснован, оставить).
**Inputs:** findings/recon-test-ci-2026-07-18.md §1/§5; phpunit.xml; phpstan-baseline.neon;
research/02-backlog.md (D-01→W2, D-06→W1 — дома в P1).
**Files:** `phpunit.xml`, `phpstan-baseline.neon`, `packages/*/src/**` (типизация под baseline↓),
`phpstan.neon` (opt).
**Required Reads:** research/05-codex-execution-contract.md; findings/recon-test-ci-2026-07-18.md §1/§5; phpunit.xml:23-29 (UnitFilament); phpstan-baseline.neon (35 записей); скилл `static-analysis`.
**Implementation Rules:** baseline→0 — ЦЕЛЬ, не догма: остаточная запись легальна ТОЛЬКО с
inline-обоснованием (реальный larastan false-positive), число строго убывает vs 35; НЕ подавлять
реальную ошибку новой baseline-записью (рост baseline запрещён — это маскировка); UnitFilament —
удалить мёртвый suite, НЕ создавать пустышку `tests/Unit/Filament/` ради галочки; verify D-01/D-06
grep'ом — регрессия (P1 не закрыл) → §10 эскалация к владельцу/P1, не тихий рефикс в P4.
**Code Guidance:** каждая снятая baseline-запись — через РЕАЛЬНУЮ типизацию/фикс, не через
`@phpstan-ignore` россыпью (это перенос долга); если запись неустранима (generics-ограничение
larastan) — оставить в baseline с комментарием-обоснованием, посчитать в «пол»; честность —
baseline не должен вырасти ни на строку (diff phpstan-baseline.neon — только удаления/обоснованные).
**Validation:** `composer analyse` зелёный; `wc -l`/grep `message:` phpstan-baseline.neon строго
< 35 (на bookkeeping-коммите, с якорем); `composer test` зелёный без warning про UnitFilament;
`grep -r DiscoveryTest tests/` == 0 (D-01 verify); `composer test` не OOM'ит (D-06 verify).
**Deliverables:** phpunit.xml без UnitFilament + уменьшенный phpstan-baseline (+ обоснования пола) + verify-запись D-01/D-06.
**Completion Notes:** —
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P4.7 — Key-length + collation-hardening RBAC-ключей 000002/000010 (D24+D32)

**Status:** 🟠 Done with deviations
**Intent:** Владелец единой правкой миграций **000002 (core: direct_grants/role_permissions) и 000010
(context: context_roles)** привести их composite-unique ключевые строковые колонки к MySQL-корректности:
(а) **key-length** — ограничить длины ключевых колонок так, чтобы `az_direct_grant_unique` (4 кол.) и
`az_ctx_roles_unique` (6 кол.) уложились в лимит ключа InnoDB 3072 байта под utf8mb4 (R2, D32); (б)
**collation** — driver-conditional `utf8mb4_bin` на ключевые RBAC-строковые колонки (`panel_id`,
`permission_key`, `model_type`/морф-типы, `context_type`/`context_id`) ТОЛЬКО для MySQL/MariaDB (PG/SQLite
уже case-sensitive). Оба конерна — одни колонки одних файлов → один проход (research/04 §2: избегаем
двойного редактирования 000002/000010). Тесты case-sensitivity + key-length-fit доказываются MySQL-лейном
(green — P4.10).
**Why:** recon-db-portability §3/§7 — MySQL дефолт `utf8mb4_*_ci` case-INSENSITIVE → `panel_id`='App'
и 'app' СХЛОПЫВАЮТСЯ; unique `(role_id, permission_key, panel_id)` становится CI и отвергает
«разные» по регистру ключи как дубль; `model_type` (FQCN, case-sensitive в PHP-матчинге) матчится
нестрого = security-корректность-баг; owner D23 «MySQL first-class» + D10 fail-closed (регистр
РАЗЛИЧАЕТ ключи, как PHP `preg_match` без `i`). **Key-length (D32):** реальный MySQL-лейн P4.2 упал
`Specified key too long (3072 bytes)` на `az_direct_grant_unique`/`az_ctx_roles_unique` — 6× varchar(255)
под utf8mb4 превышают лимит ключа InnoDB (findings/P4.2-remediation-anchors §2); длину надо ограничить
тем же проходом, что collation (те же колонки). RAG:— (repo-grounded: findings/recon-db-portability-2026-07-18.md §3/§7;
findings/P4.2-remediation-anchors-2026-07-18.md §2; WildcardPermissionMatcher.php:23; core migration 000002; context migration 000010).
**Scope Included:** правка core-миграции 000002 + context-миграции 000010 (edit-in-place — git-тегов
нет, свежие инсталы, recon §CI) — ДВА конерна за один проход по ключевым колонкам: (а) **key-length** —
подобрать длины строковых колонок composite-unique (`az_direct_grant_unique`: grantable_type/permission_key/
panel_id; `az_ctx_roles_unique`: model_type/context_type/context_id/panel_id/permission_key) так, чтобы
сумма байт ключа под utf8mb4 < 3072; (б) **collation** — driver-conditional `->collation('utf8mb4_bin')`
тем же ключевым колонкам когда `getDriverName()` ∈ {mysql, mariadb}; тест `CollationCaseSensitivityTest` —
ключи, различающиеся ТОЛЬКО регистром, различимы (unique не отвергает как дубль); на sqlite/PG тот же
тест зелён нативно; key-length-fit + collation доказываются MySQL-лейном (green — P4.10).
**Scope Excluded:** input-канонизация как ЗАМЕНА schema-collation (выбран schema-level binary —
структурнее и fail-closed; канонизация полагается на каждый write-путь — НЕ строим); PG/SQLite
ветки (уже case-sensitive — не трогать); правка публичного PHP-API (миграции не @api, снапшот
P3 не задет); правка матчеров/резолверов (они уже case-sensitive в PHP — recon §3); **миграция 000005
COALESCE/down-order (владелец P4.8 — другой файл); scopes-индекс model_has_scopes_unique уже несёт
191-префикс (recon-anchors §1) — не переопределять его в этом item'е**.
**Inputs:** findings/recon-db-portability-2026-07-18.md §1/§3/§7; findings/P4.2-remediation-anchors-2026-07-18.md §2/§3;
research/04-p4.2-remediation.md §2/§4/§5; core migration 000002 (role_permissions/direct_grants); context migration
000010 (context_roles); MorphColumns.php (морф-типы); plan.md D10 (fail-closed), D24/D32 (санкция+key-length).
**Files:** `packages/core/database/migrations/*000002*.php`, `packages/context/database/migrations/*000010*.php`,
`packages/core/src/Database/Schema/MorphColumns.php` (если морф-типы там, driver-guard),
`tests/Feature/CollationCaseSensitivityTest.php` (new).
**Required Reads:** research/05-codex-execution-contract.md; findings/recon-db-portability-2026-07-18.md §1/§3/§7 (реестр колонок, воркэраунд); findings/P4.2-remediation-anchors-2026-07-18.md §2/§3 (якоря key-length/morph); research/04-p4.2-remediation.md §4/§5 (SemVer/pre-mortem); packages/core/database/migrations (000002, `az_direct_grant_unique`); packages/context/database/migrations (000010, `az_ctx_roles_unique`); packages/core/src/Database/Schema/MorphColumns.php; plan.md D10/D24/D32/D34; скиллы `database`, `laravel-package-testing`.
**Implementation Rules:** driver-guard ОБЯЗАТЕЛЕН (`->collation('utf8mb4_bin')` только для
mysql/mariadb — иначе PG упадёт на неизвестной collation; SQLite игнорит, но не подавать);
binary/`utf8mb4_bin`, НЕ `_ci`; collation-ветка в МИГРАЦИИ допустима (не runtime resolver —
прикладной код driver-agnostic сохраняется, recon §2); поведение выравнивается ПОД уже-корректный
case-sensitive контракт (conformance, НЕ редизайн — @api не меняется). **Key-length (D32):** длины
подбирать РАСЧЁТОМ — сумма (байт на колонку) ключа < 3072 под utf8mb4 (4 байта/симв); **НЕ резать
`model_type` вслепую до 191** (FQCN бывает длиннее — pre-mortem research/04 §5: коллизия ключей) —
либо оставить `model_type` полным и сузить прочие (panel_id/context_type/context_id/permission_key —
короткие идентификаторы, напр. 128/191), либо префикс-индекс на длинную колонку; расчёт «сумма байт»
привести в Completion Notes с якорем. Если ключ не влезает даже при разумных длинах — §10 эскалация
(не резать семантику ключа ради лимита).
**Code Guidance:** правка минимальна — collation-clause + length ключевым колонкам под driver-guard;
если морф-типы генерятся `MorphColumns::add()` — key-length/driver-guard там же (единый шов); тест
доказывает РАЗЛИЧИМОСТЬ регистра (две записи 'App'/'app' сосуществуют И различимы при выборке), не
просто «вставилось»; utf8mb4_bin делает прод-MySQL case-sensitive (breaking для инсталов, полагавшихся
на схлопывание) — это НАМЕРЕННО (fail-closed D10), но добавить строку в UPGRADING/known-limitations
(deliverable P4.10 или follow-up-заметка, research/04 §4); если после правок MySQL-лейн краснит на
СУЩЕСТВУЮЩЕМ тесте (не новом) — латентный portability-баг → §10, не подгонка; @api-снапшот P3 тронут
(не должен — миграции не @api) → §10 (D# с bump P3).
**Validation:** новый `CollationCaseSensitivityTest` зелёный на MySQL-лейне (различает регистр) И на
sqlite/PG; `composer test:mysql` (стенд P4.1) НЕ падает на `key too long` для `az_direct_grant_unique`/
`az_ctx_roles_unique` (key-length fit); `composer test`/`test:pgsql` не задеты (driver-guard); полный
green MySQL-лейна доказывает P4.10; расчёт байт ключа < 3072 — в Completion Notes.
**Deliverables:** driver-conditional collation + bounded key-length в 000002/000010 + CollationCaseSensitivityTest + расчёт key-fit.
**Completion Notes:** Item-коммит `4c4970f`: migration 000002 задаёт MySQL/MariaDB-only
`utf8mb4_bin` для ключей role permissions/direct grants, 000010 — для context roles; `MorphColumns`
получил опциональные key-type параметры без изменения старых вызовов. Расчёт на `4c4970f`:
`az_direct_grant_unique` = 255×4 + 8 + 255×4 + 128×4 = 2560 B, `az_ctx_roles_unique` =
255×4 + 8 + 127×4 + 64×4 + 128×4 + 191×4 = 3068 B — оба < 3072 B. Новый
`CollationCaseSensitivityTest` прошёл SQLite, PostgreSQL и MySQL до reset test DB; MySQL также
проверил `information_schema` на `utf8mb4_bin`. `composer test` (669 passed),
`composer test:pgsql`, `composer lint:check` и `composer analyse` зелёные. Независимый Sol/high
read-only review `4c4970f` — APPROVE, findings нет.
**Pending Work:** P4.9 → P4.10: на пустом `azguard_test` воспроизвести и исправить MySQL
`RefreshDatabase` bootstrap, затем повторить `composer test:mysql`, включая
`CollationCaseSensitivityTest`; P4.10 также владеет CI green-proof и документированием намеренной
case-sensitive MySQL-семантики.
**Known Deviations:** После разрешённого reset только disposable `azguard_test` обязательный MySQL
прогон не достигает P4.7-теста: существующий `ModelHasRolesScopes` также падает в bootstrap 000000/000001
с чередующимися missing/already-existing `model_has_roles`/`roles`/`migrations`. Это material red
validation, отнесённая review к P4.10 full-lane harness; P4.7 не расширял Files.
**Escalation Needed:** no

### P4.8 — Portability миграции 000005: COALESCE morph-type-aware + down()-порядок MySQL (D30)

**Status:** 🟢 Done
**Intent:** Починить миграцию 000005 под PG (COALESCE-fallback, зависящий от morph-type) и MySQL
(порядок drop в `down()` под FK-covering-index); продиагностировать ULID-truncation `model_id`
(morph config-timing). Фикс R1 снимает и каскады — PG transaction-abort + PG boolean-суперадмин +
MySQL «table already exists» (research/04 §1).
**Why:** якорь §1 — `COALESCE(scope_entity_id, 0)` с char-морф-колонкой ломает PG (*«character and
integer cannot be matched»*), fallback-литерал `0` захардкожен вне зависимости от `Config::morphType()`;
`down()` не учитывает MySQL-отказ дропнуть unique, покрывающий FK; каскад §6/§1: PG-boolean-суперадмин
и MySQL-table-exists — следствия этого сбоя, отдельных items нет. RAG:— (repo-grounded:
findings/P4.2-remediation-anchors-2026-07-18.md §1/§3/§6; research/04-p4.2-remediation.md §1).
**Scope Included:** миграция 000005 — (а) COALESCE-fallback в PG/SQLite- и MySQL-ветках выводить из
`Config::morphType()`: int→`0`, ulid/uuid→`''` (строковый морф → строковый fallback, типы COALESCE
совпадают); (б) `down()` — порядок drop, при котором MySQL успешно снимает `model_has_roles_unique`
(снять/пересоздать зависимый FK-index ИЛИ дропать в MySQL-совместимом порядке); (в) диагностика
ULID-truncation `model_id`: если корень — morph-config теста не применён до RefreshDatabase-миграций,
фикс в ТЕСТ-setup (не миграция).
**Scope Excluded:** миграции 000002/000010 key-length+collation (P4.7 — владелец файла); filament
LIKE (P4.9); CI-джоб + green-proof (P4.10); @api PHP-поверхность (миграции не @api — снапшот P3 не задет);
input-канонизация.
**Inputs:** findings/P4.2-remediation-anchors-2026-07-18.md §1/§3/§6; research/04-p4.2-remediation.md §1;
migration 000005; MorphColumns.php; стенд P4.1 (`PGSQL_PORT=25432`/`MYSQL_PORT=23306`); P4.2 (лейн-харнесс закоммичен).
**Files:** `packages/core/database/migrations/*000005*.php`; `tests/Feature/MorphTypeTest.php` И/ИЛИ
`tests/TestCase.php` (ТОЛЬКО если ULID-truncation — тест-config-timing); `tests/Feature/ModelHasRolesScopesUniqueConstraintTest.php` (verify, если требует правки setup).
**Required Reads:** research/05-codex-execution-contract.md; findings/P4.2-remediation-anchors-2026-07-18.md §1/§3/§6; research/04-p4.2-remediation.md §1/§4; packages/core/database/migrations/*000005* (обе driver-ветки + down); packages/core/src/Database/Schema/MorphColumns.php; packages/core/src/Configuration (или где `Config::morphType()`); tests/Feature/MorphTypeTest.php; tests/Feature/ModelHasRolesScopesUniqueConstraintTest.php; скиллы `database`, `diagnosing-bugs`, `db-test-preflight`, `laravel-package-testing`.
**Implementation Rules:** COALESCE-fallback morph-type-aware (НЕ хардкод `0` — pre-mortem research/04 §5:
mixed-key инсталл); сохранить структуру driver-branch и raw `DB::statement` (expression-индекс —
намеренно raw, НЕ конвертировать в Blueprint); `down()` обязан отработать на MySQL под RefreshDatabase
(проверить и up, и down на обоих драйверах); edit-in-place, НОВЫЙ файл миграции НЕ создавать (git-тегов
нет — recon §CI, канон D24); если ULID-truncation — тест-config-timing, чинить ТЕСТ, не миграцию; после
фикса каскады обязаны сняться — verify `AuthorizerExtendedTest`/`HasAzGuardTest`/`ModelHasRolesScopes...`
зелены на PG; если симптом НЕ снялся — это независимый баг → §10 (не выдавать за починенное).
**Code Guidance:** правка минимальна — только fallback-литерал (morph-aware) + порядок drop в `down()`;
`Config::morphType()` — источник ветки int/ulid/uuid, читать его же, что MorphColumns; честность:
«каскад снят» подтверждается прогоном именно тех тестов, не предположением; @api-снапшот P3.2 тронут
(не должен) → §10 (D# с bump P3). НЕ трогать 000002/000010 (P4.7).
**Validation:** `composer test:pgsql -- --filter=MorphType` зелёный; `composer test:pgsql -- --filter=ModelHasRolesScopes`
зелёный (нет transaction-abort); `composer test:pgsql -- --filter='Authorizer|HasAzGuard'` зелёный
(каскад-суперадмин снят); `composer test:mysql -- --filter=ModelHasRolesScopes` без «cannot drop index»
(down отработал); `composer test` (sqlite) по-прежнему зелёный; расчёт/подтверждение снятия каскадов — в Completion Notes.
**Deliverables:** portable migration 000005 (COALESCE morph-type-aware + MySQL down-order) + verify снятия PG/MySQL-каскадов + (при необходимости) фикс morph-config-timing теста.
**Completion Notes:** Item-коммиты `1179b7c` и `91a67d7` внесли morph-aware COALESCE в обе driver-ветки 000005, MySQL-safe FK/index порядок в `down()` и UUID migration proof. После P4.11 повторно зелёны `PGSQL_PORT=25432 composer test:pgsql -- --filter=MorphType`, `PGSQL_PORT=25432 composer test:pgsql -- --filter=ModelHasRolesScopes`, `PGSQL_PORT=25432 composer test:pgsql -- --filter='Authorizer|HasAzGuard'`, `MYSQL_PORT=23306 composer test:mysql -- --filter=ModelHasRolesScopes`, `composer test`, `composer lint:check` и `composer analyse`. P4.11 `cda13a8` снял отдельный fixture-portability blocker без изменения migration 000005; ранее независимый Sol/high review migration-diff — APPROVE.
**Pending Work:** P4.7 — следующий владелец миграций 000002/000010; P4.9/P4.10 не начинать вне порядка roadmap.md.
**Known Deviations:** —
**Escalation Needed:** no

### P4.11 — PG wildcard: portability именованных role fixtures (D36)

**Status:** 🟢 Done
**Intent:** Честно снять либо классифицировать ровно две PG wildcard assertions, заменив только их persistent anonymous `BaseRole` fixtures на существующий именованный `AzGuard\Tests\Stubs\Roles\SuperAdminRole`; семантика assertions остаётся прежней: wildcard даёт полный доступ через `hasPermission()` и `Gate::before`. RAG:— (repo-grounded: findings/P4.8-wildcard-follow-up-2026-07-21.md).
**Why:** после P4.8 migration-fix PG воспроизводит `35/37` без SQLSTATE/transaction-abort; оба failing tests хранят anonymous `get_class()` в `roles.class_name`, а его имя содержит NUL. Контроль с именованным SuperAdminRole проходит PG `22/22`, поэтому это fixture portability, не доказанный дефект wildcard runtime. RAG:— (repo-grounded: findings/P4.8-wildcard-follow-up-2026-07-21.md; research/06-p4.8-wildcard-classification.md).
**Scope Included:** только `tests/Unit/Concerns/HasAzGuardTest.php` и `tests/Feature/AuthorizerExtendedTest.php`: импортировать/использовать existing named `SuperAdminRole::class` вместо двух local anonymous class definitions; сохранить названия кейсов и все позитивные assertions без ослабления.
**Scope Excluded:** `packages/**`; migration 000005 и все Files P4.8; `tests/Pest.php` helper; новый role/runtime class; wildcard matcher/resolver/Gate logic; API snapshot/fixture; P4.7/P4.9/P4.10; driver skip, ожидание `false`, conditional assertion или suppression PG-лейна.
**Inputs:** findings/P4.8-wildcard-follow-up-2026-07-21.md; research/06-p4.8-wildcard-classification.md; P4.8 commits `1179b7c`/`91a67d7`; P3 freeze `root/semver-policy.md`.
**Files:** `tests/Unit/Concerns/HasAzGuardTest.php`; `tests/Feature/AuthorizerExtendedTest.php`.
**Required Reads:** research/05-codex-execution-contract.md; research/06-p4.8-wildcard-classification.md; findings/P4.8-wildcard-follow-up-2026-07-21.md; phases/P4.md P4.8/P4.11; tests/Pest.php:116-126; tests/Stubs/Roles/SuperAdminRole.php; root/semver-policy.md §1–4; скиллы `laravel-package-testing`, `diagnosing-bugs`, `test-isolation-guard`.
**Implementation Rules:** использовать уже существующий именованный test stub, не создавать вторую роль и не менять `createRoleWithClass()`; обе проверки продолжают проходить persistent `class_name` → `Role::getRoleLogic()` → `ClassRoleGrantSource`, не mock/fake shortcut. Если именованный fixture всё ещё даёт `false` на PG, прекратить работу: это опровергает D36, оставить assertions строгими и эскалировать по §10 к `task:plan-design` как runtime/P3-freeze вопрос. RAG:— (repo-grounded: packages/core/src/Models/Role.php:59-71; packages/core/src/Registry/Sources/ClassRoleGrantSource.php:30-76).
**Code Guidance:** anti-подгонка `code-guidance-patterns.md` §1: никакого production normalizer для NUL, driver branch, skip или изменения expected `true` на `false`; минимальный diff — два tests/imports. P3 snapshot не регенерировать. Если `packages/**` или `tests/Fixtures/api-surface.snapshot.php` попали в diff, остановиться и эскалировать, а не расширять P4.11.
**Validation:** `composer test -- --filter='Authorizer|HasAzGuard'` зелёный; `PGSQL_PORT=25432 composer test:pgsql -- --filter='Authorizer|HasAzGuard'` зелёный `37/37`; `PGSQL_PORT=25432 composer test:pgsql -- --filter='IntegrationPolishTest|PanelRoleEnumEntrypointsTest|GateIntegrationScopedTest'` зелёный; `composer lint:check && composer analyse` зелёные; `git diff --check` зелёный; `git diff --name-only` содержит только Files до item-коммита.
**Deliverables:** два portable wildcard fixtures с неизменёнными positive assertions; PG proof `37/37`; classification в Completion Notes, что P3/SemVer/API snapshot не затронуты.
**Completion Notes:** На item-коммите `cda13a8`: оба persistent wildcard fixtures используют existing named `SuperAdminRole::class`, сохраняя путь `roles.class_name` → `Role::getRoleLogic()` → `ClassRoleGrantSource` и все positive assertions. `composer test -- --filter='Authorizer|HasAzGuard'`, `PGSQL_PORT=25432 composer test:pgsql -- --filter='Authorizer|HasAzGuard'`, контроль `IntegrationPolishTest|PanelRoleEnumEntrypointsTest|GateIntegrationScopedTest`, `composer lint:check` и `composer analyse` зелёные; отдельный Sol/high read-only review — APPROVE. P3/SemVer/API snapshot и P4.8 migration не затронуты.
**Pending Work:** вернуть P4.8 к `task:plan-run 2026.07.18-AZGUARD-STABLE P4.8` для возобновления validation/close по committed migration evidence; P4.7 не начинать до этой сверки.
**Known Deviations:** —
**Escalation Needed:** no

### P4.9 — Portability filament: LIKE-escape под MySQL-грамматику (D30)

**Status:** 🟢 Done
**Intent:** Сделать поисковый LIKE-запрос `DirectGrantResource` портируемым — убрать захардкоженный
raw `LIKE ? ESCAPE '\\'`, ломающий MySQL-грамматику, сохранив семантику литеральных `_`/`%`.
**Why:** якорь §4 — `whereRaw("{$column} LIKE ? ESCAPE '\\'", ...)` (DirectGrantResource.php:66-75) —
raw ESCAPE не переносится на MySQL (*syntax error near ''\' limit'*); тест
`DirectGrantResourceLikeEscapeTest` (2 кейса) красный на MySQL. Косметика поиска, не RBAC (recon §7).
RAG:— (repo-grounded: findings/P4.2-remediation-anchors-2026-07-18.md §4).
**Scope Included:** `DirectGrantResource::getSearchResultsUsing` — заменить raw-ESCAPE на портируемый
путь (Laravel `whereLike` при поддержке в пиновой версии Laravel; иначе driver-aware ESCAPE-символ или
grammar-корректная форма), СОХРАНИВ обработку `_`/`%` как литералов (оба тест-кейса зелены на 3 драйверах);
helper `escapeLikeValue` остаётся.
**Scope Excluded:** миграции (P4.8/P4.7); прочие filament-ресурсы; RBAC-логика; ослабление теста.
**Inputs:** findings/P4.2-remediation-anchors-2026-07-18.md §4; DirectGrantResource.php:66-75/204-207;
tests/Feature/Filament/DirectGrantResourceLikeEscapeTest.php; composer.json (constraint `illuminate/*` —
для проверки доступности `whereLike`); стенд P4.1.
**Files:** `packages/filament/src/Resources/DirectGrantResource.php` (тест НЕ трогать без нужды —
ассерты литеральности должны остаться драйвер-агностичными).
**Required Reads:** research/05-codex-execution-contract.md; findings/P4.2-remediation-anchors-2026-07-18.md §4; packages/filament/src/Resources/DirectGrantResource.php:60-80/200-210; tests/Feature/Filament/DirectGrantResourceLikeEscapeTest.php; composer.json (`illuminate/*` constraint); скиллы `laravel-package-compatibility`, `verify-claims`; при выборе `whereLike` — официальная документация/исходники минимально поддерживаемой версии Laravel.
**Implementation Rules:** предпочесть Laravel `whereLike($col, $pattern, caseSensitive)` ЕСЛИ метод
есть в МИНИМАЛЬНОЙ поддерживаемой версии Laravel (проверить по composer.json и официальным docs/source — это ВНЕШНИЙ факт,
RAG); иначе — driver-conditional ESCAPE или форма без hardcoded `ESCAPE '\\'`; `_` и `%` ОБЯЗАНЫ
оставаться литералами на всех 3 драйверах (оба тест-кейса зелены); НЕ ослаблять тест до «просто
находит»; если портируемого пути нет без driver-ветвления в query — driver-guard в ЭТОМ запросе
допустим (это не прикладная RBAC-логика, а UI-поиск).
**Code Guidance:** правка точечная в одном методе; при выборе `whereLike` — верифицировать доступность
RAG-маркером (`RAG:✅` с версией) в Completion Notes, иначе `[UNVERIFIED]`; helper `escapeLikeValue`
не удалять; честность rules/tests.md — подгонка под зелёный MySQL = `[POSSIBLE-DEFECT]`.
**Validation:** `composer test -- --filter=DirectGrantResourceLikeEscape` зелёный (sqlite);
`composer test:pgsql -- --filter=DirectGrantResourceLikeEscape` зелёный; `composer test:mysql -- --filter=DirectGrantResourceLikeEscape`
зелёный (нет syntax error); `composer lint:check && composer analyse` зелёные.
**Deliverables:** портируемый LIKE-escape в DirectGrantResource + зелёный escape-тест на 3 драйверах.
**Completion Notes:** Item-коммит `77c118c` заменил backslash-escape на нейтральный SQL-символ
`!` и экранирует `!`, `%`, `_` через `str_replace()`, сохранив существующий helper и тесты без
изменений. `composer test -- --filter=DirectGrantResourceLikeEscape`,
`PGSQL_PORT=25432 composer test:pgsql -- --filter=DirectGrantResourceLikeEscape` и
`MYSQL_PORT=23306 composer test:mysql -- --filter=DirectGrantResourceLikeEscape` прошли; также
прошли `composer lint:check && composer analyse` и `git diff --check`. `whereLike` доступен в
поддерживаемых Laravel-линиях, но его grammar не выражает обязательный `ESCAPE`; выбран явный
переносимый SQL-вариант, подтверждённый этими проверками. RAG: ✅ Perplexity с официальным Laravel
API, 2026-07-21, confidence medium; локально сверены composer constraints и grammar source.
**Pending Work:** общий Sol/high review checkpoint B6 выполняется после P4.10.
**Known Deviations:** Context7 вернул monthly-quota error; проверка external API продолжена через
Perplexity с официальным Laravel API. Непроверенных external facts и workaround в коде нет.
**Escalation Needed:** no

### P4.10 — Green-proof обоих лейнов + CI-джоб test-db-matrix + baseline→resolved (D30)

**Status:** 🟢 Done
**Intent:** После P4.8/P4.7/P4.9 — терминальный green-proof: прогнать оба реальных лейна полностью
зелёными, закоммитить ОТЛОЖЕННЫЙ (написан в P4.2, красный) CI-джоб `test-db-matrix` уже зелёным,
подтвердить union-правило, пометить baseline-finding RESOLVED, добавить UPGRADING-заметку про
utf8mb4_bin case-sensitivity.
**Why:** research/04 §2/§4 — P4.2 отложил красный CI-джоб; union-правило требует реального зелёного
борда; utf8mb4_bin делает прод-MySQL case-sensitive = breaking, требует строки в UPGRADING
(P3.3 закрыт → добор здесь). RAG:— (repo-grounded: research/04-p4.2-remediation.md §2/§4/§5).
**Scope Included:** в новом detached worktree на committed P4.15 tree выполнить `composer update --prefer-dist --no-interaction --no-progress`, затем прогон `composer test:pgsql` + `composer test:mysql` ПОЛНОСТЬЮ зелёными (стенд P4.1); не копировать и не symlink'ить ignored local `vendor/`; коммит джоба `test-db-matrix` в `.github/workflows/tests.yml` (matrix pgsql/mysql, services PG16+MySQL8
с healthcheck — hunk из P4.2, добавляется зелёным); verify union-правила в `DEVELOPMENT.md`; шапка
`RESOLVED` в `findings/P4.2-db-portability-failures.md` (какой item закрыл какой класс — провенанс
сохранён, не удаляется); строка про utf8mb4_bin case-sensitivity в `docs/introduction/upgrading.md`
(+RU-зеркало) и/или `root/known-limitations.md`.
**Scope Excluded:** новые фиксы, включая P4.12/P4.13 `MorphColumns` repair и P4.14 rollback-test recovery (лейн всё ещё красный на реальном баге → §10, НЕ continue-on-error);
paratest (P4.3); collation-логика (P4.7 сделал).
**Inputs:** P4.8/P4.7/P4.9/P4.12/P4.13/P4.14/P4.15 (терминальны); findings/P4.10-full-lane-blockers-2026-07-22.md; findings/P4.10-debug-stdout-2026-07-22.md; research/08-p4.13-p4.14-recovery.md; research/11-p4.10-clean-vendor-union-proof.md; отложенный tests.yml-hunk (P4.2 handoff / `git diff` истории);
findings/P4.2-db-portability-failures.md; docs/introduction/upgrading.md; research/04 §4; стенд P4.1.
**Files:** `.github/workflows/tests.yml`; `findings/P4.2-db-portability-failures.md`;
`docs/introduction/upgrading.md` (+ RU-зеркало по docs-parity); `root/known-limitations.md` (opt).
**Required Reads:** research/05-codex-execution-contract.md; research/04-p4.2-remediation.md §2/§4; research/08-p4.13-p4.14-recovery.md; research/11-p4.10-clean-vendor-union-proof.md; findings/P4.2-db-portability-failures.md; findings/P4.10-full-lane-blockers-2026-07-22.md; findings/P4.10-debug-stdout-2026-07-22.md; phases/P4.md P4.13/P4.14/P4.15/P4.10; .github/workflows/tests.yml (структура matrix+services, форма P4.1/P4.2); handoff.md (порты стенда); скиллы `ci-cd`, `db-test-preflight`; bin/docs-parity-gate.sh (EN+RU).
**Implementation Rules:** first build a detached worktree from the committed P4.15 tree and run the stated fresh Composer install; local ignored vendor is not validation evidence. CI-джоб добавлять ТОЛЬКО когда оба лейна зелены локально (нечестный красный
борд запрещён); services — healthcheck + retries (не sleep); лейн всё ещё красный на реальном баге →
§10 эскалация, НЕ continue-on-error ради зелёного; union-правило = оба лейна обязательны для мержа;
baseline-finding НЕ удалять — шапка RESOLVED поверх (провенанс); UPGRADING-заметка EN+RU parity
(docs-parity-gate); зелёный CI проверяется push'ем ветки (локально непушибельно → честно отметить в Completion Notes).
**Code Guidance:** это интеграция/проводка, не новая логика; CI-джоб зеркалит конвенции существующей
матрицы tests.yml; любое число (проход suite, счёт тестов) — с якорем на committed-дерево и командой;
docs-parity обязателен; честность: «зелёно» подтверждается фактическим выводом прогона, не предположением.
**Validation:** `composer test:pgsql` зелёный (полный suite, стенд поднят); `composer test:mysql` зелёный
(полный); `composer test` (sqlite) зелёный; CI-джоб `test-db-matrix` закоммичен и зелёный на push;
`bin/docs-parity-gate.sh` зелёный (UPGRADING EN+RU); finding несёт шапку RESOLVED.
**Deliverables:** зелёные PG+MySQL-лейны + закоммиченный зелёный CI-джоб + RESOLVED-baseline + UPGRADING utf8mb4_bin-заметка.
**Completion Notes:** D41 clean detached worktree `/tmp/azguard-p410-clean` с изолированным `COMPOSER_CACHE_DIR` выполнил fresh `composer update --prefer-dist --no-interaction --no-progress`: 191 packages installed, без copied/symlinked local `vendor/`. На committed tree: `composer test` — 669 passed / 1778 assertions; `PGSQL_PORT=25432 composer test:pgsql` — 669 passed / 1778 assertions; `COMPOSER_PROCESS_TIMEOUT=900 MYSQL_PORT=23306 composer test:mysql` — 669 passed / 1786 assertions за 179.17s; все три Composer-команды exit 0 без `[DEBUG-BATCH-QUERY]`. `composer analyse` в fresh worktree, `composer lint:check`, `bin/docs-parity-gate.sh` и `git diff --check` зелёные. Пользовательский CI hunk принят commit `425c93b`; GitHub PR #93 подтвердил `DB matrix · pgsql` green (2m22s) и `DB matrix · mysql` green (3m02s). `3a85dc8` добавил EN/RU UPGRADING заметку; этот closing commit сохраняет baseline provenance через RESOLVED header. Независимый full review: APPROVE.
**Pending Work:** P4.3 → P4.4 → P4.5 → P4.6; затем `task:plan-close` P4 и независимый phase audit.
**Known Deviations:** Root ignored `vendor/` локально повреждён (включая отсутствующий `laravel/agent-detector`), поэтому root `composer analyse` не является доказательством; обязательная fresh-vendor validation выше прошла. Нерелевантные branch-level PR failures (Conventional Commit title, Infection и PHP 8.5/L13) не меняют закрытый DB-matrix acceptance P4.10 и остаются для отдельного разбора.
**Escalation History:** чистый MySQL-лейн падал в `MorphTypeTest` при автоматическом имени UUID morph-index длиной свыше 64 символов; D37 выделил P4.12-owned production-helper repair вне Scope P4.10. P4.12 item-коммит `7ce4934` снял этот blocker, поэтому P4.10 re-opened для своего исходного full-lane/CI scope.
> 🟢 RESOLVED ESCALATION P4.10: P4.13 removed the forbidden `sha1()` and P4.14 recovered PostgreSQL's expected rollback failure; a new clean full PostgreSQL/MySQL proof remains required before CI/docs/B6.
> 🟢 RESOLVED ESCALATION P4.10: P4.15 removed the ULID `22P02`; D41 now requires fresh-vendor command-level union proof before P4.10 may accept CI/docs/B6.
**Escalation Needed:** no

### P4.15 — ULID morph Testbench refresh-state isolation (D40)

**Status:** 🟠 Done with deviations
**Intent:** Make the existing ULID `MorphTypeTestCase` deterministic under the repository's random suite order by applying Testbench's supported class-local `#[ResetRefreshDatabaseState]`, so its already-early ULID config override is in force before its `RefreshDatabase` migration. RAG:✅ 2026-07-22 (findings/P4.10-ulid-refresh-state-2026-07-22.md); RAG:— (repo-grounded: tests/MorphTypeTestCase.php:11-19; phpunit.xml:6).
**Why:** clean PostgreSQL on seed `1784693070` ran the ordinary integer morph migration first, leaving shared `RefreshDatabaseState::$migrated=true`; the later MorphType class then queried an integer pivot with a ULID and failed `22P02`. A focused MorphType run is green, proving this is suite lifecycle/config timing rather than a production schema defect. RAG:✅ 2026-07-22 (findings/P4.10-ulid-refresh-state-2026-07-22.md); RAG:— (repo-grounded: /tmp/azguard-p410-final-pgsql-clean.log:929-951; vendor/laravel/framework/src/Illuminate/Foundation/Testing/RefreshDatabase.php:83-91).
**Scope Included:** only `tests/MorphTypeTestCase.php`: import `Orchestra\Testbench\Attributes\ResetRefreshDatabaseState` and annotate the existing class; retain its current `getEnvironmentSetUp()` ULID override and all existing MorphType tests. The item owns focused regression proof, full PostgreSQL/MySQL validation, and an independent full read-only review.
**Scope Excluded:** `MorphColumns`, package/config migrations, production configuration or API, P3 snapshot/SemVer, global `tests/Pest.php`, `DatabaseMigrations`, manual writes to `RefreshDatabaseState`, new order-dependent test hooks, CI/docs/baseline/B6 (P4.10), and unrelated dirty `.github/workflows/tests.yml` / `tests/Pest.php`.
**Inputs:** findings/P4.10-ulid-refresh-state-2026-07-22.md; research/10-p4.15-ulid-refresh-isolation.md; D40; P4.10 escalation; P3 freeze.
**Files:** `tests/MorphTypeTestCase.php`.
**Required Reads:** research/05-codex-execution-contract.md; findings/P4.10-ulid-refresh-state-2026-07-22.md; research/10-p4.15-ulid-refresh-isolation.md; phases/P4.md P4.10/P4.15; tests/MorphTypeTestCase.php; tests/Feature/MorphTypeTest.php; tests/Pest.php; phpunit.xml; vendor/orchestra/testbench-core/src/Attributes/ResetRefreshDatabaseState.php; vendor/laravel/framework/src/Illuminate/Foundation/Testing/RefreshDatabase.php:83-91; root/semver-policy.md §1–4; skills `diagnosing-bugs`, `laravel-package-testing`, `db-test-preflight`.
**Implementation Rules:** use exactly the installed `Orchestra\Testbench\Attributes\ResetRefreshDatabaseState` class attribute on `MorphTypeTestCase`; do not write the static state directly. Keep `getEnvironmentSetUp()` calling parent first and setting only `az-guard.column_names.morph_type=ulid`. Do not change migration ordering, redefine the test connection, add a Pest hook, or alter production `MorphColumns`; if that annotation does not make the recorded PG seed green, stop and escalate under D40.
**Code Guidance:** this is class-boundary test isolation, not a migration portability redesign. Prefer the supported Testbench annotation over `DatabaseMigrations` (per-test fresh migration), `RefreshDatabaseState` mutation, retries, skips, or a seed-specific conditional. Preserve the existing three behavior assertions: ULID columns are non-integer, role assignment/query works, and UUID explicit-index proof still works. A full reviewer must reject any global harness, migration, public config/API, snapshot, or dirty-file inclusion.
**Validation:** in a clean worktree containing only committed P4 changes: `composer test -- --random-order-seed=1784693070`; `PGSQL_PORT=25432 composer test:pgsql -- --random-order-seed=1784693070`; `PGSQL_PORT=25432 composer test:pgsql`; `COMPOSER_PROCESS_TIMEOUT=900 MYSQL_PORT=23306 composer test:mysql`; `composer lint:check && composer analyse`; `git diff --check`; scoped `git diff --name-only` only `tests/MorphTypeTestCase.php`. Then independent GPT-5.6 Sol/high read-only full review. P4.10's separate clean proof/CI/docs work is explicitly deferred.
**Deliverables:** one supported, class-local Testbench lifecycle annotation; recorded PostgreSQL random seed and normal full PG/MySQL lanes green; independent full review APPROVE; no production/P3/SemVer change.
**Completion Notes:** Item-коммит `704d16b` imported the installed `ResetRefreshDatabaseState` attribute and applied it only to `MorphTypeTestCase`, retaining the existing parent-first ULID config override and all MorphType tests. In the isolated worktree, Pest reported 669/669 tests and 1778 assertions for the SQLite seed, recorded PostgreSQL seed and ordinary PostgreSQL lane (на `704d16b`; команды: `composer test -- --random-order-seed=1784693070`, `PGSQL_PORT=25432 composer test:pgsql -- --random-order-seed=1784693070`, `PGSQL_PORT=25432 composer test:pgsql`); the recorded PG run had no `22P02`. MySQL reported 669/669 tests and 1786 assertions (на `704d16b`; команда: `COMPOSER_PROCESS_TIMEOUT=900 MYSQL_PORT=23306 composer test:mysql`). `composer lint:check`, `composer analyse`, `git diff --check` and scoped name check passed. Independent GPT-5.6 Sol/high read-only review: APPROVE; no global harness, migration, API, snapshot or dirty-file inclusion.
**Pending Work:** `task:plan-design` must re-open P4.10 around the unatttributed debug stdout that makes its SQLite and PostgreSQL Composer commands exit 1 despite 669/669 passing Pest results; then P4.10 repeats fresh clean full PostgreSQL and MySQL proof before CI/docs/baseline/B6.
**Known Deviations:** SQLite seed and recorded PostgreSQL seed printed repeated `[DEBUG-BATCH-QUERY]` output and Composer exited 1 after Pest reported all 669 tests passed; this material validation failure is outside P4.15's one-file Scope and is not suppressed. `PYTHONPATH=packages/task/lib python3 -m task.journal` is unavailable in this standalone repository, so no P4.15 closing journal entry could be appended.
**Escalation Needed:** no

### P4.12 — Portable explicit names для UUID morph indexes (D37)

**Status:** 🟢 Done
**Intent:** Устранить MySQL installability defect configurable UUID morph tables: `MorphColumns` передаёт Laravel короткие deterministic table-aware names для morph indexes, а UUID regression proof сохраняет длинные fixture table names и проходит на SQLite/PostgreSQL/MySQL. RAG:— (repo-grounded: findings/P4.10-uuid-morph-index-name-2026-07-22.md).
**Why:** Laravel default name собирается из table+columns; fixture повторяет production migration 000000, поэтому её сокращение скрывает defect реального configurable table name. RAG:— (repo-grounded: findings/P4.10-uuid-morph-index-name-2026-07-22.md; packages/core/database/migrations/2026_01_01_000000_create_az_guard_tables.php:20-30).
**Scope Included:** private helper в `MorphColumns` создаёт имя <64 из actual `$table->getTable()` fingerprint + morph name, уникальное между tables в PostgreSQL schema; все int/ULID/UUID nullable/non-nullable пути передают name Laravel helper. `MorphTypeTest` сохраняет длинные names, использует `MorphColumns::add()` и доказывает index creation без driver SQL.
**Scope Excluded:** fixture shortening; migration 000005/P4.8; CI/workflow, Composer timeout и full suite (P4.10); config/public API/API snapshot; driver skip; P4.7/P4.9.
**Inputs:** findings/P4.10-uuid-morph-index-name-2026-07-22.md; research/07-p4.12-morph-index-portability.md; D37; P4.10 escalation; P3 freeze.
**Files:** `packages/core/src/Database/Schema/MorphColumns.php`; `tests/Feature/MorphTypeTest.php`.
**Required Reads:** research/05-codex-execution-contract.md; research/07-p4.12-morph-index-portability.md; findings/P4.10-uuid-morph-index-name-2026-07-22.md; phases/P4.md P4.10/P4.12; MorphColumns.php; migration 000000; MorphTypeTest.php; root/semver-policy.md §1–4; скиллы `database`, `laravel-package-testing`, `db-test-preflight`, `diagnosing-bugs`.
**Implementation Rules:** один private source of truth для short name; одинаково все morph paths, не только UUID. Если нужны public signature/config/API snapshot changes или два tables получают одно имя на PostgreSQL, остановиться по §10/D37; fixture не сокращать и driver branch не вводить. RAG:— (repo-grounded: vendor/laravel/framework/src/Illuminate/Database/Schema/Blueprint.php:1639-1670,1797-1811; tests/Fixtures/api-surface.snapshot.php).
**Code Guidance:** anti-подгонка `code-guidance-patterns.md` §1: запрещены fixture shortening, MySQL-only skip, raw driver metadata и suppression index creation. P4.10 timeout 900 не substitute for targeted proof.
**Validation:** `composer test -- --filter='creates the scopes unique index when the morph type is uuid'`; `PGSQL_PORT=25432 composer test:pgsql -- --filter='creates the scopes unique index when the morph type is uuid'`; `COMPOSER_PROCESS_TIMEOUT=900 MYSQL_PORT=23306 composer test:mysql -- --filter='creates the scopes unique index when the morph type is uuid'`; `composer lint:check && composer analyse`; `git diff --check`; scoped `git diff --name-only` only Files. P4.10 full suite не запускать.
**Deliverables:** internal deterministic short morph-index naming + unchanged long-name UUID proof на 3 drivers + Sol/high read-only APPROVE; SemVer/API snapshot untouched.
**Completion Notes:** Session: implementation/high. Item-коммит `7ce4934` добавил private `MorphColumns::morphIndexName()` с детерминированным 48-символьным именем из фактической Blueprint-таблицы и morph name; все int/ULID/UUID nullable/non-nullable пути, включая key-type-options, передают его Laravel. Длинные UUID fixture names сохранены, а regression proof вызывает `MorphColumns::add()` для обоих morph pairs. На `7ce4934` прошли focused SQLite, PostgreSQL и MySQL команды Validation; также прошли `composer lint:check`, `composer analyse` и `git diff --check`. Независимый Sol/high read-only review фактического scoped diff: APPROVE, findings нет.
**Pending Work:** P4.10 возобновляет full MySQL proof с `COMPOSER_PROCESS_TIMEOUT=900`, затем свой CI/docs scope.
**Known Deviations:** —
**Escalation Needed:** no

### P4.13 — P4.12 repair: permitted deterministic digest для morph-index names (D38)

**Status:** 🟢 Done
**Intent:** Заменить только запрещённый security architecture `sha1()` в private `MorphColumns::morphIndexName()` на permitted deterministic digest тех же `$table->getTable()` + morph-name inputs, сохранив P4.12's table-aware, 48-character index identifier и все int/ULID/UUID nullable/non-nullable paths. RAG:— (repo-grounded: findings/P4.10-full-lane-blockers-2026-07-22.md §A; packages/core/src/Database/Schema/MorphColumns.php:20-100).
**Why:** clean PostgreSQL и MySQL full lanes оба остановились на ArchTest, потому что P4.12's `sha1()` violates the repository security preset; targeted DDL proof alone therefore does not meet the package gate. The installability defect D37 is already fixed and must not regress. RAG:— (repo-grounded: findings/P4.10-full-lane-blockers-2026-07-22.md §A; tests/ArchTest.php:13; phases/P4.md P4.12 Completion Notes).
**Scope Included:** only `MorphColumns::morphIndexName()` and the UUID morph regression proof: replace `sha1()` with `hash('sha256', ...)` truncated to the existing 40 hexadecimal-character digest budget (`azg_` + 40 + `_idx` = 48); retain exactly the actual table-name + morph-name input; assert the existing long-name two-morph fixture still creates both indexes through `MorphColumns::add()` on SQLite, PostgreSQL and MySQL.
**Scope Excluded:** changing P4.12/P4.10 Completion Notes or statuses; fixture shortening; any migration, config, public signature or API snapshot change; a driver branch/skip; CI, Composer timeout and full suite (P4.10); changing the architecture rule to permit weak hashes.
**Inputs:** findings/P4.10-full-lane-blockers-2026-07-22.md §A; research/08-p4.13-p4.14-recovery.md; D37/D38; P4.12 Completion Notes; P3 freeze.
**Files:** `packages/core/src/Database/Schema/MorphColumns.php`; `tests/Feature/MorphTypeTest.php` (only if the existing proof needs an explicit deterministic-name assertion).
**Required Reads:** research/05-codex-execution-contract.md; findings/P4.10-full-lane-blockers-2026-07-22.md §A; research/07-p4.12-morph-index-portability.md; research/08-p4.13-p4.14-recovery.md; phases/P4.md P4.12/P4.13; packages/core/src/Database/Schema/MorphColumns.php; tests/Feature/MorphTypeTest.php; tests/ArchTest.php; root/semver-policy.md §1–4; skills `database`, `laravel-package-testing`.
**Implementation Rules:** use `hash('sha256', $table->getTable().'_'.$name)` and truncate its hexadecimal output to 40 characters before the existing prefix/suffix; do not use `sha1`, `md5`, random/clock data or Laravel-generated long names. The helper remains private and uses no driver metadata. If the architecture test rejects the permitted hash, if either long-name morph index cannot be created, or if a public/config/snapshot change appears necessary, stop with §10/D38 rather than weakening a gate.
**Code Guidance:** anti-fitting is mandatory: preserve the long configurable-table fixture and the existing two morph pairs; prove the real helper path, not a hand-written index or a MySQL-only path. Do not edit `tests/ArchTest.php`; security architecture is the acceptance criterion, not a hurdle to bypass. A Sol/high reviewer must independently inspect the scoped diff and three-driver output before this item closes.
**Validation:** `composer test -- --filter='preset → security'`; `composer test -- --filter='creates the scopes unique index when the morph type is uuid'`; `PGSQL_PORT=25432 composer test:pgsql -- --filter='creates the scopes unique index when the morph type is uuid'`; `COMPOSER_PROCESS_TIMEOUT=900 MYSQL_PORT=23306 composer test:mysql -- --filter='creates the scopes unique index when the morph type is uuid'`; `composer lint:check && composer analyse`; `git diff --check`; scoped `git diff --name-only` only Files. P4.10 full suites are explicitly deferred.
**Deliverables:** P4.12's short table-aware internal name with a security-permitted deterministic digest; green ArchTest and focused three-driver long-name proof; independent Sol/high APPROVE; no P3 snapshot/SemVer change.
**Completion Notes:** Session: GPT-5.6 Terra/high. Item-коммит `cf85e16` заменил только private digest на `substr(hash('sha256', $table->getTable().'_'.$name), 0, 40)`, сохранив 48-character identifier and table+morph inputs. На `cf85e16`: focused SQLite, PostgreSQL и MySQL UUID long-name proof прошли по 1 test / 1 assertion каждая; `composer lint:check`, `composer analyse` и `git diff --check` прошли. Direct `composer test -- tests/ArchTest.php` прошёл 17 tests / 35 assertions и подтвердил security preset. Independent GPT-5.6 Sol/high read-only review commit `cf85e16`: APPROVE, material findings none.
**Pending Work:** P4.14 then P4.10; do not accept CI/docs/B6 before their full-lane gate.
**Known Deviations:** Literal Validation selector `composer test -- --filter='preset → security'` exits 1 with `No tests found`; `tests/ArchTest.php` is the direct, passing security-gate proof. No source workaround or architecture-rule change was made.
**Escalation Needed:** no

### P4.14 — PG rollback-test: transaction-safe expected failure recovery (D39)

**Status:** 🟢 Done
**Intent:** Keep the documented cross-driver negative proof that migration 000004 `down()` rejects a null `scope_class` row as `QueryException`, while recovering only PostgreSQL's ambient `RefreshDatabase` transaction via a local `pgsql` savepoint wrapper. After the expected exception, prove the same testbench connection accepts a normal query. RAG:✅ 2026-07-22 (findings/P4.14-laravel-transaction-semantics-2026-07-22.md); RAG:— (repo-grounded: findings/P4.10-full-lane-blockers-2026-07-22.md §B; tests/Feature/ScopeClassMigrationRollbackTest.php:28-54).
**Why:** PostgreSQL aborts its outer transaction after the expected DDL constraint error unless the failure is rolled back to a nested savepoint; the first attempt proved that recovery but MySQL DDL invalidated its nested savepoint and replaced the required `QueryException` with `PDOException`. A local driver-aware helper preserves both proven contracts without changing the migration or global harness. RAG:✅ 2026-07-22 (findings/P4.14-laravel-transaction-semantics-2026-07-22.md); RAG:— (repo-grounded: commit 93b01a0; tests/Feature/ModelHasRolesScopesUniqueConstraintTest.php:17-32).
**Scope Included:** only `ScopeClassMigrationRollbackTest`: add a local helper that applies `DB::transaction()` around the actual `migration->down()` closure only when `DB::getDriverName() === 'pgsql'`; SQLite/MySQL execute the same closure directly. Retain `toThrow(QueryException::class)` on all three drivers and, after it, assert a normal query against the configured `model_has_scopes` table succeeds on the same connection. Keep the positive no-null-row test.
**Scope Excluded:** migration 000004 or any production schema/runtime change; Pest global registration, test connection configuration, broad driver-neutral `Throwable`/message/SQLSTATE assertion, a driver skip, exception catch/rewrite, changing the migration docblock, CI/full lanes (P4.10), P4.13 helper repair, public API/configuration/snapshot.
**Inputs:** findings/P4.10-full-lane-blockers-2026-07-22.md §B; findings/P4.14-laravel-transaction-semantics-2026-07-22.md; research/08-p4.13-p4.14-recovery.md; research/09-p4.14-driver-aware-savepoint.md; D38/D39; tests/Feature/ScopeClassMigrationRollbackTest.php; tests/Feature/ModelHasRolesScopesUniqueConstraintTest.php; migration 000004; P4.2 Completion Notes.
**Files:** `tests/Feature/ScopeClassMigrationRollbackTest.php`.
**Required Reads:** research/05-codex-execution-contract.md; findings/P4.10-full-lane-blockers-2026-07-22.md §B; findings/P4.14-laravel-transaction-semantics-2026-07-22.md; research/08-p4.13-p4.14-recovery.md; research/09-p4.14-driver-aware-savepoint.md; phases/P4.md P4.2/P4.10/P4.14; tests/Feature/ScopeClassMigrationRollbackTest.php; tests/Feature/ModelHasRolesScopesUniqueConstraintTest.php:17-32; tests/Pest.php; tests/TestCase.php; vendor/laravel/framework/src/Illuminate/Database/Concerns/ManagesTransactions.php:26-46,145-174,261-312; packages/core/database/migrations/2026_01_01_000004_make_scope_class_nullable_on_model_has_scopes.php; skills `diagnosing-bugs`, `db-test-preflight`, `laravel-package-testing`.
**Implementation Rules:** import/use the Laravel DB facade only in this test. Define the helper locally in this file; it branches only on the exact `pgsql` driver name and does not catch, rethrow or normalize exceptions. Its PostgreSQL branch must put the actual `down()` call inside `DB::transaction()`; its SQLite/MySQL branch must invoke the original closure unchanged. Keep `QueryException` as the assertion on every driver. Immediately after that assertion, query the configured `model_has_scopes` table through `DB::table()` and assert success; no debug logging/instrumentation may remain. If PG still emits `25P02`, any driver loses `QueryException`, the normal query fails, or global isolation becomes necessary, stop under §10/D39.
**Code Guidance:** copy the proven narrow shape of `expectUniqueConstraintViolation()` rather than inventing a global abstraction: PostgreSQL is an explicit savepoint-recovery exception, not a skip. Do not use `Throwable`, a broad `try/catch`, a driver-specific expected class, a raw PDO savepoint, or a test order dependency. The two semantic invariants are non-negotiable: null data makes rollback fail as `QueryException`, and the following lifecycle query works. A full reviewer must inspect three-driver output and confirm no migration/Pest/config/API path changed.
**Validation:** `composer test -- --filter='T5 — migration 000004 down\\(\\) rollback'`; `PGSQL_PORT=25432 composer test:pgsql -- --filter='T5 — migration 000004 down\\(\\) rollback'` twice, each green with the post-exception query; `COMPOSER_PROCESS_TIMEOUT=900 MYSQL_PORT=23306 composer test:mysql -- --filter='T5 — migration 000004 down\\(\\) rollback'`; `composer lint:check && composer analyse`; `git diff --check`; scoped `git diff --name-only` only Files. Then independent GPT-5.6 Sol/high read-only full review. P4.10 full suites are explicitly deferred.
**Deliverables:** D39 driver-aware transaction-safe expected rollback regression proof; PostgreSQL no longer emits `25P02` from this test lifecycle; SQLite/MySQL retain the direct `QueryException` negative/positive proof; independent full review APPROVE; no production/API/SemVer change.
**Completion Notes:** Session: GPT-5.6 Terra/high. Item-коммит `976909e` added file-local `withPostgresSavepoint()`: only `pgsql` executes the actual `down()` closure inside `DB::transaction()`, while SQLite/MySQL retain the direct closure. The existing `QueryException` assertion is followed by `DB::table()` on configured `model_has_scopes`. Focused SQLite passed 2 tests / 4 assertions (на `976909e`); PostgreSQL passed 2 tests / 4 assertions twice without `25P02` (на `976909e`); MySQL passed 2 tests / 4 assertions with `COMPOSER_PROCESS_TIMEOUT=900` (на `976909e`); `composer lint:check`, `composer analyse`, and `git diff --check` passed. Independent GPT-5.6 Sol/high read-only full review: APPROVE, findings none.
**Pending Work:** P4.10 resumes in a clean worktree containing only committed P4 changes: full `PGSQL_PORT=25432 composer test:pgsql`, then `COMPOSER_PROCESS_TIMEOUT=900 MYSQL_PORT=23306 composer test:mysql`. Only if both are green may P4.10 accept its CI/docs/baseline deliverables and B6 review.
**Known Deviations:** The `task:plan-run` session used GPT-5.6 Terra/high instead of Routing implementation/medium (process-only over-tier). `PYTHONPATH=packages/task/lib python3 -m task.journal` was unavailable because this repository has no `packages/task/lib`; no journal entries were written.
**Escalation Needed:** no

## Phase Handoff

P4.10 закрыт: fresh-vendor SQLite/PostgreSQL/MySQL proof, CI DB matrix, EN/RU docs и baseline provenance подтверждены. Далее P4.3 выполняет paratest/random-order hardening; затем P4.4–P4.6.
