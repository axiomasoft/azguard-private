# P2 — Структурный канон + fluent/DX редизайн API

## Phase Context

Финальная пре-1.0 волна свободных breaking-изменений (бриф п.7 + прямое указание владельца
«идеальная структура, любые breaking, лучшие решения» — D14): доменный канон core (роспуск
Support/-свалки, переезд корневых типов, двух-домовый канон контрактов), fluent/DX редизайн
публичного API по 9 кластерам P0.6 (единый immutable grant-корень core↔context, config→fluent,
`::using()`-middleware, `AzGuard::fake()`, словарь терминов, локус фасада, headless-порог,
контрактные швы), пере-оценка отложенных на 1.0 breaking (wildcard-флип F22; F4/F40/F51 —
verify). Каждое несущее решение — D-запись + синтез research/03-p2-canon.md. Развилки
грамматики/TTL/fake/headless РАЗРЕШЕНЫ (D14–D18): immutable-with единый корень, TTL-парность,
fake() строим, headless doc-only, wildcard→Hierarchical. Завершение: канон применён, Support/
упразднён, arch-тесты расширены, docs EN/RU в паритете, `composer check` зелёный.

> **Контракт фазы (contract-first).** Потребляет: research/02-backlog.md (9 кластеров, 14
> находок) · research/03-p2-canon.md (целевая структура + грамматика) · findings/P0-axis-{a,b,c,d}-*.md ·
> закрытую базу P1 (чистый main; D-04 arch-Registry\Contracts и A-01 doctor-examples уже сделаны P1).
> Производит: финальную доменную структуру и публичные имена · `root/contracts/facade-cutline.md`
> (вход cut-line P3) · `root/glossary.md` · ADR/D-записи · расширенные arch-тесты · миграцию
> context expires_at · обновлённые docs EN/RU. Границы: severity-находки закрыты P1 (не
> переделывать); заморозка/SemVer/исполнение cut-line — P3; новая функциональность (кроме
> fake() и headless-quick-start) не добавляется; рантайм panel-less путь НЕ строится (YAGNI, D14).

## Phase Status

| ID | Title | Status | Updated |
|:--|:--|:--|:--|
| P2.1 | Структурный канон core: роспуск Support/, корневые типы, двух-домовый канон контрактов | 🟠 Done with deviations | 2026-07-18 |
| P2.2 | Контрактные швы: 6 структурных baseline → уточнение контрактов/generics | 🟠 Done with deviations | 2026-07-18 |
| P2.3 | Единая grant-грамматика: immutable fluent-корень core↔context + TTL-парность | 🟠 Done with deviations | 2026-07-18 |
| P2.4 | Config→fluent: Filament-плагин + middleware `::using()` + единый порядок аргументов | 🟠 Done with deviations | 2026-07-18 |
| P2.5 | Локус фасада: cut-line target-спека (вход P3) | 🟢 Done | 2026-07-18 |
| P2.6 | Testing DX: `AzGuard::fake()` (Recorder + ассерции) | 🟢 Done | 2026-07-18 |
| P2.7 | Словарь терминов: глоссарий + docs-маршрутизация (guard=бренд, context↔scope) | ⬜ Not started | 2026-07-18 |
| P2.8 | Headless-порог: minimal-setup quick-start + doctor-hint (doc-only) | ⬜ Not started | 2026-07-18 |
| P2.9 | Пере-оценка breaking: wildcard-флип на Hierarchical + verify F4/F40/F51 | 🟠 Done with deviations | 2026-07-18 |
| P2.10 | Консолидация: docs EN/RU паритет + arch-тесты канона | ⬜ Not started | 2026-07-18 |

### P2.1 — Структурный канон core: роспуск Support/, корневые типы, двух-домовый канон контрактов

**Status:** 🟠 Done with deviations
**Intent:** Упразднить catch-all `AzGuard\Support` (9 файлов, 6 ролей — D-05), перевезти файлы и два корневых типа (PanelProvider, PermissionKey) в доменные неймспейсы по карте research/03-p2-canon.md §1; закрепить двух-домовый канон контрактов (Contracts/ + Registry\Contracts — сознательно, не сливать).
**Why:** Граница «Support = всё остальное» не несёт смысла и размывает @api/@internal по соседству (D-05, Minor, но структурный корень фазы «идеальная структура» — D14). Пре-1.0 свобода переименований (бриф п.7) делает переезд бесплатным по миграции. RAG:— (repo-grounded: findings/P0-axis-d-structure.md §C-D1/§D-05, §C-D2).
**Scope Included:** Перенос 9 файлов Support/ (вкл. Schema/MorphColumns) + PanelProvider.php + PermissionKey.php в целевые неймспейсы (карта §1); упразднение `AzGuard\Support`; обновление ВСЕХ ссылок (use/FQCN) в core/filament/context/tests; обновление PSR-4 при необходимости; фиксация двух-домового канона контрактов в arch-тесте (проверить, что P1-D-04 расширил expectation на Registry\Contracts — если нет, расширить здесь) и в docblock/ADR.
**Scope Excluded:** Слияние Contracts/ и Registry\Contracts (решено НЕ сливать — §1); правка контрактных сигнатур (это P2.2); переезд AzGuardManager/AzGuardServiceProvider (канон, на месте).
**Inputs:** research/03-p2-canon.md §1 (карта переездов) · findings/P0-axis-d-structure.md §C-D1/§C-D2 · research/02-backlog.md кластер 5.
**Files:** packages/core/src/Support/** (переезд+удаление) · packages/core/src/PanelProvider.php · packages/core/src/PermissionKey.php · новые каталоги packages/core/src/{Panels,Permissions,Configuration,Runtime,Abilities,Auth,Database/Schema}/ · все потребители (grep FQCN) в packages/{core,filament,context}/src/** · tests/** (ссылки) · tests/ArchTest.php · packages/*/composer.json (PSR-4, если менялось) · root/architecture.md (ADR структуры).
**Required Reads:** research/03-p2-canon.md §1, §10 · findings/P0-axis-d-structure.md §C-D1 · tests/ArchTest.php:60-96 (namespace-matrix) · ARCHITECT_REVIEW.md §6.
**Implementation Rules:** namespace-refactor — по скиллу `php:namespace-refactor-safety` (три фазы: alias→update use→hard rename; anchor find-replace на FQCN, не на короткое имя). Ни одного файла не оставить в Support/ (неймспейс упразднён полностью). `@api`/`@internal`-докблоки сохранить при переезде (D-05: не размывать границу). AzGuardManager/ServiceProvider не трогать. Двух-домовый канон контрактов — задокументировать явно (root/architecture.md): Contracts/ = cross-cutting публичные; Registry\Contracts = субдоменные (locality). Если P1-D-04 уже расширил ArchTest на Registry\Contracts — только сверить; если нет — расширить expectation (tests/ArchTest.php:21).
**Code Guidance:** Механический, но широкий рефактор — риск пропущенной ссылки высок: после переезда grep по СТАРЫМ FQCN (`AzGuard\\Support\\`, `AzGuard\\PanelProvider`, `AzGuard\\PermissionKey`) обязан быть пуст ВЕЗДЕ (src+tests+docs-примеры). Не переименовывать «на ходу» классы (только неймспейс) — переименование имён классов вне scope. Запрещено `git add -A` (Execution Rules) — коммит по Files. Если переезд ломает автозагрузку тестов (tests/Pest.php биндинги) — поправить в Files, не эскалировать. НЕ трогать docs-контент кроме code-fence FQCN (полный docs-свип — P2.10). if-then: если обнаружится потребитель Support/-класса ВНЕ монорепо-пакетов (неизвестный шов) → эскалация §10 (public contract).
**Validation:** `grep -rE 'AzGuard\\(Support|PanelProvider|PermissionKey)' packages tests --include='*.php'` == пусто (кроме новых Permissions\PermissionKey и т.п.) · `composer analyse` зелёный · `composer test:unit` зелёный · `php vendor/bin/pest tests/ArchTest.php` зелёный (namespace-matrix + contracts-interfaces на обоих доменах) · `composer lint:check` зелёный.
**Deliverables:** Support/ упразднён; 11 файлов в доменных неймспейсах; все ссылки обновлены; двух-домовый канон контрактов в root/architecture.md + arch-тест; зелёные analyse/unit/arch/lint.
**Completion Notes:** Session: fable/high (plan-run, D18). 11 файлов перевезены через `git mv` (история сохранена) по карте research/03-p2-canon.md §1: Support/ (9, вкл. Schema/MorphColumns) + корневые PanelProvider/PermissionKey → Panels/ · Permissions/ · Configuration/ · Runtime/ · Abilities/ · Auth/ · Database/Schema/; каталог Support/ удалён. FQCN-свип anchored (longest-first, single+double-backslash формы, вкл. Blade-директивы AzGuardServiceProvider и stub guardpanelprovider.stub). Ручные дофиксы сверх механики: `use Configuration\Config` в PanelResolver, `use AzGuard\AzGuardManager` в Panels/PanelProvider (бывшие соседи по неймспейсу), 3 docblock-`@see` FQCN. PSR-4 не менялся (`AzGuard\ → src/`). ArchTest: expectation `registry contracts are interfaces` уже покрывает Registry\Contracts (P1-D-04, tests/ArchTest.php:24-30) — сверено, правка не потребовалась; двух-домовый канон задокументирован в root/architecture.md (ADR + инварианты). Item-коммит 9cc1897: 109 files changed, +214/−158 (`git show --stat 9cc1897`). Validation (все на 9cc1897): grep старых FQCN по packages+tests == 0 строк · `composer analyse` passed 0 errors · `composer test:unit` 269 passed/714 assertions · `php vendor/bin/pest tests/ArchTest.php` 16 passed/31 · `composer lint:check` passed. Сверх ТЗ: полный `composer test` 610 passed/1639 assertions (страховка от строковых FQCN в рантайме).
**Pending Work:** Каталог tests/Unit/Support/ (5 тест-файлов) остался под старым именем — ссылки обновлены, но имя каталога дрейфует от канона; кандидат в консолидацию P2.10. Полный docs-свип (кроме FQCN code-fence) — P2.10 по ТЗ.
**Known Deviations:** (1) material: item-коммит несёт дифф вне литерального перечня Files — docs/** (8 страниц, только FQCN в code-fence), packages/core/stubs/panel/guardpanelprovider.stub, packages/{core,context}/database/migrations (3 миграции, импорт MorphColumns); все три группы предписаны Code Guidance/Validation самого ТЗ («grep по старым FQCN пуст ВЕЗДЕ», «docs-контент — только code-fence FQCN»), перечень Files их не назвал — гэп перечня, не scope drift. (2) process: три фазы скилла namespace-refactor-safety схлопнуты в один hard-rename проход без alias-стадии — все потребители в монорепо, Support/ упраздняется в том же item'е (alias-стадии нечего защищать), безопасность обеспечена FQCN-якорями + гейтами analyse/full-test.
**Escalation Needed:** no

### P2.2 — Контрактные швы: 6 структурных baseline → уточнение контрактов/generics

**Status:** 🟠 Done with deviations
**Intent:** Разрешить 6 «структурных» phpstan-baseline-записей (D-09) уточнением контрактов/типов на реальных швах (карта research/03-p2-canon.md §2), затем УДАЛИТЬ эти baseline-записи.
**Why:** Baseline легализует вызовы мимо объявленных контрактов на реальных швах (Authorizer, PermissionDefinition::label, Model::dbPermissions, панель-провайдер) — тихий обход типовой границы (D-09, Minor; вход P2 по backlog). RAG:— (repo-grounded: phpstan-baseline.neon:22,52,148,172,184,190; findings/P0-axis-d-structure.md §C-D7/§D-09).
**Scope Included:** 6 швов из §2: (1) Authorizer::check тип $user; (2) ServiceProvider::panel() в контракт; (3) Authenticatable::$roles через контракт AzGuardUser/трейт; (4) PermissionDefinition::label() ×2 в контракт; (5) Model::dbPermissions() в контракт роли/модели; удаление соответствующих baseline-строк.
**Scope Excluded:** 17 «снимаемых» baseline (P1-W2) и 12 «легитимных» (природа пакета) — не трогать; переезды неймспейсов (сделаны P2.1).
**Inputs:** research/03-p2-canon.md §2 (карта швов) · findings/P0-axis-d-structure.md §C-D7 «структурные» · phpstan-baseline.neon.
**Files:** phpstan-baseline.neon · packages/core/src/Guard/Authorizer.php · packages/core/src/Registry/Contracts/PermissionDefinition.php · packages/core/src/Contracts/{AzGuardUser,PanelProvider-контракт,role-контракт}.php (по швам) · packages/core/src/Panels/PanelProvider.php (после P2.1) · соответствующие реализации.
**Required Reads:** research/03-p2-canon.md §2 · findings/P0-axis-d-structure.md §C-D7 · packages/core/src/Contracts/AzGuardUser.php · ARCHITECT_REVIEW.md §6 (контракты только на реальных швах).
**Implementation Rules:** Разрешать шов уточнением ТИПА/КОНТРАКТА, не подавлением (baseline-строка удаляется, не переписывается). Инвариант §6 «контракты только на реальных швах» — не плодить контракты ради phpstan; каждый добавленный метод контракта обязан иметь реального потребителя. `label()`/`dbPermissions()`/`panel()` — добавлять в СУЩЕСТВУЮЩИЙ контракт (PermissionDefinition, роль-контракт, панель-провайдер), не создавать новый. Зависит от P2.1 (пути неймспейсов).
**Code Guidance:** Не «глушить» phpstan `@phpstan-ignore` вместо уточнения контракта — цель D-09 именно убрать обход. Если уточнение типа $user в Authorizer::check тянет каскад (Authorizable vs Authenticatable по всему Gate-шву) → сузить до минимального пересечения контрактов, не расширять scope на весь Gate. if-then: если разрешение шва требует SemVer-breaking правки публичного контракта интерфейса → это легально (пре-1.0, D14), но зафиксировать в Completion Notes как breaking. Валидация: 6 baseline-строк исчезли И phpstan не породил новых.
**Validation:** `composer analyse` зелёный БЕЗ 6 удалённых baseline-записей (проверить `grep -cE 'label\(\)|dbPermissions|Authorizer.*Authenticatable|ServiceProvider.*panel|Authenticatable.*roles' phpstan-baseline.neon` == 0) · `composer test:unit` зелёный · `composer lint:check` зелёный.
**Deliverables:** 6 швов разрешены уточнением контрактов; 6 baseline-строк удалены; analyse зелёный без них.
**Completion Notes:** Session: fable/high (plan-run, D18). Все 6 baseline-записей разрешены уточнением типа/контракта, ни одного `@phpstan-ignore`: (1) `:22` — `Authorizer::check` принимает `Authorizable&Authenticatable` (пересечение контрактов по карте §2), внутренний runtime-guard `! instanceof Authenticatable` удалён — pass-through перенесён в `Gate::before`-клоужер провайдера (обе instanceof-проверки); **breaking**: Authorizable-only юзер теперь TypeError, тест-пин переписан (`AuthorizerTest`). (2) `:52` — `MakeGuardRoleCommand::getProviderClassById` narrowing `$instance instanceof PanelProvider` (метод уже объявлен контрактом — абстрактным `PanelProvider::panel()`; новый интерфейс не создавался, §6-11), заодно снят потенциальный null-deref `getProvider()`. (3) `:148` — `ClassRoleGrantSource`: гейт дополнен `$user instanceof Model` (fail-closed: relation вне Eloquent-модели невозможна), trait-гейт СОХРАНЁН (документированное решение b81f7f2 — trait-direct юзеры продолжают резолвиться; вариант «гейт на контракт instanceof» отклонён как тихая регрессия их грантов), доступ `$user->roles` → `$user->getAttribute('roles')` c `@var Collection<int, Role>` — та же runtime-семантика (`__get` → `getAttribute`, relation-cache сохранён), downstream-цепочка стала типизированной. (4) `:172`/`:184` — `label(): ?string` добавлен в контракт `PermissionDefinition` + реализации Simple/Enum (делегация `meta()->label()`); baseline маскировал РЕАЛЬНЫЙ runtime-баг — ни одна реализация `label()` не имела, Filament-вьюхи (DirectGrantResource, RolePermissionsRelationManager) звали несуществующий метод; тест-пин `test_label_on_definition_mirrors_meta_label`; **breaking**: внешние реализации @api-контракта обязаны добавить метод. (5) `:190` — `RolePermissionsRelationManager`: owner-record narrowing до `Role` хелпером `ownerRole()` (assert instanceof; контракт роли-модели = базовый класс `Role`, интерфейс не плодился — §6-11), `canViewForRecord` — instanceof-гейт, избыточный `if ($role instanceof Role)` у flush-цикла удалён; narrowing сделал ненужным scoped-ignore `Model::$class_name|$id` в phpstan.neon — удалён (форсировано `reportUnmatchedIgnoredErrors: true`). Анонимные тест-реализации контракта дополнены `label()` (CompositePermissionCatalogTest ×2). Item-коммит 0724a24: 13 files, +90/−67 (`git show --stat 0724a24`). Validation (все на 0724a24): `grep -cE 'label\(\)|dbPermissions|Authorizer.*Authenticatable|ServiceProvider.*panel|Authenticatable.*roles' phpstan-baseline.neon` == 0 · `composer analyse` passed 0 errors · `composer test:unit` 270 passed/716 assertions · `composer lint:check` passed. Сверх ТЗ: полный `composer test` 611 passed/1641 assertions (610→611: +1 тест label).
**Pending Work:** SimplePermissionDefinition::label() покрыт только косвенно (interface-гейт analyse + Composite-тесты); прямой unit-тест дефиниции — кандидат P2.10. Generics `MorphToMany<Role, Model>` в контракте `Contracts\HasRoles::roles()` не уточнялись (шов :148 разрешён без этого) — кандидат при grant-грамматике P2.3/P2.10.
**Known Deviations:** (1) material: item-коммит несёт дифф вне перечня Files — phpstan.neon (удаление scoped-ignore, форсировано reportUnmatchedIgnoredErrors после narrowing) и tests/{Unit/Guard/AuthorizerTest,Unit/Registry/CompositePermissionCatalogTest,Unit/Registry/EnumPermissionDefinitionTest}.php (пины изменённых контрактов; Validation ТЗ требует зелёный test:unit); обе группы — прямое следствие предписанных швов, гэп перечня, не scope drift. Пути baseline-записей (AzGuardServiceProvider, MakeGuardRoleCommand, ClassRoleGrantSource, RolePermissionsRelationManager) трактованы как «соответствующие реализации» из Files. (2) SemVer-breaking по Code Guidance (легально, D14): `Authorizer::check` сигнатура Authorizable → Authorizable&Authenticatable; `PermissionDefinition` (@api) + `label()`.
**Escalation Needed:** no

### P2.3 — Единая grant-грамматика: immutable fluent-корень core↔context + TTL-парность

**Status:** 🟠 Done with deviations
**Intent:** Свести две grant-грамматики (core fluent-корень + позиционные shorthands vs ручной `new ContextGrantBuilder`) в ЕДИНЫЙ immutable fluent-корень `AzGuard::forUser()` с context-расширением `->inContext()` и TTL-парностью; builders — `final readonly` + with-методы; арх-ратчет расширить.
**Why:** Потребитель мультиворкспейса учит две грамматики одной операции «выдать право» (B-03, Major); builders мутабельны и вне арх-ратчета вопреки канону F49 (B-08, Minor). Развилка разрешена — immutable-with + единый корень + TTL-парность (D16, «лучшие решения»). RAG:— (repo-grounded: findings/P0-axis-b-fluent.md §C-B5/§C-B9); канон F49 RAG:— (tests/ArchTest.php:120-122).
**Scope Included:** (1) Единый корень: `AzGuard::forUser($user)` возвращает immutable builder; context-ветка — `->inContext($type,$id)` того же корня, не отдельный `new`; (2) immutable: builders `final readonly`, скоуп-сеттеры (on/inContext/until/ttl) возвращают новый инстанс; (3) TTL-парность: ContextGrantBuilder получает until()/ttl() + active()-фильтр в grants(); НОВАЯ миграция `expires_at` в context-хранилище; (4) арх-ратчет toBeFinal→toBeReadonly расширить на AzGuard\Grants + context-builder-неймспейс; (5) фасадные позиционные shorthands grant/revoke/grants → @internal (docblock, публичный путь = fluent).
**Scope Excluded:** Исполнение cut-line фасада (P2.5 решает target, P3 режет); полиморфизм panelId/role string|BackedEnum (сделан P1-W1 B-04); config→fluent (P2.4).
**Inputs:** research/03-p2-canon.md §3 · findings/P0-axis-b-fluent.md §C-B5 (карта расхождений) / §B-03 / §B-08 · research/01-fluent-api-priors.md §B (приоры 6,9).
**Files:** packages/core/src/Grants/GrantBuilder.php · packages/core/src/AzGuardManager.php (forUser/shorthands) · packages/core/src/Facades/AzGuard.php (@internal docblocks) · packages/context/src/ContextGrantBuilder.php · packages/context/src/Models/** (expires_at) · packages/context/database/migrations/** (новая миграция) · tests/ArchTest.php · tests/{Unit,Feature}/** (грамматика/TTL) · docs/advanced/context.md · docs/basic-usage/direct-grants.md.
**Required Reads:** research/03-p2-canon.md §3, §10 · findings/P0-axis-b-fluent.md §C-B5 · packages/core/src/Grants/GrantBuilder.php · packages/context/src/ContextGrantBuilder.php · tests/ArchTest.php:104-135 · ARCHITECT_REVIEW.md §6 (F49, прямые предикаты одним вызовом).
**Implementation Rules:** Immutable-with строго: каждый сеттер возвращает `new self(...)` с скопированным состоянием, НЕ `$this->x = ...; return $this`. Терминальные глаголы (grant/revoke/revokeAll/grants) остаются явными. Единый корень: context-builder создаётся ИЗ core-корня (`forUser()->inContext()`), прямой `new ContextGrantBuilder` — @internal/deprecated. Миграция expires_at — НОВАЯ (не правка применённых, канон C-16). TTL context: `grants()` фильтрует active() симметрично core (GrantBuilder.php:171). Арх-ратчет: добавить expectation toBeFinal()->toBeReadonly() на AzGuard\Grants и неймспейс context-builder. Инвариант §6: прямые предикаты (can/isSuperAdmin) НЕ превращать в fluent-цепочки.
**Code Guidance:** Не ломать union-only и существующие терминалы. Immutable-with НЕ должен молча ронять состояние при копировании — юнит-тест на «две ветки от одного билдера независимы» (иммутабельность). TTL-парность требует миграции — если context-хранилище окажется без своей таблицы грантов (context пишет в core-таблицу) → проверить по коду, миграция может быть в core; выяснить чтением ContextGrantBuilder/Models перед миграцией, не гадать. if-then: если единый корень тянет циклическую зависимость core→context (core не должен знать про context-пакет) → context-расширение реализовать через macro/registered-extension в context-SP, корень в core остаётся context-агностичным; если развязать не выходит → эскалация §10 (unclear architecture). Тесты: грамматика читается как фраза (приёмочный тест-кейс из §3), TTL context (until→не в active после срока), иммутабельность, арх-ратчет на новые неймспейсы.
**Validation:** `php vendor/bin/pest --filter='Grant|Context|Builder'` зелёный · новый arch-expect на Grants/context-builder зелёный (`php vendor/bin/pest tests/ArchTest.php`) · миграция context expires_at применяется в тест-БД (`composer test:feature` зелёный) · `composer analyse` зелёный · `composer lint:check` зелёный.
**Deliverables:** Единый immutable fluent-корень core↔context; TTL-парность + миграция; арх-ратчет расширен; shorthands @internal; зелёные targeted+arch+feature+analyse+lint.
**Completion Notes:** Session: fable/high (plan-run, D18). Единый корень построен по if-then Code Guidance (registered-extension, циклическая зависимость core→context исключена): core объявляет контракты `Contracts\ContextGrantBuilder` (@api, грамматика ветки: on/inContext/until/ttl + терминалы) и `Contracts\ContextGrantBuilderFactory` (@api, swap-шов — паттерн `Contracts\ContextGuard`); context-SP биндит фабрику; `GrantBuilder::inContext()` переносит накопленный scope (panel/ttl/until) в ветку, без биндинга бросает новый `ContextPackageNotInstalledException` (fail-closed). Оба builder'а — `final readonly` + with-сеттеры (`new self`, named args); **breaking**: `GrantBuilder::expiresAt()` → `until()` (канон §3), тест-пин переписан (IntegrationPolishTest). TTL-парность: `ContextGrantBuilder` получил `until()/ttl()`, `grant()` переведён `firstOrCreate`→`updateOrCreate` (идемпотентный re-stamp expires_at — паритет core), `grants()` фильтрует `active()`; НОВАЯ миграция `2026_01_01_000011_add_expires_at…` (nullable timestamp, config-driven имя таблицы, обратимый down); ContextRole: fillable/cast/scopeActive/isExpired + flush-хук `updated` (re-stamp инвалидирует кеш); read-путь `ContextPermissionLayer::contextPermissions` тоже фильтрует истёкшие (иначе TTL — фикция: истёкший грант продолжал бы давать право). Позиционные shorthands grant/revoke/grants — `@internal` в AzGuardManager + AzGuardManagerInterface + фасадный докблок; публичный путь docs — fluent-корень (context.md переписан на корень, попутно исправлена стухшая претензия «wildcard даёт полный доступ» — отклоняется с P1 C-13; direct-grants.md: `until()`, shorthands убраны из примеров). Арх-ратчет: `grant builders are final and readonly` на `AzGuard\Grants` + ContextGrantBuilder + Factory (ArchTest 16→17). Существующие внутренние вызовы `new ContextGrantBuilder` (команды, chained) совместимы. Тесты: +12 (UnifiedGrantGrammarTest — фраза-приёмка, перенос scope, идемпотентность, независимость веток, active-фильтры grants()/layer, fail-closed без пакета; GrantBuilderTest — иммутабельность веток, until last-wins). Item-коммит 278cdfc: 19 files, +673/−84 (`git show --stat 278cdfc`). Validation (все на 278cdfc): `php vendor/bin/pest --filter='Grant|Context|Builder'` 218 passed/463 · `php vendor/bin/pest tests/ArchTest.php` 17 passed/35 · `composer test:feature` 334 passed/910 (миграция применяется) · `composer analyse` passed 0 errors · `composer lint:check` passed. Сверх ТЗ: полный `composer test` 623 passed/1681 assertions (611→623).
**Pending Work:** RU-зеркала docs/ru/{advanced/context,basic-usage/direct-grants}.md рассинхронены с переписанными EN-страницами — сквозной свип и паритет по ТЗ P2.10. Generics `Contracts\HasRoles::roles()` (P2.2 Pending Work, кандидат «при грамматике P2.3») не потребовались корню — остаются кандидатом P2.10. Внутренние команды context-пакета продолжают direct `new ContextGrantBuilder` (@internal-легально) — при желании дог-фудить корень в CLI решить в P2.10.
**Known Deviations:** (1) material: item-коммит несёт дифф вне литерального перечня Files — packages/core/src/Contracts/{ContextGrantBuilder,ContextGrantBuilderFactory}.php + Exceptions/ContextPackageNotInstalledException.php (новый шов: предписан if-then Code Guidance «registered-extension в context-SP, корень context-агностичен» + fail-closed отказ), packages/core/src/Contracts/AzGuardManagerInterface.php (@internal shorthands — Scope (5) называет фасад/manager, interface — тот же контракт), packages/context/src/AzGuardContextServiceProvider.php (сам биндинг registered-extension), packages/context/src/ContextPermissionLayer.php (фильтр истёкших в read-пути — без него TTL-парность недейственна, истёкший грант продолжал бы конферить право). Все группы — прямое следствие предписаний самого ТЗ; гэп перечня, не scope drift (прецедент P2.1/P2.2). (2) SemVer-breaking (легально, D14/D16): `GrantBuilder::expiresAt()` → `until()`; оба builder'а стали `final readonly` (мутация/наследование исключены); прямой `new ContextGrantBuilder` помечен @internal; shorthands manager'а/фасада — @internal.
**Escalation Needed:** no

### P2.4 — Config→fluent: Filament-плагин + middleware `::using()` + единый порядок аргументов

**Status:** 🟠 Done with deviations
**Intent:** Перенести поведенческие опции из config в fluent (Filament-плагин — fluent-сеттеры + `app(static::class)`), добавить статические `::using(string|BackedEnum)`-конструкторы параметризуемым middleware, выровнять единый порядок аргументов алиасов.
**Why:** Filament-плагин config-центричен против канона v5 (B-06, Major); нет `::using()` вопреки канону spatie/Laravel (B-02, Major); асимметрия порядка аргументов middleware-DSL (A-03, Minor). RAG:✅ 2026-07-18 (findings/P0-rag-fluent-dx.md Запрос 1 — Filament fluent; Запрос 2 — spatie `::using()` + PR #52679).
**Scope Included:** (1) Filament AzGuardPlugin: fluent-сеттеры (enforce/source/abilities/keyTemplate/case) + геттеры, чтение в register(), `make()`→`app(static::class)`, config=fallback; (2) middleware ::using(string|BackedEnum ...) на grant/panel/panel_check/check, alias-DSL параллельно; (3) единый порядок аргументов `что,где` (permission,panel) для всех алиасов — выровнять PanelCheckAccess под CheckDirectGrant.
**Scope Excluded:** Grant-грамматика (P2.3); полиморфизм трейтов (P1-W1 B-04); удаление config-ключей wiring (классы/таблицы — канон, остаются).
**Inputs:** research/03-p2-canon.md §4 · findings/P0-rag-fluent-dx.md Запрос 1/2 · findings/P0-axis-b-fluent.md §B-02/§B-06/§C-B8 · findings/P0-axis-a-integration.md §A-03.
**Files:** packages/filament/src/AzGuardPlugin.php · packages/filament/config/az-guard-filament.php · packages/core/src/Http/Middleware/{CheckDirectGrant,PanelCheckAccess,SetCurrentPanel,CheckAccess}.php · packages/core/src/AzGuardServiceProvider.php (alias-регистрация) · tests/{Feature,Unit}/** (middleware/plugin) · docs/basic-usage/{filament,http-access}.md.
**Required Reads:** research/03-p2-canon.md §4 · findings/P0-rag-fluent-dx.md Запрос 1, Запрос 2 · findings/P0-axis-b-fluent.md §C-B8 · packages/filament/src/AzGuardPlugin.php · packages/core/src/Http/Middleware/CheckDirectGrant.php · packages/core/src/Http/Middleware/PanelCheckAccess.php.
**Implementation Rules:** Filament fluent-сеттеры `return $this` (плагин мутабелен by convention Filament — НЕ immutable, в отличие от grant-builders: это разные каноны, не путать); `make()` = `app(static::class)`; каждая fluent-опция дублируется в config как fallback (не удалять config, config→fallback). Middleware `::using()` принимает `string|BackedEnum ...$args`, unwrap enum на границе (->value), alias-DSL остаётся. Порядок аргументов: зафиксировать `что,где` каноном, выровнять PanelCheckAccess (сейчас `panel,permission` → `permission,panel`) — это breaking для роутов, легально пре-1.0 (D14). RAG:✅-каноны — точно по первоисточникам (findings/P0-rag-fluent-dx.md), не по памяти.
**Code Guidance:** Не ломать существующий alias-DSL путь (оба в docs — параллельны). `app(static::class)` для make() — проверить, что swap плагина в рантайме реально работает (тест подмены). Выравнивание порядка аргументов PanelCheckAccess — обновить ВСЕ вызовы в tests/docs (breaking). if-then: если fluent-сеттер плагина требует опции, которой нет в текущем config (новое поведение) → это расширение, не редизайн — оставить в scope только перенос СУЩЕСТВУЮЩИХ опций (enforce/source/abilities/keyTemplate/case), новое поведение = эскалация/YAGNI. Тесты: plugin fluent+swap, middleware ::using(enum), симметрия порядка аргументов.
**Validation:** `php vendor/bin/pest --filter='Middleware|Plugin|Filament'` зелёный · тест swap плагина через app() зелёный · `composer analyse` зелёный · `composer lint:check` зелёный · docs-примеры middleware обновлены (свип паритета — P2.10).
**Deliverables:** Filament-плагин fluent + container-make; middleware ::using(); единый порядок аргументов; зелёные targeted+analyse+lint.
**Completion Notes:** Item-коммит `250e53a` (15 файлов, +517/−14). AzGuardPlugin
(`packages/filament/src/AzGuardPlugin.php`): fluent-сеттеры `enforce()/source()/
abilities()/keyTemplate()/case()` + геттеры `isEnforcing()/getSource()/
getAbilities()/getKeyTemplate()/getCase()` (nullable-поле → fluent ?? config,
config остаётся fallback); `make()` → `app(self::class)` (pint нормализовал
`static::class`→`self::class` для `final class` — эквивалентно, класс не
наследуется); `register(Panel)` пишет эффективные значения обратно в
`config(['az-guard-filament.*' => …])`, чтобы независимые контейнер-синглтоны
(`PermissionSchema`/`FilamentDiscovery`/`FilamentPermissionCatalogBuilder`/
`ResourceGate`/`PageWidgetAccessEvaluator` в `AzGuardFilamentServiceProvider`)
видели их; `boot()` читает `$this->isEnforcing()/getSource()` напрямую (без
config-круга). Middleware `::using(string|BackedEnum ...)` на все 4:
`CheckDirectGrant`/`PanelCheckAccess`/`SetCurrentPanel`/`CheckAccess` (canon
`static::class.':'.implode(',', …)`, unwrap через `PermissionKey::normalize()`/
`PanelResolver::normalizeId()`); `CheckAccess::using()` — без аргументов
(attribute-driven, добавлен для DX-симметрии). `PanelCheckAccess::handle()`
порядок аргументов breaking: `panelId,permission` → `permission,panelId`
(`azguard.panel_check:{permission},{panel}`); alias-DSL путь не тронут, оба в
docs параллельно. Validation на `250e53a`: `pest --filter='Middleware|Plugin|
Filament'` 117 passed/194 assertions · `composer analyse` 0 errors ·
`composer lint:check` passed · полный `composer test` 648 passed/1720
assertions (было 623 на P2.3 — +25 новых тестов). Docs EN обновлены:
`docs/basic-usage/filament.md` (секция Fluent configuration) ·
`docs/basic-usage/http-access.md` (секция Static constructors, канон
`::using()` "что,где" + breaking-нота по `panel_check`); RU-зеркала не
трогались (P2.10, D28 handoff Open risks).
**Pending Work:** RU-зеркала `docs/ru/basic-usage/{filament,http-access}.md` —
паритет с новыми EN-секциями закрывает P2.10. `docs/basic-usage/
direct-grants.md` не получил `::using()`-пример (вне Files item'а — уже
документирует `azguard.grant:` alias-DSL, свежий канон-пример — кандидат
P2.10 doc-sweep, не breaking).
**Known Deviations:** (process, статус 🟠 material — дифф вне Files item'а)
дифф несёт 2 файла сверх перечня Files: (1) `packages/filament/src/
AzGuardFilamentServiceProvider.php` — `boot()` перестал резолвить
`ResourceGate` эagerly в локальную переменную (захват в замыкание
`Gate::before`), резолвит теперь ЛЕНИВО внутри самого замыкания; это
НЕОБХОДИМО для корректности самой фичи item'а (Code Guidance: «make() для
swap плагина в рантайме реально работает» — эффективные fluent-опции плагина
пишутся в config из `AzGuardPlugin::register(Panel)`, которое Filament
резолвит через отложенный `PanelRegistry`-callback; порядок относительно
`boot()` этого провайдера конструктивно не гарантирован vendor-кодом
(`Filament::registerPanel()` регистрирует `resolving()`-callback, не вызывает
сразу) — без ленивого резолва eager-захваченный `$gate` мог бы навсегда
остаться построенным на дефолтном config, игнорируя fluent-опции); (2)
`tests/Pest.php` — 2 новых Feature-файла зарегистрированы в `uses(TestCase::
class, RefreshDatabase::class)->in(...)` (без этого `config()`/`$this->get()`
недоступны в новых файлах — механика Pest-конфигурации проекта, не
опциональна). Оба — предписаны самим Code Guidance/тест-инфраструктурой
item'а, не «улучшение заодно».
**Escalation Needed:** no

### P2.5 — Локус фасада: cut-line target-спека (вход P3)

**Status:** 🟢 Done
**Intent:** Произвести target-спеку сужения фасада к оркестровому входу (какие @method удалить / @internal / оставить) на базе готовой классификации C-B4 — БЕЗ исполнения выреза (исполнение — P3).
**Why:** Фасад шире локуса (B-05, Minor): 2 мёртвых резолвера, 2 предиката-дубля трейта, 8/17 методов вне docs — против канона spatie (трейт/Gate — центр). Backlog: классификация готова, исполнение cut-line — P3. RAG:— (repo-grounded: findings/P0-axis-b-fluent.md §C-B4/§B-05); канон локуса RAG:✅ (findings/P0-rag-fluent-dx.md Запрос 2).
**Scope Included:** Написать `root/contracts/facade-cutline.md` — таблица 17 @method с вердиктом (удалить/`@internal`/оставить) по research/03-p2-canon.md §5 + обоснование каждого + отметка «согласовано с P2.3» (shorthands→@internal). Это ДИЗАЙН-артефакт (вход P3), не правка кода фасада.
**Scope Excluded:** Правка packages/core/src/Facades/AzGuard.php (исполнение — P3); @internal-докблоки shorthands grant/revoke/grants (их ставит P2.3 как часть грамматики).
**Inputs:** research/03-p2-canon.md §5 · findings/P0-axis-b-fluent.md §C-B4 (классификация 17 @method) · findings/P0-rag-fluent-dx.md Запрос 2 (локус).
**Files:** plans/2026.07.18-AZGUARD-STABLE/root/contracts/facade-cutline.md (создать).
**Required Reads:** research/03-p2-canon.md §5 · findings/P0-axis-b-fluent.md §C-B4.
**Implementation Rules:** Чистый дизайн-item (Exec=manual, design-класс). Спека — конечная таблица с колонками `@method | вердикт | обоснование | SemVer-эффект`. tryPermission/panelIdForPermission → удалить (0 потребителей); grant/revoke/grants → @internal (согласовано P2.3); isSuperAdmin → @internal (локус трейт); hasContextGuard → оставить (полезен без user). Не решать за P3 порядок/механику выреза — только целевое состояние.
**Code Guidance:** Не править код фасада (это отдельная SemVer-операция P3 под заморозку). Спека обязана быть самодостаточной для P3-исполнителя (холодный старт). if-then: если при написании спеки обнаружится @method без единого потребителя, не попавший в классификацию C-B4 → добавить строку с вердиктом «удалить», отметить расхождение с C-B4.
**Validation:** `test -f plans/2026.07.18-AZGUARD-STABLE/root/contracts/facade-cutline.md` · спека покрывает все 17 @method (17 строк-вердиктов) · согласованность с P2.3 (shorthands @internal) явно отмечена. Код не менялся → тестов нет.
**Deliverables:** root/contracts/facade-cutline.md — target-спека cut-line для P3.
**Completion Notes:** Session: fable/high (plan-run, D18 — соответствует Routing D28, недобора/перебора нет). Спека `root/contracts/facade-cutline.md` создана (122 строки, item-коммит 2a395ef, 1 file +122 — `git show --stat 2a395ef`): §1 — таблица ровно 17 вердиктов C-B4 (`grep -c '^| [0-9]'` == 17 на 2a395ef) с колонками `@method | вердикт | обоснование | SemVer-эффект`; §2 — пост-recon поверхность P2.6 (fake() + assertGranted/assertDenied/assertChecked → оставить @api, для полноты заморозки P3.2); §3 — расхождения с C-B4 по факту кода; §4 — target state докблока (итог: 14 @api @method + fake(); @internal-секция grant/revoke/grants+isSuperAdmin; удаляются строки tryPermission/panelIdForPermission); §5 — заметки P3-исполнителю; §6 — команды воспроизводимости счётчиков. Согласованность с P2.3 (shorthands @internal) отмечена явно (`grep -c 'Согласованность с P2.3'` == 1 на 2a395ef). Сверка вердиктов с фактом кода (предписана handoff Open risks) опровергла premise C-B4 «0 внутренних потребителей» мёртвых резолверов: `tryPermission` ← `Permissions/PermissionName.php:31` (шов всех grant-путей; вошёл 3e9adb1 ДО аудита, аудит мерил только фасадную форму), `panelIdForPermission` ← `Concerns/HasScopedRoles.php:324` (P1-W1 ed64c93, B-04, ПОСЛЕ аудита) → вердикт скорректирован: удалить только @method-СТРОКИ фасада, методы interface/manager → @internal (полное удаление сломало бы оба шва) — оформлено D29 (уточнение формулы D19). Validation на 2a395ef: `test -f …/facade-cutline.md` ✓ · 17 строк-вердиктов ✓ · согласованность P2.3 ✓; кода нет → тестов нет; сверх ТЗ: `composer lint:check` passed · `composer analyse` 0 errors (страховка гейта Execution Rules, код не менялся).
**Pending Work:** `docs/recipes/temp-access-via-grant.md:33` всё ещё показывает позиционный `AzGuard::revoke` (рассинхрон с @internal P2.3) — дом фикса P2.10 doc-sweep; регенерация bundled boost-скилла (`SKILL.md:102` упоминает `AzGuard::isSuperAdmin`) после cut-line — кандидат P3.1/P2.10 (оба названы в спеке §5).
**Known Deviations:** Вердикт #7/#8 отличается от буквы Implementation Rules («tryPermission/panelIdForPermission → удалить (0 потребителей)»): premise «0 потребителей» опровергнут фактом кода (см. Completion Notes), сверка с кодом предписана handoff Open risks, item — design-класс (fable, D28) и является органом вердикта; разрешение оформлено D29, Scope («таблица 17 @method с вердиктом + обоснование») выполнен полностью. Диффа вне Files нет.
**Escalation Needed:** no

### P2.6 — Testing DX: `AzGuard::fake()` (Recorder + ассерции)

**Status:** 🟢 Done
**Intent:** Реализовать `AzGuard::fake()` — фасадный fake менеджера (Recorder-паттерн) с high-level ассерциями `assertGranted/assertDenied/assertChecked` (+ closure-варианты), как канон spatie fake-фасадов.
**Why:** Нет `AzGuard::fake()` (A-04, Major) — потребитель собирает фейки руками (FakeGrantSource — 3 вызова), ассерций нет; Testing DX — акцент брифа (интеграционная поверхность). Развилка разрешена — строим (D14). RAG:✅ 2026-07-18 (findings/P0-rag-fluent-dx.md Запрос 3 — Pdf::fake + assert*).
**Scope Included:** (1) `AzGuard::fake()` — swap менеджера на записывающий double через контейнер (как Event::fake); (2) Recorder фиксирует grant/revoke/check-вызовы; (3) ассерции assertGranted($user,$key)/assertDenied($user,$key)/assertChecked($key) + closure-предикат-вариант каждой; (4) тесты на сам fake + пример в docs testing.
**Scope Excluded:** Удаление FakeAzGuardUser/FakeGrantSource (остаются, fake() дополняет); «get log»-метод (канон — только ассерции); интеграция в главу Testing docs (текст — P1-W2 A-05 переносит testing-kit; здесь — только пример fake()).
**Inputs:** research/03-p2-canon.md §6 · findings/P0-rag-fluent-dx.md Запрос 3 · findings/P0-axis-a-integration.md §A-04 · packages/core/src/Testing/** (существующие фейки).
**Files:** packages/core/src/Facades/AzGuard.php (fake()) · packages/core/src/Testing/** (Recorder/fake-double + ассерции) · packages/core/src/AzGuardManager.php (swap-поверхность) · tests/** (fake-тесты) · docs/advanced/testing.md (пример fake()).
**Required Reads:** research/03-p2-canon.md §6 · findings/P0-rag-fluent-dx.md Запрос 3 · packages/core/src/Facades/AzGuard.php · packages/core/src/Testing/FakeGrantSource.php · packages/core/src/AzGuardManager.php (что записывать).
**Implementation Rules:** Recorder — записывающий double реального AzGuardManagerInterface (swap через `app()->instance()`/фасадный `swap()`), НЕ мок с ручной сборкой. Ассерции бросают PHPUnit-исключение при провале (как Laravel fake). Closure-вариант: `assertGranted(fn (Recorded $r) => ...)`. Не ломать union-only и реальный резолвер, когда fake НЕ активен. Канон точно по Pdf::fake (findings/P0-rag-fluent-dx.md Запрос 3) — простая форма ИЛИ closure, «get log»-метода нет.
**Code Guidance:** fake() не должен протекать между тестами — swap живёт в рамках теста (Laravel сбрасывает контейнер). @api-докблок на fake() и ассерциях. if-then: если запись всех вызовов менеджера раздувает интерфейс double → записывать только grant/revoke/check-поверхность (то, что нужно ассерциям), остальное проксировать/no-op. Тесты: assertGranted проходит после grant, assertDenied после revoke, assertChecked после can(), closure-вариант, изоляция между тестами.
**Validation:** `php vendor/bin/pest --filter='Fake|AzGuardFake'` зелёный · `composer analyse` зелёный (@api-типизация fake) · `composer lint:check` зелёный.
**Deliverables:** AzGuard::fake() + Recorder + 3 ассерции с closure-вариантами + тесты + пример docs; зелёные targeted+analyse+lint.
**Completion Notes:** Item-commit `c07f157`. `AzGuardFake` (`packages/core/src/Testing/AzGuardFake.php`) — `final class` implementing `AzGuardManagerInterface` + Laravel `Fake` marker, конструктор принимает реальный (уже забутстрапленный) менеджер и делегирует ему КАЖДЫЙ метод интерфейса без исключения (грант/ревок/панели/абилки идут через настоящий резолвер — fake() только наблюдает, не подменяет поведение). Запись реализована НЕ переопределением `forUser()` (`GrantBuilder` — `final readonly`, сигнатура `forUser(): GrantBuilder` не даёт вернуть другой тип), а подпиской на уже существующие доменные события `GrantGiven`/`GrantRevoked` (`Event::listen`, диспетчатся `GrantBuilder::grant()/revoke()` синхронно вне зависимости от точки входа — и fluent-корень, и позиционный shorthand) + хук `Gate::after()` (стандартный Laravel API, фиксирует КАЖДЫЙ Gate/`can()`-чек независимо от того, кто его разрешил — azguard `Gate::before` или сторонняя policy). Оба механизма не требуют правок Guard/Authorizer.php, Registry/Resolver/EffectivePermissionResolver.php, Concerns/HasPermissions.php, Grants/GrantBuilder.php — ни один из них не входит в Files item'а. `assertGranted`/`assertDenied`/`assertChecked` — простая форма (User+key[+panelId, дефолт через `PanelResolver::resolveDefault`]) ИЛИ closure-предикат над `Recorded` (`user`/`key`/`panelId`/`result`); внутри — `PHPUnit::assertTrue()` (не голый `fail()`) — считается PHPUnit-ассершеном, иначе PHPUnit риски "no assertions performed" (поймано локально: 6 risky тестов до правки, 0 после — `phpunit.xml failOnRisky=true`). `AzGuard::fake()` — `app(AzGuardManagerInterface::class)` (не `static::getFacadeRoot()`, чтобы Larastan типизировал аргумент конструктора) + `static::swap()`. `AzGuardManager.php` НЕ тронут — swap-поверхность реализована целиком через Facade::swap()+делегирующий double, добавление in-class метода не потребовалось (Files допускал правку, не требовал её). Validation на `c07f157`: `pest --filter='Fake|AzGuardFake'` 15 passed/42 assertions · `composer analyse` 0 errors · `composer lint:check` passed · полный `composer test` 657 passed/1755 assertions (на `c07f157`; было 648 на P2.4, +9 новых тестов). Docs: `docs/advanced/testing.md` — новая секция «`AzGuard::fake()` — recording grants and checks» с обеими формами ассерций; RU-зеркало не трогалось (P2.10 doc-parity).
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P2.7 — Словарь терминов: глоссарий + docs-маршрутизация (guard=бренд, context↔scope)

**Status:** ⬜ Not started
**Intent:** Устранить онбординг-налог «4 термина при 3 сущностях»: закрепить глоссарий (guard=бренд, изоляция=panel), переформулировать multiple-guards.md через панели, добавить маршрутизирующий раздел «context или scope?».
**Why:** «guard» без сущности + doc-декларация привязки panel↔auth-guard без носителя (A-07, Minor); context vs scope — два параллельных механизма без маршрутизации (A-08, Minor). RAG:— (repo-grounded: findings/P0-axis-a-integration.md §A-07/§A-08/§C-A10, Panel.php: 0 вхождений guard).
**Scope Included:** (1) `root/glossary.md` — 4 термина → сущность → видимость (таблица C-A10) + вердикты (guard=бренд; context=runtime; scope=persist; panel=изоляция); (2) переформулировать docs/basic-usage/multiple-guards.md через панели (убрать «panel is bound to guards» без носителя); (3) маршрутизирующий раздел «context или scope?» в docs (advanced/context.md или новый) — когда что; RU-зеркала.
**Scope Excluded:** Введение привязки panel↔auth-guard в код (её нет и не нужна — A-07); слияние механизмов context/scope (разные by design — §9); переименование неймспейса Guard/ (бренд остаётся).
**Inputs:** research/03-p2-canon.md §9 · findings/P0-axis-a-integration.md §C-A10 (таблица словаря) / §A-07 / §A-08.
**Files:** plans/2026.07.18-AZGUARD-STABLE/root/glossary.md (создать) · docs/basic-usage/multiple-guards.md · docs/advanced/context.md · docs/advanced/entity-scopes.md · docs/ru/** (зеркала).
**Required Reads:** research/03-p2-canon.md §9 · findings/P0-axis-a-integration.md §C-A10 (таблица «термин→сущность→видимость»).
**Implementation Rules:** Глоссарий — доменный язык (скилл `domain-modeling`: CONTEXT/glossary — только термины). Никакого кода: чисто docs/глоссарий. multiple-guards.md: убрать ложную декларацию привязки, объяснить guard=бренд + Laravel auth guard (коллизия названа честно). Routing-раздел context↔scope — таблица выбора, не слияние. RU-паритет держать сразу (иначе docs-parity-gate P2.10 покраснеет).
**Code Guidance:** Не «изобретать» привязку panel↔guard ради устранения расхождения — носителя нет, это doc-фикс. Не сливать context и scope — они разные механизмы (runtime vs persisted); routing объясняет выбор. if-then: если переформулировка multiple-guards.md обнаружит реальный код-шов panel↔guard (аудит мог упустить) → grep подтвердить; если шов есть — задокументировать его, не выдумывать.
**Validation:** `test -f plans/2026.07.18-AZGUARD-STABLE/root/glossary.md` · `bash bin/docs-parity-gate.sh` зелёный (EN↔RU паритет затронутых страниц) · `grep -c 'bound to.*guard' docs/basic-usage/multiple-guards.md` == 0 (ложная декларация убрана).
**Deliverables:** root/glossary.md; multiple-guards.md через панели; routing-раздел context↔scope; RU-зеркала; docs-parity зелёный.
**Completion Notes:** —
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P2.8 — Headless-порог: minimal-setup quick-start + doctor-hint (doc-only)

**Status:** ⬜ Not started
**Intent:** Снизить headless/embedded-порог (первая проверка требует PanelProvider+enum+каталог+sync) через doc-only minimal-setup quick-start + подсказку guard:doctor при 0 панелей — БЕЗ ослабления fail-closed рантайма.
**Why:** Headless-порог (A-06, Major): embedded-потребитель (мост/библиотека) не может пройти быстрый путь; закрытие «только доками» ранее покрыло лишь тесты. Развилка разрешена — doc-only minimal-setup (D14; рантайм panel-less НЕ строим — fail-closed сохраняется, YAGNI). RAG:— (repo-grounded: findings/P0-axis-a-integration.md §A-06/§C-A9).
**Scope Included:** (1) `docs/introduction/headless-quick-start.md` (+RU): минимальный путь install→`implements AzGuardUser`→один минимальный PanelProvider→`$user->can()`/abilitiesFor() без Filament; (2) guard:doctor — onboarding-hint при 0 панелей (различает пустой сетап от ошибки); тест на hint.
**Scope Excluded:** Рантайм panel-less/lenient путь (НЕ строим — D14, fail-closed); синхронизация выдуманного вывода doctor (сделал P1-W2 A-01); TS-export (снят A-01).
**Inputs:** research/03-p2-canon.md §7 · findings/P0-axis-a-integration.md §A-06/§C-A9/§A-01.
**Files:** docs/introduction/headless-quick-start.md (создать) · docs/ru/introduction/headless-quick-start.md (создать) · packages/core/src/Commands/DoctorCommand.php + packages/core/src/Guard/AzGuardDiagnostics.php (hint при 0 панелей) · tests/** (doctor-hint) · docs/introduction/index/навигация (ссылка на новый quick-start).
**Required Reads:** research/03-p2-canon.md §7 · findings/P0-axis-a-integration.md §A-06 · findings/P0-axis-a-integration.md §A-01 (что P1 уже сделал с doctor) · packages/core/src/Guard/AzGuardDiagnostics.php.
**Implementation Rules:** Fail-closed НЕ ослаблять: прод-путь по-прежнему требует панель/каталог; headless quick-start показывает МИНИМАЛЬНЫЙ, но полный сетап (один PanelProvider), не «без панели». Doctor-hint — информативный (не ошибка) при 0 панелей: «no panels registered — see headless-quick-start». Купля с A-01 (P1): P1 убрал выдуманный вывод; здесь добавляем реальный hint — не конфликтует (проверить, что A-01 закрыт до старта). RU-паритет сразу.
**Code Guidance:** Не вводить lenient/auto-panel рантайм (D14 явно исключает — риск fail-closed). Hint — одна ветка в диагностике, не рефактор doctor. if-then: если A-01 (P1) ещё не закрыл выдуманный вывод doctor (P1 не исполнена на момент exec P2) → это нарушение phase-first (P1 закрывается до P2); эскалация §10. Тест: doctor на 0 панелей печатает hint, на ≥1 — нет.
**Validation:** `test -f docs/introduction/headless-quick-start.md` && `test -f docs/ru/introduction/headless-quick-start.md` · `php vendor/bin/pest --filter='Doctor'` зелёный (hint-ветка) · `bash bin/docs-parity-gate.sh` зелёный · `composer lint:check` зелёный.
**Deliverables:** headless-quick-start EN+RU; guard:doctor 0-панелей hint + тест; docs-parity зелёный.
**Completion Notes:** —
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

### P2.9 — Пере-оценка breaking: wildcard-флип на Hierarchical + verify F4/F40/F51

**Status:** 🟠 Done with deviations
**Intent:** Флипнуть дефолтную wildcard-грамматику на `HierarchicalPermissionMatcher` (F22, пре-1.0), legacy — opt-out на один цикл; выровнять PermissionSet вне контейнера на Hierarchical; верифицировать и зафиксировать, что F4/F40/F51 уже сделаны.
**Why:** Дефолтный matcher пересекает сегменты (`app.*`→`app.a.b`) — F22-остаток (C-06, Major); PermissionSet вне контейнера всегда legacy-matcher — расхождение результатов (C-07, Minor). Развилка разрешена — флип сейчас (D18; вердикт C-C4 + гейт D9 + пре-1.0). RAG:— (repo-grounded: findings/P0-axis-c-correctness.md §C-06/§C-07/§C-C4, packages/core/src/Registry/Matching/).
**Scope Included:** (1) дефолт matcher = Hierarchical (флаг `features.wildcard_permission`: инвертировать смысл — дефолт Hierarchical, legacy=opt-out на цикл); (2) PermissionSet::matcher() вне контейнера дефолтит на Hierarchical (C-07), расхождение задокументировать на @api; (3) verify F4/F40/F51 сделаны (C-C4) — записать в Completion Notes с якорями, кода не требуют; (4) тесты флипа + миграционная заметка docs (breaking).
**Scope Excluded:** C-15 winningSource (P1-W2, заполнение — не удаление); удаление legacy-matcher (остаётся opt-out на цикл); wildcard-валидация context-грантов (сделана P1-W1 C-13).
**Inputs:** research/03-p2-canon.md §8 · findings/P0-axis-c-correctness.md §C-06/§C-07/§C-C4 · research/01-fluent-api-priors.md §C (приор 12).
**Files:** packages/core/config/az-guard.php (features.wildcard_permission) · packages/core/src/Registry/Matching/{WildcardPermissionMatcher,HierarchicalPermissionMatcher}.php · packages/core/src/Registry/Values/PermissionSet.php · packages/core/src/AzGuardServiceProvider.php (биндинг matcher по флагу) · tests/** (wildcard/PermissionSet) · docs/basic-usage/super-admin.md · docs/advanced/** (wildcard-семантика) · docs/introduction/upgrading.md (breaking-заметка).
**Required Reads:** research/03-p2-canon.md §8 · findings/P0-axis-c-correctness.md §C-06/§C-07/§C-C4 · packages/core/src/Registry/Matching/HierarchicalPermissionMatcher.php · packages/core/src/Registry/Values/PermissionSet.php:129.
**Implementation Rules:** Флип — смена ДЕФОЛТА биндинга, legacy остаётся классом за opt-out флагом (deprecate-заметка на один цикл). PermissionSet вне контейнера: дефолт Hierarchical (согласован с новым дефолтом приложения), расхождение с явно переданным matcher — @api-докблок. F4/F40/F51: только ВЕРИФИЦИРОВАТЬ по якорям C-C4 (AbilitiesDto.php:45 / PermissionCatalog.php:61 / guard:-префикс 22 команд) и записать — НЕ переделывать. Breaking-заметка в upgrading.md (дефолт грамматики изменился). RU-паритет затронутых docs.
**Code Guidance:** Флип семантически ломает существующие `*`-паттерны потребителя — явная breaking-заметка обязательна (upgrading.md). Не удалять legacy-matcher (opt-out нужен на цикл). Тест: `app.*` при Hierarchical НЕ покрывает `app.a.b`, `app.**` покрывает; PermissionSet standalone матчит как приложение. if-then: если флип роняет существующие тесты, завязанные на legacy-семантику → это ожидаемо (breaking), обновить тесты под Hierarchical, НЕ возвращать legacy-дефолт; если тест проверяет legacy-путь осознанно — перевести на opt-out флаг.
**Validation:** `php vendor/bin/pest --filter='Wildcard|Hierarchical|PermissionSet|Matcher'` зелёный · тест «дефолт=Hierarchical» зелёный · `composer test` зелёный (полный — флип не ломает прочее) · `composer analyse` зелёный · `bash bin/docs-parity-gate.sh` зелёный.
**Deliverables:** дефолт=Hierarchical + legacy opt-out; PermissionSet выровнен; F4/F40/F51 верифицированы (Completion Notes); breaking-заметка; зелёные targeted+full+analyse+parity.
**Completion Notes:** Session: fable/high (plan-run, D18 — соответствует Routing D28). Item-коммит 73072fd: 18 files, +313/−109 (`git show --stat 73072fd`). Смысл «инвертировать смысл флага» (Scope 1) исполнен по единодушной букве D18/канона §8: `features.wildcard_permission` (дефолт false) больше НЕ гейтит учёт паттернов — паттерны учитываются всегда; true = вернуть legacy-грамматику 0.2 на один deprecate-цикл. Реализация: (1) `config('az-guard.matcher')` дефолт → `HierarchicalPermissionMatcher` (config + fallback `Config::matcherClass()`); флаг переопределяет ключ (логика в `matcherClass()` — `AzGuardServiceProvider:154` не потребовал правки, биндинг уже резолвит через Config); `Config::wildcardEnabled()` → `legacyWildcardEnabled()` (смысл инвертирован — тихая смена значения под старым именем была бы ловушкой). (2) `PermissionSet::matcher()` standalone-fallback → Hierarchical (C-07), расхождение задокументировано в @api-докблоке. (3) Catalog-фильтр `EffectivePermissionResolver::filterAgainstCatalog` унифицирован (две ветки → одна: паттерн живёт, если покрывает ≥1 ключ каталога в грамматике биндинга); голый `*` отбрасывается БЕЗУСЛОВНО в обеих грамматиках — закрыт R7 из P1.4 (голый `*` из кастомной MergeStrategy при включённом wildcard проходил фильтр; предписано handoff Open risks «закрыть там же»). (4) Verify-record C-C4 (кода не требуют, якоря на 73072fd): F4 сделан — `AbilitiesDto.php:43` `array_filter(get_object_vars, is_bool(...))` (якорь C-C4 «:45» дрейфнул после P2.1); F40 сделан — `PermissionCatalog.php:55` panels() lazy-контракт + `:63` flush(); F51 сделан — 20 команд `guard:` + 5 `make:guard-*` = 25, `grep -rhn "signature = 'azguard:" packages/*/src/Commands/*.php | wc -l` == 0, `grep -rn '\$aliases' …/Commands/*.php | wc -l` == 0 (счёт C-C4 «22» вырос: +context-команды после аудита). Тесты: re-baseline по правилу «legacy-намерение vs случайность» — пин старой грамматики `PermissionSetTest` («do not switch … without a deliberate breaking change» — это ТОТ САМЫЙ deliberate change, D18) переписан на Hierarchical+`**`; `WildcardCatalogFilterTest` переписан (5 кейсов: дефолт-паттерн, не-пересечение точек, `**`-рекурсия, stale-паттерн дропается, legacy-флип); `PermissionMatcherTest` +3 (дефолт-биндинг Hierarchical, legacy-биндинг по флагу, standalone-fallback C-07); `EffectivePermissionResolverTest` +R7-тест legacy-режима, re-baseline «паттерн покрывает dynamic-definition → живёт». Docs: upgrading.md EN+RU — breaking-глава 0.3.0 (грамматика, оба направления поведенческого сдвига, opt-out, standalone, R7-hardening); extending/configuration/super-admin-wildcard EN+RU обновлены под новый дефолт (RU-паритет затронутых страниц по ТЗ). Validation (все на 73072fd): `php vendor/bin/pest --filter='Wildcard|Hierarchical|PermissionSet|Matcher'` 55 passed/110 · тест «binds the hierarchical matcher by default» зелёный · полный `composer test` 664 passed/1768 (было 657 на P2.6, +7) · `composer analyse` 0 errors · `bash bin/docs-parity-gate.sh` OK (оба чека) · `composer lint:check` passed.
**Pending Work:** Удаление legacy `WildcardPermissionMatcher` + флага `features.wildcard_permission` — следующий deprecate-цикл (post-0.3.0, вне плана; кандидат в known-limitations/semver-policy P3.3). `docs/basic-usage/super-admin.md` правки не потребовал (только bare-wildcard семантика, не изменилась). RU-страницы upgrading.md/super-admin-wildcard.md имели предсуществующий контент-дрейф с EN — добавлены только зеркала новых секций, полный ресинк — P2.10 по ТЗ.
**Known Deviations:** (1) material: item-коммит несёт дифф вне литерального перечня Files — `packages/core/src/Configuration/Config.php` (дефолт `matcherClass()` + инверсия смысла флага живут в Config-акцессоре; ServiceProvider из Files правки не потребовал — биндинг уже идёт через `Config::matcherClass()`), `packages/core/src/Registry/Resolver/EffectivePermissionResolver.php` (гейт `wildcardEnabled()` строки 131 нёс СТАРЫЙ смысл флага — без унификации фильтра инверсия смысла невыполнима; там же R7, предписанный handoff Open risks), `docs/recipes/super-admin-wildcard.md` + 4 RU-зеркала (docs/ru/**: RU-паритет затронутых страниц предписан Implementation Rules, recipes-страница — единственный дом «segment wildcards» в docs). Все группы — прямое следствие предписаний ТЗ/handoff; гэп перечня, не scope drift (прецедент P2.1–P2.4). (2) SemVer-breaking (легально, D14/D18, зафиксировано в upgrading.md): дефолт грамматики флипнут; смысл `features.wildcard_permission` инвертирован; `Config::wildcardEnabled()` → `legacyWildcardEnabled()`; голый `*` из layer больше не проходит фильтр даже в legacy-режиме (R7 — security-fix). (3) process: R7 формально отсутствовал в Scope Included (ТЗ писалось до P1.4-ревью), исполнен по прямому предписанию handoff Open risks «закрыть там же (D18)» — маршрут R7→P2 зафиксирован в P1.4 Completion Notes.
**Escalation Needed:** no

### P2.10 — Консолидация: docs EN/RU паритет + arch-тесты канона

**Status:** ⬜ Not started
**Intent:** Финальная консолидация фазы: сквозной свип docs EN/RU под новый API (грамматика/config/fake/wildcard/структура), обновление всех примеров, паритет EN↔RU, консолидация arch-тестов канона P2.
**Why:** Каждый предыдущий item трогал docs точечно; редизайн публичной поверхности требует сквозной сверки примеров и паритета до заморозки P3. Arch-тесты канона (immutable builders, namespace-matrix, contracts-interfaces) — canon-the-fleet-follows, консолидируются одним проходом. RAG:— (repo-grounded: findings/P0-axis-d-structure.md §C-D12 docs-parity-gate; tests/ArchTest.php).
**Scope Included:** (1) сквозной свип docs-примеров под новый API (единый fluent-корень, ::using(), fake(), Filament fluent, wildcard Hierarchical, новые неймспейсы в FQCN-примерах); (2) EN↔RU паритет всех затронутых страниц (44↔44); (3) консолидация arch-тестов канона P2 (namespace-matrix после переездов, immutable Grants/context, contracts-два-дома) — сверить, что все зелёны и покрывают новый канон; (4) финальный `composer check`.
**Scope Excluded:** Новый контент сверх редизайна (только приведение к новому API); cut-line фасада (P3); mutation-ratchet/paratest (P4).
**Inputs:** research/03-p2-canon.md (весь) · Completion Notes P2.1–P2.9 · findings/P0-axis-d-structure.md §C-D12.
**Files:** docs/** (EN) · docs/ru/** (RU) · tests/ArchTest.php · bin/docs-parity-gate.sh (только запуск, не правка) · root/architecture.md (финальная сверка ADR).
**Required Reads:** research/03-p2-canon.md §10 · findings/P0-axis-d-structure.md §C-D12 · handoff.md (что закрыли P2.1–P2.9).
**Implementation Rules:** Только приведение docs к УЖЕ РЕАЛИЗОВАННОМУ API (не проектировать заново). EN↔RU паритет — жёсткий (docs-parity-gate green; 05_AI и README исключены by design — docs-parity-gate.sh:5-7). Arch-тесты: не ослаблять, только консолидировать/сверить покрытие нового канона. Финал — полный `composer check` зелёный (lint/analyse/refactor/type-coverage/test/coverage/mutation). if-then: если `composer check` красный из-за mutation-ratchet (не docs/arch) → mutation — предмет P4, зафиксировать в Pending Work, не чинить здесь (но analyse/test/parity/arch ОБЯЗАНЫ быть зелёными).
**Code Guidance:** Свип обязан быть полным — grep старых паттернов (позиционный grant, старые FQCN Support/, legacy wildcard-примеры, config-центричный Filament) по docs == пусто. Не оставить ни одной страницы с рассинхроном EN/RU. Финальный `composer check` — граница «фаза готова к P3». if-then: если docs-паритет краснеет из-за страницы, которую ни один item P2 не трогал (предсуществующий дрейф) → зафиксировать отдельно (не наводить косметику вне scope), назвать в Completion Notes.
**Validation:** `bash bin/docs-parity-gate.sh` зелёный · `grep -rE 'AzGuard\\Support\\|->grant\(\$user,' docs --include='*.md'` == пусто (старый API вычищен) · `php vendor/bin/pest tests/ArchTest.php` зелёный · `composer check` зелёный (кроме mutation-ratchet, если отложен в P4 — назвать).
**Deliverables:** docs EN/RU в паритете под новый API; arch-тесты канона консолидированы; `composer check` зелёный (mutation — P4); фаза готова к cut-line/заморозке P3.
**Completion Notes:** —
**Pending Work:** —
**Known Deviations:** —
**Escalation Needed:** no

## Phase Handoff

—
