# P4.10 — local vendor debug stdout blocker (2026-07-22)

## Evidence

The P4.15 isolated worktree copied the repository's ignored `vendor/` directory and then
reported `669/669` passing Pest tests for the SQLite and recorded PostgreSQL seed commands.
Both Composer commands nevertheless exited 1 after repeated `[DEBUG-BATCH-QUERY]` output.
The same command-level artifact is absent from the MySQL full lane, which exited cleanly
after `669/669` tests.

`git status --ignored --short vendor` reports only ignored `vendor/`; no tracked P4.15 file
contains the marker. The workflow accepted for P4.10 performs `composer update`, so CI will
not consume this local ignored dependency tree.

## Verdict

The next P4.10 proof must use a new detached worktree and a fresh `composer update` there,
not a copied or symlinked local `vendor/`. This is an environment-integrity precondition,
not a product-code repair, and it preserves P4.15's class-local boundary. RAG:—
(repo-grounded: /tmp/azguard-p415-{sqlite,pgsql-seed,pgsql,mysql}.log; git status --ignored
--short vendor; .github/workflows/tests.yml:115-118).
