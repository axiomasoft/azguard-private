# Бриф — 2026.07.17-AZGUARD-TAILS

Источник: сообщение владельца в сессии сразу после мержа PR #91 («refactor: finish
IMPROVEMENT_PLAN (Phases 5-8)», main @ `bd3026f`), запрошено явно через
`/task:plan-design`.

## Исходная формулировка (переформулирована без потери мысли)

Закрыть хвосты T1–T7, зафиксированные в `REMAINDER_REPORT.md` (написан в этой же сессии
при закрытии PR #91) как НЕ закрытые этим PR. Явно расставленный владельцем приоритет:

- **T1 — главный приоритет.** Eloquent global query-scope в
  `packages/core/src/Concerns/HasScopedRoles.php` (`bootHasScopedRoles`) не panel-aware:
  фильтрует по `scope_class` независимо от `panel_id`. Permission-**check** путь
  (`hasScopedPermission`) уже изолирован по панели (F8, предыдущая фаза); query-**filtering**
  путь — нет. Тот же класс проблемы (граница изоляции панелей), что и F8.
- **T6.** Фикс TTL эпохи кеша (`e3e33c3`, коммит из фазы 2) внёс НОВУЮ гонку:
  `PermissionCache.php:95-99` делает `increment()` (атомарен на большинстве стораджей) и
  отдельный, неатомарный `put()` — конкурентные `forgetForUser()` могут откатить эпоху
  назад, и отозванный грант живёт до истечения TTL закешированного `PermissionSet`, а не
  немедленно.
- **T2–T5** — продуктовые/дешёвые правки, без явного приоритета владельца сверх порядка в
  отчёте.
- **T7** — явно указано: «YAGNI-закрыть без действия» — владелец не хочет тратить на него
  цикл выполнения, только формально закрыть решением.

Репозиторий: `/home/vostrikov/projects/packages/azguard`. `main` на момент авторинга —
`bd3026f` (мерж PR #91). Источники контекста для этого плана: `REMAINDER_REPORT.md`
(корень репо) и `IMPROVEMENT_PLAN.md` (колонка `Status`, добавленная тем же PR).

## Уточнения, полученные при авторинге (в этом же заходе)

- T1 и T6 — оба **correctness-critical** (граница изоляции между панелями / гонка в
  кеше прав), требуют effort=high и ручного запуска (Exec=manual по routing-гейту).
- T2 (семантика `removeScopedRole(panelId=null)` — сносить ли ВСЕ панели или только
  null-панель) — это **продуктовое решение владельца**, не техническая деталь: план НЕ
  принимает решение за него самовольно, вопрос вынесен в `open-questions.md` и
  `## Обсуждение` плана.
- T3–T5 — независимый scope, детерминированная Validation → фаза P2 подпадает под
  критерий обязательной оркестрации (SKILL `plan-protocol` §7): заведён workflow-скрипт
  `plans/2026.07.17-AZGUARD-TAILS/workflows/wf-azguard-tails-p2.js`.
- T7 закрывается прямо в этом плане статусом `⛔ Skipped by decision` — экзекуции не
  требует (см. `## 5. Decision Log` D3).
