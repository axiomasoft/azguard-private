# P1.4 — Adversarial review диффа фазы P1 (2026-07-18)

> Оркестратор: fable/high (Routing §3 P1.4, исполнено через `/task:plan-run`, D18).
> Субагенты: `azguard-security-review` (security), `azguard-reviewer` (корректность/канон).
> `azguard-blade-review` пропущен по ТЗ — дифф не задевает `resources/views`
> (`git diff --name-only bdf9416..HEAD -- 'resources/views' 'packages/*/resources/views'` пуст).

## Объект ревью

Дифф `bdf9416..HEAD` (база P1 = закрытие P0, `bdf9416`): 94 файла, ~2415 вставок / 441
удаление (без `plans/`), 29 item-коммитов волн P1.1–P1.3 + 3 bookkeeping-коммита плана.

## 1. Scope drift — чисто

Каждый из 29 item-коммитов атрибутируется ровно одной из 27 находок бэклога
(C-01=1, W1=12+1 fix-up, W2=14+1 сплит A-01); неатрибутируемых правок продукта в диффе НЕТ
(пофайловая сверка `git show --stat` каждого коммита против Scope Included волн).
Вне-Files дрейф P1.1–P1.3 покрыт Known Deviations соответствующих item'ов, кроме
недоучёта в P1.2 — см. находку R16.

## 2. Регрессионные тесты security-находок W0/W1 — 8/9 на входе, 9/9 после фикса

| Находка | Доказывающий тест | Вердикт на входе ревью |
|:--|:--|:--|
| C-01 console/queue bypass | `ScopedRolesConsoleQueueTest` (queue фильтрует, console-без-актора no-op, fail-closed без панели) | ✅ есть |
| C-02 on_missing_panel | `OnMissingPanelConfigTest` + re-baseline `ScopedRoleQueryScopePanelIsolationTest` (3 ветки) | ✅ есть |
| C-03 stale scope_class | `StaleScopeClassTest` + `DoctorCommandTest` | ✅ есть |
| C-04 TTL/store | `CacheConfigValidationTest` | ✅ есть |
| C-05 epoch без лока | `PermissionCacheEpochLockWarningTest` | ✅ есть |
| C-08 union-only | `ResourceEnforcementTest` (defer + «поздний before всё ещё грантит») | ✅ есть |
| C-10 morph write/read | `DirectGrantMorphMapTest` + context-кейс | ✅ есть |
| C-11 mass-assign | **отсутствовал** — ТЗ P1.2 явно требовало | ❌ → R0, закрыт `f0055ae` |
| C-13 wildcard в context | `ContextGrantBuilderWildcardTest` + defense-in-depth в `EffectivePermissionResolverTest` | ✅ есть |

## 3. Вердикты security-прохода (azguard-security-review)

0 находок Blocker/Major. Все 10 атакованных векторов (C-01, C-02, C-03, C-04/C-05, C-08,
C-10, C-11, C-12, C-13, C-16) — «закрыт, не воспроизводится»; остаточных `$user::class`/
`get_class()` в морф-путях нет (grep по core/context/filament); SQL-инъекций нет (биндинги +
`ESCAPE`), секретов в новых `Log::warning` нет; union-only соблюдён.
Side-note вне scope P1 (предсуществующее, не регрессия диффа): у Filament `RoleResource`
потенциально нет Policy-авторизации — кандидат для заметки P2 (Filament-кластер).

## 4. Находки ревью (сводно) и вердикты

Каждая Major верифицирована оркестратором по коду до фикса (plausible-but-unverified не
принимались); каждая чинящая правка несёт тест, падающий на до-фиксовом коде (проверено
stash-прогоном для R4/R6).

| # | Sev | Что (файл до фикса) | Вердикт | Фикс-коммит |
|:--|:--|:--|:--|:--|
| R4 | Major | C-10 хвост: read-path query-scope искал `scope_entity_type` по `static::class` (FQCN), write писал morph-алиас → под `enforceMorphMap` фильтр изоляции молча не применялся (fail-open) — `HasScopedRoles.php:88` | **снято** | `5c555f1` (+`ScopedRoleMorphMapTest`) |
| R6 | Major | C-11 хвост: `firstOrCreate()`+второй `save()` — обрыв между запросами персистит строку `scope_class=null` («logic-less», фильтр никогда не применится) — `HasScopedRoles.php:149-167` | **снято** | `69d9e1a` (один INSERT, catch unique-race; тест на единственный INSERT со scope_class) |
| R2 | Major | C-16 дыра: SQL UNIQUE считает NULL различными — дефолтная форма `assignScopedRole` (panel_id NULL) не защищалась вовсе — миграция 000005 | **снято** | `4bd209e` (COALESCE-expression index: SQLite/PG expression, MySQL functional parts + SUBSTRING 191; тест NULL-panel дубля) |
| R3 | Major | C-16 дыра: `up()` падает на предсуществующих дублях (до миграции их ничто не запрещало) | **снято** | `4bd209e` (дедуп: rebuild для model_has_roles без PK, keep-MIN(id) для model_has_scopes; тест down→дубли→up) |
| R1 | Major | Регрессия C-14: `JobProcessing` стреляет и для sync — `dispatch_sync` в HTTP-запросе обнулял панель посреди запроса (остаток — deny/`PanelNotSetException`) — `AzGuardServiceProvider.php:216` | **снято** | `c681ee9` (sync-исключение + re-baseline теста: реальный коннекшен сбрасывает, sync сохраняет) |
| R5 | Major | B-04 закрыт неполно: карта C-B6 включала `hasRole`, но он остался `string\|RoleInterface` — `hasRole(Enum::Case)` = TypeError при работающем `assignRole(Enum::Case)` | **снято** | `07ccbfe` (трейт+контракт до `string\|BackedEnum\|RoleInterface`, unwrap `PermissionKey::normalize`; parity-гейт зелёный) |
| R0 | Major | C-11 без доказывающего mass-assign теста (наруш. гейта «каждая security-находка W0/W1 несёт тест»; найдено оркестратором) | **снято** | `f0055ae` (`MassAssignmentGuardTest`: create/fill/update + firstOrCreate-values, пин премис-дефекта P1.2) |
| R8 | Minor | C-04 валидация сравнивала ИМЯ store, не драйвер: ложный fail для кастомного array-store, ложный pass для «array» на redis-драйвере | **снято** | `cb8c819` (решает `cache.stores.<store>.driver`; неизвестный store = персистентный, fail-closed; +3 теста) |
| R10 | Minor | C-15: `winningSource=null` при layer-only allow + возможная мисатрибуция — семантика нигде не заявлена | **снято** (док-контракт) | `8d91611` (докблок best-effort семантики) |
| R11 | Minor | Мёртвый `bypassScopedRolesConsoleGuard()` + ложный докблок про снятый C-01 console-guard | **снято** | `49238d9` |
| R16 | Minor | Bookkeeping: P1.2 Known Deviations недоучитывает вне-Files пути `SuperAdminCommand`/`SyncRolesCommand`/`RoleResource/Pages/{CreateRole,EditRole}`/`AzGuardManagerInterface`/`InvalidCacheConfigException`/`tests/Support/InteractsWithGuard` (найдено оркестратором) | **снято** (дополнение записи) | bookkeeping-коммит P1.4 |
| R7 | Minor | C-13 defense-in-depth работает только при выключенной wildcard-фиче: при `wildcard.enabled=true` голый `*` из кастомной MergeStrategy проходит catalog-фильтр (сам тест это честно комментирует) | **принято-как-риск → P2** (D18 флипает wildcard-семантику и переустраивает matcher; legacy-фича off по умолчанию; чинить дважды — переделка) | — |
| R9 | Minor | C-10 без upgrade-пути: morph-map-инсталляции с дозафиксовыми FQCN-строками грантов теряют их резолюцию после апгрейда (данные/docs-нота) | **принято-как-риск → P3.3** (глава UPGRADING «0.2→0.3» консолидирует все breaking P1+P2 — D21; занесено в Pending Work) | — |
| R12 | Nit | Reverse-parity D-03 сверяет только имена методов (не сигнатуры) в обратную сторону | **принято-как-риск → P2** (contract review углубит; прямая сторона сигнатуры сверяет) | — |
| R13 | Nit | No-effect `use` (Reflection* в ContractTraitParityTest, Closure в AbilitiesDtoTest) — PHP warning каждый прогон | **снято** | `49238d9` |
| R14 | Nit | Фикстура `Role::create([... 'panel_id'])` — не-fillable ключ тихо отбрасывался (вводящая в заблуждение) | **снято** | `4bd209e` |
| R15 | Nit | C-09: mass-update через query builder не стреляет событиями — флаша нет (предсуществующее свойство всех model-event хуков) | **снято** (док-контракт) | `0701d03` |

Blocker-находок нет; все Major сняты кодом+тестом → гейт «review-Blocker/Major сняты» выполнен.

## 5. Что ревьюер подтвердил чистым (выборка)

B-04 unwrap на всех границах (enum не утекает в SQL/кэш-ключи, ключи не раздваиваются);
C-02 re-baseline A1 честный (старое поведение — явный opt-in `all` с тем же ассертом);
C-09 `getOriginal()` до `syncOriginal()`; C-15 `sources()` без дублирования resolve(),
`check()` hot-path не тронут (инвариант §6 п.12); ослабленных ассертов в диффе нет (все
снятые `toBeFalse` — осознанный C-08 re-baseline); массовые правки тестов — механическая
замена на `createRoleWithClass()` (следствие C-11) с сохранением ассертов; named arguments
и `final`/`@internal` на новых классах.

## 6. Не проверено (честные границы)

MySQL-ветка миграции C-16 (functional key parts — локально/CI только SQLite; исполнится в
P4.2/P4.7 БД-матрицей); поведение под реальным Octane/queue-воркером (только unit-эмуляция
событий); id-стратегии uuid/ulid для grantable_id; docs-коммиты A-01/A-02/A-05/B-01
построчно (docs-only, парность держит `bin/docs-parity-gate.sh`).

## 7. Validation итогового дерева (на `49238d9`, код финален)

- `composer test` — **610 passed / 1639 assertions** (600 на входе + 10 новых ревью-тестов), не OOM.
- `composer lint:check` — pint passed; `composer analyse` — phpstan 0 errors, baseline не менялся.
- `bash bin/docs-parity-gate.sh` — OK.
- Финальный гейт фазы: 27 находок закрыты, review-находки Blocker/Major = 0 открытых.
